# 第一章　`=` 为什么不是“等于”

托马斯第一次见到 R-01，是在皇家自动化工坊最里面的一间实验室。

那台机器人身高只到他的肩膀，方脑袋，方胳膊，胸口嵌着一块红石能源核心。它安静地站在工作台旁，眼睛里闪着两点蓝光，看起来比村民交易大厅里那些只会“哼”来“哼”去的家伙聪明多了。

Notch 把一块写字板递给托马斯。

“今天的任务很简单，”他说，“让 R-01 学会修改一个数字。”

托马斯有些失望。他原以为第一天至少会造自动挖矿机，或者让机器人去下界堡垒偷烈焰棒。结果只改一个数字。皇家自动化工坊的入门任务，看起来和让钻石镐负责挖泥土差不多浪费。

工作台上有一个透明方盒，盒子正面写着一个字母：

```text
x
```

盒子里面放着三块金锭。

旁边的显示屏写着：

```cpp
x = 3;
```

“这是什么意思？”Notch 问。

“太简单了。”托马斯说，“x 等于 3。”

R-01 的眼睛亮了一下。

“记录：人类使用了‘等于’。”

“有什么问题吗？”托马斯问。

“有。”R-01 回答，“如果 `=` 表示数学中的等于，那么下面这条命令无法成立。”

屏幕上出现了一行新代码：

```cpp
x = x + 1;
```

托马斯盯着它看了两秒。

数学上，如果：

\[
x=x+1
\]

那么两边同时减去 \(x\)，就会得到：

\[
0=1
\]

这显然不可能。哪怕村民把绿宝石价格涨到一组泥土换半颗钻石，零也不会突然等于一。

“这条式子是错的。”托马斯说。

R-01 伸出机械手，打开标着 `x` 的方盒，从里面取出三块金锭，又从旁边拿来一块，最后把四块金锭全部放回盒子。

显示屏变成：

```text
x = 4
```

“执行成功。”机器人说。

托马斯愣住了。

Notch 笑了笑。

“欢迎来到编程世界。这里第一个需要忘掉的东西，就是你对等号的部分理解。”

---

## 一、同一个符号，两种完全不同的工作

在数学中，等号表示：

> 左边和右边具有相同的值。

例如：

\[
2+3=5
\]

这是一种关系。它告诉我们，左边计算出来和右边一样。

又例如：

\[
x+2=7
\]

这里的 \(x\) 是未知数。我们要找出一个值，使左右两边相等。

但在程序中：

```cpp
x = 3;
```

通常不是在宣布一个永远成立的数学关系，也不是让计算机解方程。

它是在下命令：

> 先计算右边的值，再把结果写进左边的变量。

所以程序中的 `=` 更接近：

```text
把右边的值放进左边的格子
```

而不是数学里的：

```text
左边和右边永远相等
```

为了避免混乱，我们把程序中的 `=` 叫作：

# 赋值符号

“赋值”就是：

> 给一个变量放入一个值。

R-01 在写字板上画了一个方盒：

```text
┌───────┐
│   x   │
│   3   │
└───────┘
```

“变量 `x` 是一个有名字的存储格子。”它说，“执行 `x = 3`，表示把数值 3 写入这个格子。”

托马斯问：“那是不是以后 `x` 永远等于 3？”

R-01 没有回答，而是执行了：

```cpp
x = 8;
```

盒子里的三块金锭被取走，换成了八块铁锭。

```text
执行前：x = 3
执行后：x = 8
```

“原来的 3 呢？”托马斯问。

“已被覆盖。”

“你怎么不保存一下？”

“命令中没有要求保存。”

托马斯沉默了片刻。他开始怀疑，R-01 的主要用途不是自动化，而是非常精确地指出人类说话有多不负责任。

---

## 二、赋值真正执行了三步

看起来只有一行：

```cpp
x = x + 1;
```

但计算机实际完成了三个动作。

假设开始时：

```text
x = 3
```

第一步，读取右边需要的旧值：

```text
读取 x，得到 3
```

第二步，计算右边：

```text
3 + 1 = 4
```

第三步，把结果写回左边：

```text
把 4 写入 x
```

于是：

```text
旧状态：x = 3
新状态：x = 4
```

这条语句的正确读法不是：

> x 等于 x 加一。

而是：

> 取出 x 原来的值，加一，再把结果存回 x。

为了看清变化，可以写成状态表：

| 时刻 | `x` 的值 | 正在做什么 |
|---|---:|---|
| 开始 | 3 | 读取旧的 `x` |
| 计算中 | 3 | 算出 `3+1=4` |
| 结束 | 4 | 用 4 覆盖原来的 3 |

这里最关键的是：

> 右边的 `x` 指的是执行前的旧值，左边的 `x` 是准备接收新值的格子。

它们写着同一个名字，但在这一行里承担不同角色。

右边负责“提供旧数据”，左边负责“接收新数据”。

人类用同一个字母同时扮演两个角色，随后让初学者自己困惑，算是编程语言设计中非常经典的传统项目。

---

## 三、变量不是数学里的未知数

托马斯以前学数学时，看到 \(x\)，第一反应是：

> 这是一个不知道多少的数，要把它算出来。

但程序中的变量并不一定是未知数。

它更像一个带标签的箱子。

例如：

```cpp
int apples = 5;
```

可以想成：

```text
名字：apples
内容：5
```

后来吃掉一个苹果：

```cpp
apples = apples - 1;
```

执行过程是：

```text
读取旧值 5
计算 5 - 1 = 4
写回 apples
```

现在：

```text
apples = 4
```

再获得三个苹果：

```cpp
apples = apples + 3;
```

得到：

```text
apples = 7
```

所以程序变量最重要的特点不是“未知”，而是：

> 它的值可以随着程序执行不断变化。

数学中的一个方程通常在描述一个固定关系，而程序中的变量是在记录一个过程。

数学关心：

> 它们之间是什么关系？

程序关心：

> 此刻它是什么值？下一步又会变成什么值？

这也是为什么编程常被称为处理“状态”的过程。

所谓状态，就是：

> 某一时刻，所有变量当前保存的值。

例如：

```text
apples = 7
diamonds = 3
day = 12
hasKey = true
```

这些值合在一起，就是程序此刻的状态。

每执行一条语句，状态就可能发生变化。

---

## 四、代码不会修改过去，只会覆盖现在

Notch 在桌上放了两个方盒。

```text
x = 3
y = 5
```

然后让托马斯预测下面代码的结果：

```cpp
x = y;
```

托马斯说：“x 和 y 相等。”

“别用‘相等’糊弄机器人。”Notch说，“说动作。”

托马斯重新回答：

“读取 `y` 的值 5，把 5 写入 `x`。”

R-01 执行后，两个盒子变成：

```text
x = 5
y = 5
```

“原来的 `x=3` 呢？”Notch问。

“被覆盖了。”

“`y` 变了吗？”

“没有。因为我们只是读取它，没有写它。”

这就是赋值的方向性：

```cpp
x = y;
```

表示：

```text
从 y 读取
向 x 写入
```

它不是双向的。

执行后，`x` 得到 `y` 的值，但 `y` 不会得到 `x` 原来的值。

就像把一本书的内容复制到另一张纸上，原书不会因此自动变成白纸。可惜很多学生第一次写交换代码时，会期待计算机表现出这种超自然礼貌。

---

## 五、顺序一换，结果就变了

R-01 在显示屏上写出两组代码。

第一组：

```cpp
x = 3;
x = x + 2;
```

结果：

```text
x = 5
```

第二组：

```cpp
x = x + 2;
x = 3;
```

假设 `x` 开始也是 0，那么执行过程是：

```text
先变成 2
再被覆盖成 3
```

最终：

```text
x = 3
```

使用的语句差不多，顺序不同，结果完全不同。

原因很简单：

> 程序不是一组同时成立的数学式子，而是一串按顺序执行的命令。

每执行一行，变量状态就可能改变。下一行看到的，是已经被上一行修改过的新世界。

例如：

```cpp
int x = 2;
x = x * 3;
x = x + 4;
```

逐帧执行：

| 执行阶段 | `x` |
|---|---:|
| 初始化 | 2 |
| 乘以 3 后 | 6 |
| 加 4 后 | 10 |

最终结果是：

```text
x = 10
```

但换成：

```cpp
int x = 2;
x = x + 4;
x = x * 3;
```

状态变成：

| 执行阶段 | `x` |
|---|---:|
| 初始化 | 2 |
| 加 4 后 | 6 |
| 乘以 3 后 | 18 |

最终是：

```text
x = 18
```

数学里：

\[
2\times3+4=10
\]

和：

\[
(2+4)\times3=18
\]

本来就不同。代码只是把这种先后顺序更加赤裸地展开了。

---

## 六、一次赋值，只改变左边那个格子

托马斯看着下面的代码：

```cpp
int x = 3;
int y = x + 2;
```

他问：“执行第二句后，`x` 会不会也变成 5？”

R-01 回答：

“不会。第二句左边是 `y`。”

执行过程是：

1. 读取 `x`，得到 3；
2. 计算 `3+2`，得到 5；
3. 把 5 写入 `y`。

结果：

```text
x = 3
y = 5
```

判断一条赋值语句会改变谁，有一个极简单却极有用的办法：

> 看等号左边是谁。

例如：

```cpp
a = b + c;
```

只会直接修改 `a`。

```cpp
score = score + bonus;
```

只会修改 `score`。

```cpp
items[i] = value;
```

只会修改数组中的 `items[i]` 这一格。

右边的变量只是被读取，不会因为“参与计算”而自动变化。

这条规则看起来简单，却是以后理解复杂代码的地基。

---

## 七、写代码前，先问四个问题

Notch 在写字板上写下：

```cpp
x = x + y;
```

“不要急着算。”他说，“先回答四个问题。”

### 第一问：现在的旧值是什么？

假设：

```text
x = 3
y = 5
```

### 第二问：右边会算出什么？

```text
x + y = 3 + 5 = 8
```

### 第三问：结果写到哪里？

写入：

```text
x
```

### 第四问：谁被覆盖了？

`x` 原来的值 3 被覆盖。

最终：

```text
x = 8
y = 5
```

这四问可以用来读几乎所有赋值语句：

1. 旧值是什么？
2. 右边算出什么？
3. 写入哪个位置？
4. 哪个旧值会消失？

以后遇到数组、递推、交换、插入时，真正困难的部分仍然是这四个问题。代码会越来越长，但麻烦的本质没有变，只是人类给它穿了更多括号。

---

## 八、错误博物馆：把赋值当成数学方程

皇家自动化工坊有一间“错误博物馆”，专门收藏那些看起来合理、执行后却足以让村民举报的程序。

第一件展品是：

```cpp
int x = 3;
int y = 5;

x = y;
y = x;
```

设计者本来想交换 `x` 和 `y`。

数学目标是：

```text
新 x = 旧 y
新 y = 旧 x
```

也就是：

```text
新 x = 5
新 y = 3
```

但代码逐行执行。

初始：

```text
x = 3
y = 5
```

第一句：

```cpp
x = y;
```

结果：

```text
x = 5
y = 5
```

原来的 `x=3` 已经消失。

第二句：

```cpp
y = x;
```

读取的 `x` 已经是 5，因此：

```text
x = 5
y = 5
```

交换失败。

问题不在于数学目标错误，而在于：

> 数学中的两个变化希望同时发生，程序中的两句赋值却只能依次执行。

这是后面“为什么需要临时变量”的起点。

但在本章先记住一句：

> 每次赋值都会立刻改变状态，后面的代码只能看到改变后的结果。

---

## 九、给代码配音

Notch 不允许托马斯把代码读成符号。

看到：

```cpp
x = x + 1;
```

不能读：

> x 等于 x 加一。

应该配音成：

> 取出 `x` 原来的值，加一，再存回 `x`。

看到：

```cpp
y = x;
```

应该读：

> 复制 `x` 当前的值，写入 `y`。

看到：

```cpp
x = 0;
```

应该读：

> 把 0 写进 `x`，覆盖它之前保存的内容。

看到：

```cpp
sum = sum + value;
```

应该读：

> 把新的 `value` 加入目前的总和，再把新总和存回 `sum`。

一个学生只要能把代码配音成连续动作，就不容易再把赋值误解为数学等式。

因为他脑中看到的不再是一排符号，而是变量格子中的值正在变化。

---

## 十、本章挑战：预测 R-01 的下一步

### 挑战一

```cpp
int x = 4;
x = x + 3;
```

执行后 `x` 是多少？

答案：

```text
7
```

过程：

```text
读取旧值4
计算4+3
写回7
```

---

### 挑战二

```cpp
int x = 2;
int y = x;
x = 9;
```

最终 `x` 和 `y` 分别是多少？

答案：

```text
x = 9
y = 2
```

因为：

```cpp
y = x;
```

复制的是当时 `x` 的值 2。后来 `x` 改成 9，不会自动影响 `y`。

普通变量没有心灵感应。

---

### 挑战三

```cpp
int x = 3;
x = x * 2;
x = x - 1;
```

状态变化：

```text
3 → 6 → 5
```

最终：

```text
x = 5
```

---

### 挑战四

```cpp
int a = 2;
int b = 5;
a = b + 1;
b = a + 1;
```

逐步推演：

初始：

```text
a = 2
b = 5
```

第一句后：

```text
a = 6
b = 5
```

第二句读取的是新 `a=6`：

```text
a = 6
b = 7
```

最终：

```text
a = 6
b = 7
```

---

### 挑战五

下面两段代码结果一样吗？

第一段：

```cpp
int x = 2;
x = x + 3;
x = x * 4;
```

第二段：

```cpp
int x = 2;
x = x * 4;
x = x + 3;
```

第一段：

```text
2 → 5 → 20
```

第二段：

```text
2 → 8 → 11
```

结果不同。

程序的顺序不是排版习惯，而是执行规则。

---

## 十一、这一章真正要记住什么

托马斯离开实验室时，Notch 没让他背任何代码，只让他在门口的黑板上写下四句话：

> 数学中的 `=` 表示相等关系。

> 程序中的 `=` 通常表示赋值。

> 赋值会先计算右边，再写入左边。

> 新值写进去以后，左边原来的旧值会被覆盖。

R-01 看了一遍，补充了一句：

> “除非人类提前保存。否则丢失的数据不会因为懊悔而回来。”

托马斯看着那个标着 `x` 的透明盒子，终于明白：

代码并不是把数学公式换一种符号写出来。

数学可以站在时间之外，描述一个关系。

程序却必须走进时间里，一步一步改变现实。

而这一切，都从一个看起来最熟悉、实际上最容易误解的符号开始：

```cpp
=
```
