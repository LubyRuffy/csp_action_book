# 第一章　`=` 为什么不是“等于”

时间往前拨回三天。托马斯第一次走进皇家自动化工坊时，序章里的矿车还没到，R-01 也还没有学会对他的命令提出十七项补充要求。

当天的任务是给村民交易柜台更新绿宝石余额。透明登记格里原本有 3 颗绿宝石，柜台刚收到 1 颗，余额应当变成 4。

托马斯很快写下自己的命令。现有余额是 3，要“增加一”，于是他把 1 写进了余额格：

```cpp
emeralds = 1;
```

R-01 取走原来的 3，把 1 放了进去。

村民低头看着余额，随后发出一声极长的“哼”。这声音通常表示两件事：账不对，以及有人很快要写事故报告。

“我说增加一，不是只剩一！”托马斯喊道。

“命令要求把 1 写入 `emeralds`。”

R-01 在屏幕上给出另一种写法：

```cpp
emeralds = emeralds + 1;
```

托马斯更不满意了：“这在数学上是假的。两边减去 `emeralds`，会得到 $0=1$。”

Notch 正好推门进来。他看了看余额格，又看了看已经开始计算赔偿金额的村民。

“第一天就碰上它了，”他说，“长得最像数学等号，干的却是搬运工的活。”

---

## 1.1 数学中的等号：左右两边相同

数学里的等号描述关系：左边和右边有相同的值。

\[
2+3=5
\]

这句话不要求谁先动，也没有格子被改写。把两边调换位置仍然成立：

\[
5=2+3
\]

方程里的等号也一样：

\[
x+2=7
\]

这里要找一个 $x$，使左右两边相等。等号像一架天平，关心两边是否平衡。

程序中的单个 `=` 做的是另一件事。它不是天平，而是一条带方向的命令：

```text
计算右边 → 把结果写进左边
```

在 C++ 中，这个符号叫赋值运算符。赋值的目标必须是一个能被写入的位置，所以可以写：

```cpp
x = 5;
```

却不能反过来写：

```cpp
5 = x;
```

数字 5 不是储物格。R-01 可以把一件东西放进箱子，却不能把箱子放进“数字五”这个概念里。数学等号允许换边，赋值箭头不允许。

如果只想检查两个值是否相等，C++ 使用 `==`。本章先盯住单个 `=`：它负责改状态，不负责宣布真理。

---

## 1.2 程序中的等号：计算右边，写入左边

事故发生前，登记格的内存状态是：

```text
名字：emeralds
内容：3
```

执行 `emeralds = 1;` 时，R-01 没有理解“增加”的愿望。它只看见两个明确动作：

1. 右边的结果是 1；
2. 把 1 写进左边的 `emeralds`。

写入以后：

```text
名字：emeralds
内容：1
```

原来的 3 被覆盖了。它没有藏在变量背面，也没有进入自动恢复区。托马斯的本意更不会被编译器当作备份。

这就是内存视角。赋值语句关心三个东西：

- 右边要读取哪些当前值；
- 右边最后算出什么；
- 左边哪个格子会被改写。

可以把 `left = right;` 配音成：

> 读取 `right` 当前保存的值，复制一份，写入 `left`。

右边通常只被读取，左边才被写入。执行 `x = y;` 后，`x` 得到 `y` 当前的值，`y` 自己不会因此改变。

| 步骤 | `x` | `y` | 发生的动作 |
|---|---:|---:|---|
| 初始 | 3 | 8 | 两个格子各有一个值 |
| 读取右边 | 3 | 8 | 从 `y` 读到 8 |
| 写入左边 | 8 | 8 | 8 覆盖 `x` 中的 3 |

这不是交换。它只是复制一次，然后覆盖一次。真正的交换要处理旧值的去向，第四章会专门解决这场麻烦。

---

## 1.3 `x = 5` 到底发生了什么

Notch 清空一个透明格，在外面贴上 `x`。

“现在让它保存 5。”

动手写代码前，托马斯先回答几个问题：

- 现在已知：有一个可写入的整数格 `x`；
- 需要记住：当前数值 5；
- 下一步改变：`x` 的内容；
- 旧值是否还需要：格子刚建立，没有需要保留的旧内容；
- 执行次数：一次。

于是代码只有一行：

```cpp
int x = 5;
```

这行同时完成了两件事：准备一个名为 `x` 的整数格，并把 5 作为初始内容写进去。

后来又执行：

```cpp
x = 9;
```

格子名字仍叫 `x`，内容却从 5 变成了 9。变量名回答“这是哪个格子”，变量值回答“这个格子现在装什么”。两者不是一回事。

| 时刻 | 执行语句 | `x` 的内容 | 已经消失的旧值 |
|---:|---|---:|---:|
| 0 | 尚未建立 | 未定义 | 无 |
| 1 | `int x = 5;` | 5 | 无 |
| 2 | `x = 9;` | 9 | 5 |

数学里写 $x=5$，常常是在描述一个关系或给出一个条件。程序里执行 `x = 5;`，则是在某个时刻做一次写入。下一条语句完全可以再次改它。

R-01 把这一点记进事故档案：“变量格允许重复使用。人类对此的另一种称呼是‘怎么刚才还不是这个数’。”

---

## 1.4 `x = x + 1` 为什么在程序里成立

交易柜台的余额恢复为 3 后，任务仍然是增加 1。

数学目标可以写成新旧两个时刻：

\[
\text{新余额}=\text{旧余额}+1
\]

如果用撇号区分，会更清楚：

\[
x'=x+1
\]

程序把“新 `x`”和“旧 `x`”压进了同一个名字里：右边读取旧值，左边接收新值。

执行 `x = x + 1;` 时，机器依次做三件事：

1. 从右边读取旧 `x`；
2. 用旧值加 1，得到计算结果；
3. 把结果写回左边的 `x`。

假设旧 `x=3`，逐帧过程是：

| 帧 | `x` 格中的值 | 暂时算出的结果 | 说明 |
|---:|---:|---:|---|
| 0 | 3 | 未产生 | 语句尚未执行 |
| 1 | 3 | 4 | 右边读取 3，并算出 4 |
| 2 | 4 | 4 | 结果写回，旧 3 被覆盖 |

在整条右边算完之前，左边不会先改。因此右边仍能安全读到旧的 3。

这句代码不应该读成“`x` 等于 `x` 加一”，而应该读成：

> 取出 `x` 当前的值，加一，再把新值存回 `x`。

托马斯重新更新柜台：

```cpp
emeralds = emeralds + 1;
```

余额这次从 3 变成 4。村民数了两遍，暂时取消了赔偿申请。

---

## 1.5 旧值、计算结果和新值

同一个赋值语句中，至少有三个时刻值得分开：

```cpp
score = score + bonus;
```

假设执行前 `score=40`、`bonus=5`：

- 旧值：`score` 里原本的 40；
- 计算结果：右边算出的 45；
- 新值：写回以后 `score` 里的 45。

`bonus` 只被读取，所以仍然是 5。`score` 同时出现在两边，但作用不同：右边的它提供旧数据，左边的它指定写入位置。

| 执行阶段 | `score` | `bonus` | 右边结果 |
|---|---:|---:|---:|
| 执行前 | 40 | 5 | 未计算 |
| 右边计算完成 | 40 | 5 | 45 |
| 写回以后 | 45 | 5 | 45 |

写代码前，可以用五个问题把动作逼出来：

1. 现在已知什么？旧分数 40，奖励 5。
2. 需要记住什么？新分数要由旧分数算出。
3. 下一步改谁？改 `score`。
4. 旧分数以后还要不要？只在本次右边计算中需要。
5. 先做什么？先算右边，再写左边。

回答完这些，代码才出现：

```cpp
int score = 40;
int bonus = 5;
score = score + bonus;
```

代码不是把数学式换成分号。它把“旧状态如何变成新状态”写成机器能照做的动作。

---

## 1.6 错误博物馆：把赋值当成方程求解

### 展品一：把“增加一”写成“改成一”

```cpp
emeralds = 1;
```

若旧余额是 3，执行后就是 1。右边没有读取旧余额，所以这条语句不可能完成“在原数上增加”。

正确的状态关系是：

\[
\text{新余额}=\text{旧余额}+1
\]

对应动作才是：

```cpp
emeralds = emeralds + 1;
```

### 展品二：试图给常量赋值

```cpp
5 = x;
```

数学等式可以左右交换，赋值不行。左边必须是可写入的位置，5 只是一个值。编译器会拒绝这条命令，态度比村民简短得多。

### 展品三：以为赋值建立了永久关系

```cpp
int width = 4;
int height = 3;
int area = width * height;
width = 10;
```

执行第三行时，`area` 得到 12。后来 `width` 变成 10，`area` 仍是 12，因为面积公式只计算了那一次。

| 步骤 | `width` | `height` | `area` |
|---|---:|---:|---:|
| 建立三个变量后 | 4 | 3 | 12 |
| `width = 10;` 后 | 10 | 3 | 12 |

如果希望面积更新，必须再次执行：

```cpp
area = width * height;
```

程序不会守在公式旁边，发现谁变了就主动重算。它只执行写出来的命令。

---

## 翻译训练

### 训练一：数学到代码

旧金币数为 `coins`，捡到 3 枚后，新金币数满足：

\[
coins'=coins+3
\]

先说动作，再写代码。答案是：读取旧 `coins`，加 3，把结果写回同一格。

```cpp
coins = coins + 3;
```

### 训练二：代码到状态

初始 `x=4`：

```cpp
x = x * 2;
x = x - 3;
```

请补状态轨迹：

| 步骤 | `x` |
|---|---:|
| 初始 | 4 |
| 乘 2 后 | 8 |
| 减 3 后 | 5 |

最终值是 5，但更重要的是能说出 `4 → 8 → 5`。

### 训练三：找出被覆盖的值

初始 `a=2、b=7`，执行：

```cpp
a = b;
```

回答三个问题：读取谁，写入谁，哪个旧值消失？

答案：读取 `b` 中的 7，写入 `a`，旧 `a=2` 被覆盖；`b` 仍是 7。

### 训练四：给代码配音

```cpp
count = count + 1;
```

不要读成数学等式。配音应当是：读取目前的 `count`，加一，再把新的计数写回 `count`。

托马斯在事故簿最后写下：

> 程序中的 `=` 有方向。右边提供当前值，左边接收计算结果。

R-01 补了一句：“写入前若不关心旧值，写入后通常也没有机会再关心。”

托马斯看向那个仍贴着 `emeralds` 的登记格。等号的问题暂时解决了，新的问题却摆在眼前：等号左边这个叫变量的东西，究竟只是一个数学字母，还是机器真正用来记事的地方？
