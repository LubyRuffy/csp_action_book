# 第九章　最大值不是突然出现的

皇家仓库新开了一间寒冰库。门口的温度计依次记录了五次温度：

```text
-8  -3  -11  -5  -6
```

村民主管要求 R-01 报告“最高温度”。托马斯觉得这比数绿宝石还简单，随手写下：

```cpp
int maximum = 0;
```

R-01 检查完所有记录，郑重宣布：“最高温度是零度。”

“可记录里根本没有零！”托马斯说。

“人类提前把零放上冠军台，我未找到比它更大的候选者。”

寒冰库里的村民听说温度凭空上升了八度，立刻要求减少取暖煤炭配给。五分钟后，所有人都围住了托马斯。

Notch 把门重新关好：“今天要解决的不是怎么使用 `max` 函数，而是数学里的最大值怎样在机器眼前一步一步长出来。”

---

## 一、数学里的 `max` 藏起了比较过程

数学可以直接写：

\[
M=\max(a_0,a_1,\ldots,a_{n-1})
\]

这个式子告诉我们最终关系：$M$ 不小于序列中的任何元素，并且等于其中至少一个元素。

人站在纸外，可以一眼看见全部数字。R-01 却只能按顺序检查。它看到第一个数时，还不知道后面会不会出现更大的数；看到第三个数时，也不能忘记前面最好的是谁。

所以程序需要把“最终最大值”改写成一个过程中的状态：

> `maximum` 保存目前已经看过的元素中的最大值。

“目前”两个字不能删。处理尚未开始时，最终答案还没有出现；每处理一个新元素，`maximum` 才可能向最终答案靠近。

---

## 二、先让第一个元素站上冠军台

现在已知数组非空：

```text
a = [-8, -3, -11, -5, -6]
```

程序要记住两件事：

1. 当前检查到哪个下标；
2. 已检查部分的最大值。

最稳妥的初始状态不是凭空编一个数字，而是让真实存在的第一个元素先当候选者：

```cpp
int maximum = a[0];
```

执行后：

```text
已检查区域：a[0]
maximum = -8
```

这个候选者不一定能坚持到最后，但它至少来自数据。接下来从下标 `1` 开始，因为 `a[0]` 已经检查过了。

每遇到新元素，只问一个问题：

> 它比目前的候选者更大吗？

如果是，就换冠军；如果不是，什么也不做。

---

## 三、逐帧看见最大值形成

| 正在检查 | 当前元素 | 比较前 `maximum` | 是否更新 | 比较后 `maximum` |
|---|---:|---:|---|---:|
| `a[0]` | -8 | 未定义 | 用第一个元素初始化 | -8 |
| `a[1]` | -3 | -8 | -3 更大 | -3 |
| `a[2]` | -11 | -3 | 不更新 | -3 |
| `a[3]` | -5 | -3 | 不更新 | -3 |
| `a[4]` | -6 | -3 | 不更新 | -3 |

表中每一行都能翻译成仓库动作：R-01 拿着一张“当前最高温度”记录卡，读到更高的温度才把卡片改掉。

处理完前 $k$ 个元素时，记录卡保存的是这 $k$ 个元素中的最大值。循环结束时，$k=n$，它自然就变成全部元素的最大值。

代码不是从公式里突然蹦出来的。先有状态含义，再有更新规则，最后才有循环：

```cpp
int maximum = a[0];
for (int i = 1; i < n; i++) {
    if (a[i] > maximum) {
        maximum = a[i];
    }
}
```

这里没有神秘技巧：

- `maximum = a[0]` 准备第一个真实候选者；
- `i = 1` 表示从第二个元素继续；
- `i < n` 保证只访问有效下标；
- 条件成立时才更新候选者。

---

## 四、为什么不能随便从零开始

托马斯最初的代码在全是正数时常常“看起来没事”：

```text
3  8  2  9  5
```

零比这些数都小，真正的最大值迟早会把它替换掉。问题被好运遮住了。

但面对全负数：

```text
-8  -3  -11
```

如果 `maximum` 从 `0` 开始，任何元素都不大于零，候选者永远不会更新。程序最后报告一个根本不在数据中的数。

初始化必须回答：

> 在刚处理完第一个元素时，当前最大值应该是什么？

答案只能是第一个元素本身。若题目明确保证所有数都大于某个下界，也可以使用那个下界，但对初学者而言，用第一个元素更直观，也更不容易把题目条件记错。

空数组是另一个问题：没有 `a[0]`，也没有最大值。程序必须先保证 `n > 0`，或者单独规定空数据该怎样处理。不能一边访问不存在的第一格，一边期待苦力怕替你提交异常报告。

---

## 五、最大值和最大值的位置不是同一个格子

仓库主管接着问：“最高温度出现在哪一次记录？”

只保存 `maximum` 不够。数值 `-3` 告诉我们最高温度是多少，却没有告诉我们它在哪个下标。

于是增加一张位置卡：

```cpp
int maximum = a[0];
int maxPosition = 0;

for (int i = 1; i < n; i++) {
    if (a[i] > maximum) {
        maximum = a[i];
        maxPosition = i;
    }
}
```

两个变量必须在同一次发现新冠军时一起更新：

| 时刻 | `maximum` | `maxPosition` | 含义 |
|---|---:|---:|---|
| 初始化 | -8 | 0 | 当前冠军是 `a[0]` |
| 看到 `a[1]` | -3 | 1 | 新值和新位置一起登记 |
| 后续检查 | -3 | 1 | 没有更大值，不改记录 |

如果只改数值、不改位置，记录卡就会写成“冠军是 -3，站在 0 号位置”，而 `a[0]` 明明是 -8。两张卡描述的是同一个候选者，不能各自去过不同的人生。

当最大值出现多次时，条件也决定保留哪个位置：

- 使用 `>`，保留第一次出现的位置；
- 使用 `>=`，后出现的相同最大值会替换位置，最终留下最后一次。

这不是谁更正确，而是程序必须把题目想要的规则说清楚。

---

## 六、最小值只是把比较方向反过来

求最小值时，状态仍是“目前已经看过的元素中的最好候选者”，只是“最好”改成最小：

```cpp
int minimum = a[0];
for (int i = 1; i < n; i++) {
    if (a[i] < minimum) {
        minimum = a[i];
    }
}
```

初始值仍使用第一个元素。不能因为求最小值就随手从零开始；若数据全为正数，零同样会成为一个不存在的假冠军。

最大值和最小值的共同翻译模式是：

\[
\text{最终最优值}
\rightarrow
\text{目前最优值}
\rightarrow
\text{遇到更优者才更新}
\]

---

## 七、错误博物馆

### 展品一：零号幽灵

```cpp
int maximum = 0;
```

面对 `[-8,-3,-11]`，最终得到 0。错误不是比较写错，而是初始状态不属于已处理数据。

### 展品二：每次都更新

```cpp
for (int i = 1; i < n; i++) {
    maximum = a[i];
}
```

这段代码最后保存的是最后一个元素，不是最大值。候选者只有在新元素更大时才应更换。

### 展品三：数值换人，位置没换

```cpp
if (a[i] > maximum) {
    maximum = a[i];
}
```

若题目还要求位置，这里漏掉了 `maxPosition = i`。错误来自两个状态没有同步更新。

### 展品四：从 `i = 0` 再比一次

这通常不会改变答案，但重复检查了第一个元素。它不是灾难，却说明程序员没有说清楚“哪些数据已经处理”。状态解释含糊时，循环边界迟早会找到更昂贵的方式报复。

---

## 八、翻译训练

1. 数组为 `[4,9,2,9]`，使用 `>` 更新位置。写出 `maximum` 与 `maxPosition` 的轨迹。
2. 把条件改成 `>=` 后，最终位置为何变化？
3. 找出 `[-7,-2,-5]` 的最小值，补全状态表。
4. 解释为什么求最大值时不能无条件把初始值设为 0。
5. 给下面代码配音：

```cpp
if (a[i] > maximum) {
    maximum = a[i];
    maxPosition = i;
}
```

参考要点：第一题的位置轨迹为 `0→1→1→1`，最终保留第一次出现的 9；第二题会保留最后一个 9 的位置 3；配音应说明“当前元素成为新候选者，数值卡和位置卡同时更新”。

---

离开寒冰库前，托马斯把真实温度改回了 `-3`。村民主管重新发放煤炭，并要求在所有程序旁加一条规定：“未经数据同意，零不得自行参加冠军竞选。”

R-01 在记录卡背面写下本章结论：

> 最大值不是循环结束时突然出现的。`maximum` 每一步都保存已检查部分的最大值，处理完全部数据后，它才成为最终答案。
