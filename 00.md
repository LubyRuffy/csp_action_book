# 序章　数学会瞬移，计算机只能一步一步走

故事先从皇家自动化工坊的一次夜班说起。

那时托马斯已经认识 R-01 三天。至于他们第一天为了一个等号争论了多久，后面再说。眼下更急的是矿车：北矿区送来了三车红石，数量分别是 3 箱、5 箱和 2 箱，午夜前必须登记总数，否则值班村民会按照“未登记物资”把它们重新卖给仓库。

托马斯在纸上一划：

\[
S=3+5+2=10
\]

“十箱。”他说，“把总数写到登记牌上。”

R-01 看着三辆矿车，没有动。

“为什么不写？”

“你提供了最终关系，没有提供执行步骤。”

“加起来啊。”

“‘起来’不是 C++ 指令。”

值班村民抱紧账本，发出一声很有财产意识的“哼”。托马斯只好补充：“先拿 3，再拿 5，再拿 2，最后就是 10。”

R-01 立刻照办。它先把登记牌写成 3，又擦掉改成 5，再擦掉改成 2，最后报告：“已依次拿取。登记结果为 2。”

“我说的是加！”

“你没有说明前面的数量需要保存。”

托马斯终于发现，自己脑中那个一眼就能看到的 10，到了机器面前竟然缺了好几级台阶。

---

## 一、托马斯明明知道答案

人看见 `3、5、2`，很容易直接跳到 10。纸上的公式也允许这种跳跃：等号左边写总和，右边摆出所有数字，关系已经完整。

可 R-01 所处的不是纸面，而是时间。它先看见 3 时，还没处理 5 和 2；看见 5 时，又必须记得前面已经有 3。最终的 10 并不会提前躲在某个格子里等它发现。

数学视角关心的是：

\[
S=a_0+a_1+a_2
\]

程序视角必须补出另一句话：

> 每处理一个数字，就把它加入“目前的总和”。

“目前”两个字很要紧。它把一个静止答案拆成了会变化的状态。

托马斯在工作台上放了一个透明格子，标签写着 `sum`。

“这个格子装 10。”他说。

“现在还不能装 10。”Notch 从门口走进来，“R-01 刚开始时只该知道：一个数都没加进去，所以目前总和是 0。”

这就是内存视角。三辆矿车的数量放在三个格子里，另有一个格子记住已经处理过的总量：

```text
a[0]  a[1]  a[2]     sum
  3      5      2       0
```

`sum` 不是数学题额外赠送的未知数。它是机器为了走到下一步而需要的记忆。

---

## 二、R-01 为什么还是不会做

托马斯说：“那就从 0 开始，每次加一个。”

“加哪个？”

“当前那个。”

“当前是谁？”

托马斯指了指第一辆矿车，随后又指向第二辆、第三辆。他的手指移动得很自然，R-01 却需要一个明确的数字来表示当前位置。于是又出现了一个格子：`i`。

`i=0` 时处理第一车，`i=1` 时处理第二车，`i=2` 时处理第三车。每处理完一车，`i` 增加一；当 `i` 到 3 时停止，因为有效位置只有 0、1、2。

同一个任务，现在有了两种状态：

- `sum` 记住已经加了多少；
- `i` 记住当前走到哪一车。

逐帧看，10 并没有瞬移出来：

| 时刻 | `i` | 当前读取 | `sum` 更新前 | `sum` 更新后 |
|---|---:|---:|---:|---:|
| 尚未处理 | 0 | — | 0 | 0 |
| 处理第一车 | 0 | 3 | 0 | 3 |
| 处理第二车 | 1 | 5 | 3 | 8 |
| 处理第三车 | 2 | 2 | 8 | 10 |
| 停止 | 3 | — | 10 | 10 |

R-01 看着表格说：“确认。最终的 10 是状态 0 经三次更新后的结果。”

值班村民问：“能不能简单说人话？”

“不能。你们的账本也有十七页附加条款。”

---

## 三、数学描述关系，代码描述过程

数学式：

\[
S=3+5+2
\]

把所有数字放在同一幅画面里。代码却要回答画面之外的问题：

1. 现在已知三车数量是 `3、5、2`；
2. 需要记住已经处理部分的总和 `sum`，还要记住当前位置 `i`；
3. 下一步把 `a[i]` 加进 `sum`；
4. `sum` 的旧值还要参与这次加法，所以先读取右边，再写回左边；
5. 每次先累加，再把 `i` 移到下一格；
6. 当 `i` 不再小于 3 时停止。

把这些问题答完，代码才有资格出现：

```cpp
int a[3] = {3, 5, 2};
int sum = 0;
for (int i = 0; i < 3; i++) {
    sum = sum + a[i];
}
```

每一行都能配音：准备三格数据；准备一个目前总和为 0 的格子；让 `i` 依次指向 0、1、2；读取当前数量；加到旧的 `sum` 上；把新总和写回。

代码没有发明新的数学。它只是把数学隐藏起来的时间、记忆、顺序和停止条件补齐了。

---

## 四、全书的翻译公式

Notch 把那张登记牌翻过来，写下全书都会反复使用的一条路线：

\[
\boxed{
\text{数学关系}
\rightarrow
\text{状态变化}
\rightarrow
\text{变量与顺序}
\rightarrow
\text{代码}
}
\]

第一站是数学关系：最终要满足什么？

第二站是状态变化：机器从当前情况走到结果，中间会经过哪些状态？

第三站是变量与顺序：哪些信息必须记住，谁先更新，谁后更新，什么时候停止？

最后才是代码。代码不是路线的起点，更不是需要背诵的咒语。它是前面那些选择被写成了机器能执行的句子。

这条路线还可以反着走。看到代码时，先追踪每个格子的变化，再说出变量在记什么，最后还原它表示的数学关系。能双向翻译，才算真的读懂。

---

## 五、错误博物馆的第一张临时门票

托马斯最初想这样登记：

```cpp
int sum;
sum = 3;
sum = 5;
sum = 2;
```

这三句都能执行，却只是在同一个格子里连续写入。状态是：

| 执行后 | `sum` | 消失的旧值 |
|---|---:|---:|
| `sum = 3` | 3 | 未定义 |
| `sum = 5` | 5 | 3 |
| `sum = 2` | 2 | 5 |

它完成的是“最后记住 2”，不是“把三个数相加”。错误不在加法算错，而在于根本没有保存累计状态。

第二张展品把停止条件写成 `i <= 3`。R-01 会处理 0、1、2 之后，再去读取 `a[3]`。可仓库只有三格，`a[3]` 在墙外。那里没有隐藏奖励，只有非法位置以及一只常在边界测试时出现的苦力怕。

托马斯问：“它为什么偏偏多走一步？”

R-01 回答：“因为小于等于三包括三。数学没有背叛你，是你读得太有希望了。”

---

## 六、翻译训练：先别急着敲键盘

### 训练一：从数学到状态

要计算：

\[
P=4\times3\times2
\]

请先说出需要的过程变量。参考答案是 `product`，表示目前已经乘出的积。尚未处理任何数字时，它应从 1 开始，而不是 0；否则第一步以后，一切都会被乘成 0。

### 训练二：从状态到代码

补完这张表对应的更新动作：

| 当前数字 | `count` 更新前 | 是否为偶数 | `count` 更新后 |
|---:|---:|---|---:|
| 3 | 0 | 否 | 0 |
| 8 | 0 | 是 | 1 |
| 4 | 1 | 是 | 2 |

可以配音为：每遇到一个偶数，就把目前个数增加一。这里加的是“一个”，不是把偶数本身加进 `count`。

### 训练三：从代码还原关系

```cpp
int x = 4;
x = x + 3;
x = x * 2;
```

状态轨迹是 `4 → 7 → 14`，所以最终关系是先加 3 再乘 2，即 \((4+3)\times2\)，不是 \(4+3\times2\)。代码的换行在安排时间。

午夜钟响前，R-01 把 10 写上登记牌。村民检查三遍，确认一颗绿宝石也没有因此多出来，失望地盖了章。

托马斯却盯着 `sum` 格子看了很久。他原以为编程只是把答案告诉机器。现在他知道，机器真正追问的是：

> 你站在当前这一步，手里记着什么，又准备怎样安全地走到下一步？

要回答这个问题，得先从那个最像数学、也最不像数学的符号讲起。