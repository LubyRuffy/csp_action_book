# 第十章　平均数背后藏着两个过程

村民交易所决定公布“每个柜台平均收到多少颗绿宝石”。四个柜台当天的收入是：

```text
5  6  4  6
```

托马斯心算出总数 21，又在程序里写下：

```cpp
double average = 21 / 4;
```

R-01 打印：

```text
5
```

“应该是 5.25。”托马斯说。

“程序先完成了两个整数的除法，得到 5，再把 5 放进 `double`。”

村民主管看着消失的 0.25 颗绿宝石，非常认真地问：“被整数除法吃掉的部分，会进入谁的账户？”

Notch 立刻封存账本。今天的事故不涉及贪污，只涉及计算顺序和数据类型。但在交易所里，这两者造成的表情十分相似。

---

## 一、平均数不是边读边猜

数学把平均数写成：

\[
A=\frac{a_0+a_1+\cdots+a_{n-1}}{n}
\]

人看到公式，会直接把“求和”和“除法”当成一个整体。程序需要把它展开成两个阶段：

1. 逐个得到总和；
2. 用总和除以数量。

所以需要的过程状态是：

- `sum`：目前已经读到的数的总和；
- `n`：一共有多少个数；
- `average`：求和完成后计算出的平均数。

在数据还没读完时，`sum` 会不断变化。若此时就不断做整数除法，前面丢掉的小数不会在后面自动回来。

---

## 二、先把总数算完整

已知数组：

```text
a = [5, 6, 4, 6]
n = 4
```

尚未处理任何柜台时，总和应为 0。因此：

```cpp
int sum = 0;
```

每读一个元素，把它加入目前的总和：

| 已处理柜台 | 当前收入 | 更新前 `sum` | 更新后 `sum` |
|---|---:|---:|---:|
| 无 | - | - | 0 |
| 0 | 5 | 0 | 5 |
| 1 | 6 | 5 | 11 |
| 2 | 4 | 11 | 15 |
| 3 | 6 | 15 | 21 |

到这里，第一阶段完成。代码来自这张状态表：

```cpp
int sum = 0;
for (int i = 0; i < n; i++) {
    sum += a[i];
}
```

循环结束条件 `i < n` 表示下标 `0` 到 `n-1` 都恰好处理一次。接下来才进入除法阶段。

---

## 三、整数除法为什么会吃掉小数

在 C++ 中，如果除号两边都是整数，执行的是整数除法：

```cpp
21 / 4
```

结果是整数 `5`，余下部分不会保留在结果中。之后即使把这个 5 存进 `double`：

```cpp
double average = 21 / 4;
```

也只能得到 `5.0`。格子变宽了，但丢掉的 0.25 已经不在手里。把一张被剪掉小数的账单放进更大的文件夹，不会让纸角重新长出来。

要得到小数除法，除法发生时至少有一边是浮点数：

```cpp
double average = static_cast<double>(sum) / n;
```

执行顺序是：

1. 把 `sum` 的值转换成 `double`；
2. 用浮点数除法计算；
3. 把结果存进 `average`。

状态表如下：

| 步骤 | 左操作数 | 右操作数 | 运算类型 | 结果 |
|---|---:|---:|---|---:|
| 原写法 `sum / n` | 21（整数） | 4（整数） | 整数除法 | 5 |
| 转换后 | 21.0（浮点） | 4（整数） | 浮点除法 | 5.25 |

也可以写成 `sum / 4.0`，但当数量保存在变量 `n` 中时，显式转换更清楚地说明意图。

---

## 四、`int` 和 `double` 管的是保存方式，也影响运算

托马斯曾经以为，只要左边是 `double`，右边就会自动认真保留小数：

```cpp
double result = 5 / 2;
```

真正的时间线却是：

```text
先算右边：5 / 2 → 2
再写左边：把 2 转成 2.0 存入 result
```

左边的数据类型来不及回到过去，要求右边重算。

对比：

```cpp
double result = 5.0 / 2;
```

右边含有 `5.0`，所以运算本身就是浮点除法，得到 `2.5`。

这再次说明一行代码内部也有时间：先完成右边，再写入左边。变量类型不是贴在答案上的装饰，它参与决定中间计算怎样发生。

总和本身是否要用 `double`，取决于输入。如果每个元素都是整数，`int` 或更大范围的整数类型可以保存精确总和；如果输入本身有小数，总和也应使用 `double`。若数据很多，还要考虑 `int` 是否可能装不下总和。入门阶段先记住：类型必须能容纳过程中的值，而不只是最后打印出来的样子。

---

## 五、为什么不建议“每来一个就除一次”

一名村民提出节省内存的新方案：

```cpp
int average = 0;
for (int i = 0; i < n; i++) {
    average = (average + a[i]) / 2;
}
```

这不是在计算全体平均数。每次除以 2，会让较晚出现的数据获得不同权重。

用 `[5,6,4,6]` 推演：

| 读到的值 | 更新前 `average` | 计算 | 更新后 |
|---:|---:|---|---:|
| 5 | 0 | `(0+5)/2` | 2 |
| 6 | 2 | `(2+6)/2` | 4 |
| 4 | 4 | `(4+4)/2` | 4 |
| 6 | 4 | `(4+6)/2` | 5 |

结果碰巧仍是 5，却不是 5.25，而且这套更新没有保持“已处理元素的总和”或“已处理元素的正确平均值”。程序只是反复把当前记录和新值折半。

最清楚的做法仍是保存足够的信息：总和与数量。数学公式里的分子和分母，正好对应程序中的两个状态来源。

---

## 六、除法以前先检查数量

如果 `n = 0`，公式中的分母为零。程序不能执行：

```cpp
double average = static_cast<double>(sum) / n;
```

空仓库没有“每个柜台平均收入”。必须先决定业务规则：是报告“没有数据”，还是使用某个约定值。

```cpp
if (n == 0) {
    cout << "没有数据\n";
} else {
    double average = static_cast<double>(sum) / n;
    cout << average << '\n';
}
```

条件判断不是为了让代码显得谨慎，而是数学关系本身要求分母不能为零。

---

## 七、分段计算也要尊重括号

交易所还要计算“平均收入加 2 后再打八折”。数学关系是：

\[
F=(A+2)\times0.8
\]

正确代码应保留括号：

```cpp
double finalValue = (average + 2) * 0.8;
```

若写成：

```cpp
double finalValue = average + 2 * 0.8;
```

乘法先执行，得到的是 $A+1.6$。代码没有误解公式，它只是严格执行了另一条公式。

长表达式可以拆开，让每个中间状态更容易检查：

```cpp
double adjusted = average + 2;
double finalValue = adjusted * 0.8;
```

多一个变量，少一次全仓库对账。通常很划算。

---

## 八、错误博物馆

### 展品一：先丢再存

```cpp
double average = sum / n;
```

`sum` 与 `n` 都是整数时，小数在右边已经丢失。

### 展品二：分母写错

```cpp
double average = static_cast<double>(sum) / (n - 1);
```

若处理了 `n` 个元素，数量就是 `n`。循环边界与分母必须描述同一批数据。

### 展品三：循环里反复覆盖总和

```cpp
sum = a[i];
```

这只保留当前元素。要积累总和，应从旧 `sum` 出发更新：`sum += a[i]`。

### 展品四：空数据照常除

当 `n == 0` 时仍做除法。错误来自没有把公式的适用条件翻译成程序条件。

---

## 九、翻译训练

1. 数组 `[2,3,7]` 的 `sum` 轨迹是什么？平均数是多少？
2. 解释 `double x = 7 / 2;` 与 `double y = 7.0 / 2;` 的不同。
3. 某程序处理了下标 `0` 到 `4`，分母应是多少？
4. 为平均数程序补上 `n == 0` 的处理。
5. 给 `static_cast<double>(sum) / n` 配音，必须说出转换发生在除法之前。

参考要点：第一题轨迹为 `0→2→5→12`，平均数为 4；第二题分别得到 `3.0` 与 `3.5`；第三题共处理 5 个元素。

---

村民主管最终在账本上写下 `5.25`。他又在旁边画了一个小方框，专门标注那 0.25 没有进入任何人的账户。

托马斯则记住了更重要的事：

> 平均数在程序里不是一个动作，而是“累加总和”和“按正确类型做除法”两个过程。小数是否活下来，取决于除法发生的那一刻。
