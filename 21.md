# 第二十一章　为什么递推更新需要临时变量

村民育种站嫌保存整张饲料记录表太占地方，要求 R-01 只用两个显示格预测下一轮供应量。

格子上分别贴着：

```text
older = 3
newer = 5
```

它们表示前两轮是 3 包和 5 包。下一轮应为 8 包，再下一轮应为 13 包。

托马斯觉得这比上一章还简单，写下：

```cpp
older = newer;
newer = older + newer;
```

R-01 执行后，显示屏变成：

```text
older = 5
newer = 10
```

育种站立刻收到“下一轮十包”的通知。三个村民同时发出长长的“哼”，那是村民社会中接近正式投诉的声音。

“应该是八！”托马斯说。

“第一句之后，`older` 已经是 5。”R-01 回答，“第二句计算的是 `5+5`。机器无法读取已被你覆盖的 3。”

Notch 把错误通知挂上红石钉：“上一章用数组保存整个历史，所以旧项一直在。今天只有两个格子，历史会被下一次写入吃掉。”

---

## 一、数学写的是同时换班

设当前两个状态为：

\[
older=F_{k-1},\qquad newer=F_k
\]

下一轮结束后，我们希望：

\[
older'=F_k,
\]

\[
newer'=F_{k+1}=F_{k-1}+F_k
\]

数学里的撇号清楚地区分了新旧状态。两条关系可以并排写，仿佛同时生效：新的 `older` 接过旧 `newer`，新的 `newer` 接过两个旧值之和。

程序却只有原来的两个格子。先写任何一个，都可能破坏另一个更新还要读取的数据。

托马斯的第一句完成了 `older'=newer`，但也让旧 `older` 消失。第二句本来欠旧 `older` 一笔数据，此时只能拿新 `older` 冒充。事故不是加法算错，而是加法来晚了。

---

## 二、内存里缺的不是答案，是一个落脚处

执行前：

```text
older  newer
  3      5
```

目标是：

```text
older  newer
  5      8
```

若直接覆盖 `older`，旧值 3 无处可找。因此需要第三个临时格 `current`，先把两个旧值算出的新项放进去：

```text
current = older + newer = 8
```

此时三个值都安全：旧的 3、旧的 5、新的 8。接着才让状态向前滚动。

| 执行动作 | `older` | `newer` | `current` | 哪些值仍安全 |
|---|---:|---:|---:|---|
| 初始 | 3 | 5 | ? | 旧 3、旧 5 |
| 先算新项 | 3 | 5 | 8 | 旧 3、旧 5、新 8 |
| `older=newer` | 5 | 5 | 8 | 旧 5、新 8 |
| `newer=current` | 5 | 8 | 8 | 新状态 5、8 |

第三行暂时出现两个 5 没有问题。旧 3 已完成使命，新一轮只需保留 5 和 8。安全更新不是让每个中间画面都好看，而是让尚未完成的计算仍能拿到需要的数据。

---

## 三、先算新状态，再替换旧状态

写代码前逐项确认：现在已知 `older` 和 `newer`；下一项同时依赖二者；旧 `older` 在求和完成前不能覆盖；`current` 保存尚未写回的新项；写回时先移动窗口左端，再放入右端。

核心更新只有三行：

```cpp
int current = older + newer;
older = newer;
newer = current;
```

给它配音：

> 趁两个旧数还在，先算出下一项并放进临时格。然后让较新的旧项成为较老项，最后让刚算出的新项成为较新项。

生成后续若干项时，把这三步放进循环：

```cpp
int older = 1, newer = 1;
for (int i = 2; i < n; i++) {
    int current = older + newer;
    older = newer;
    newer = current;
}
```

循环第 `i` 轮结束后，`newer` 保存 `F_i`，`older` 保存 `F_{i-1}`。循环变量告诉我们走到第几项，两个状态变量只保存继续前进所需的最近历史。

---

## 四、状态滚动的逐帧动画

从 `1,1` 开始生成到第六项。每轮开头，两个格子是“窗口”；每轮结束，窗口向右移动一项。

| 轮次 `i` | 轮初 `older` | 轮初 `newer` | 算出的 `current` | 轮末 `(older,newer)` |
|---:|---:|---:|---:|---|
| 2 | 1 | 1 | 2 | `(1,2)` |
| 3 | 1 | 2 | 3 | `(2,3)` |
| 4 | 2 | 3 | 5 | `(3,5)` |
| 5 | 3 | 5 | 8 | `(5,8)` |
| 6 | 5 | 8 | 13 | `(8,13)` |

托马斯把纸条盖在表格左侧，只露出相邻两行，果然看见窗口一步步右移。

数组版本保存的是：

```text
1 1 2 3 5 8 13
```

滚动版本保存的只是此刻需要的两个数。它没有神秘地忘掉历史，而是在确认更早的项不会再被公式读取后，主动把格子让给新状态。

这仍然是一堂状态更新课。我们比较的是“保存全部历史”和“只保存下一步依赖的历史”这两种内存画面，不扩展成动态规划或复杂算法讨论。

---

## 五、为什么更新顺序不能随意换

有人会问，先把 `newer` 更新不行吗？

```cpp
newer = older + newer;
older = newer;
```

初始 `(3,5)`。第一句后是 `(3,8)`，第二句读取新 `newer=8`，得到 `(8,8)`。旧的 5 又丢了。

也有人先搬旧状态，再计算：

```cpp
older = newer;
int current = older + newer;
```

初始 `(3,5)` 变成 `(5,5)`，`current` 得到 10。临时变量虽然出现了，却来得太晚。救生艇应在箱子落水前使用，不能等它沉底后再举行下水仪式。

正确顺序背后的依赖是：

```text
current 需要旧 older 和旧 newer
新 older 需要旧 newer
新 newer 需要 current
```

因此 `current` 必须最先计算。等它安全保存了新项，旧 `older` 才可以被覆盖。

---

## 六、和交换、插入是同一类麻烦

托马斯忽然认出了这种局面。

交换变量时：

```text
新 x 需要旧 y
新 y 需要旧 x
```

所以先用 `temp` 保护旧 `x`。

数组插入时，右边格要读取左边格的旧内容。如果从左往右搬，先写入的新副本会覆盖下一步还需读取的旧数据，所以要从右往左。

递推滚动时，新项需要两个旧项。若先覆盖任何一个依赖值，公式就会读到混合了新旧时刻的状态，所以先用 `current` 计算新项。

三件事表面不同：交换两个数、移动一排箱子、生成下一项。内存里的共同问题是：

> 我准备写入的格子中，是否还装着后续计算要读取的旧值？

如果答案是“是”，就要先保存，或者换一个不会踩坏数据的顺序。

---

## 七、错误博物馆：时间混合展厅

### 展品一：两个格子都变成同一个数

```cpp
older = newer;
newer = older + newer;
```

`(3,5)` 的轨迹是 `(3,5)→(5,5)→(5,10)`。第二句使用了新 `older` 和旧 `newer`，两个时刻被混在一起。

### 展品二：把临时格写成旧值

```cpp
int current = older;
older = newer;
newer = current;
```

这段代码完成的是交换，不是求下一项。`current` 应保存两个旧状态计算出的“未来”，却只复制了一个“过去”。

### 展品三：循环少走或多走

若 `older=F_0`,`newer=F_1`，它们已经覆盖两项。要得到 `F_{n-1}`，循环应处理 `i=2` 到 `n-1`。写成 `i<=n` 会多生成一项；从 1 开始则把已经存在的第二项又算一次。

### 展品四：只看最终变量名

循环结束时答案在 `newer`，不是因为它的名字天生代表答案，而是我们规定它始终保存窗口中较新的那一项。变量含义来自每轮更新规则，不能靠英文单词施法。

---

## 八、翻译训练

### 1. 排列更新顺序

把下面三句排成安全顺序：

```cpp
newer = current;
int current = 2 * newer - older;
older = newer;
```

答案：先计算 `current`，再 `older=newer`，最后 `newer=current`。第一句公式同时需要两个旧状态。

### 2. 补状态表

初始 `older=2,newer=4`，规则 `current=older+newer`。执行两轮后的窗口是什么？

第一轮得到 `(4,6)`；第二轮得到 `(6,10)`。

### 3. 代码到数学

```cpp
int current = 3 * newer + older;
older = newer;
newer = current;
```

若轮初两格保存 \(u_{k-1},u_k\)，则关系是：

\[
u_{k+1}=3u_k+u_{k-1}
\]

轮末两格保存 \(u_k,u_{k+1}\)。

### 4. 解释旧值

为什么不能把 `current` 的计算放到两次赋值之后？因为那时 `older` 和 `newer` 都已换成新状态，原公式需要的旧组合已经不存在。

### 5. 不写代码

画出初始 `(1,2)`、规则“新项等于两项之和”的三轮逐帧表。窗口依次为 `(1,2)→(2,3)→(3,5)→(5,8)`。

---

## 九、育种站的更正通知

R-01 撤回十包的错误通知，重新发送八包。村民们检查了状态表，虽然看不懂 `current`，但确认自己的饲料没有被数学凭空增加两包，终于停止“哼”。

托马斯把三只透明格摆成一排。每一轮，他先把未来放进 `current`，再让两个正式格子换班。动作完成后，临时格里的值被复制进新位置；下一轮开始，旧得不再需要的数据才真正退出舞台。

“我以前以为临时变量是多出来的。”他说。

Notch 摇头：“它不是多余空间。它是新旧两个时刻之间的一块踏板。”

R-01 补充：“没有踏板也可以向前走。根据今日记录，通常会直接走进错误答案。”

黑板上留下本章原则：

> 先用完整的旧状态算出新状态，再安全地替换旧状态。

公式中的撇号只占一小笔，代码里的新旧世界却不能挤在同一个格子里。只要后一步还欠旧值一笔数据，就让覆盖再等一会儿。