# 附录 C　50 道数学到代码翻译练习

要求：先写出需要保存的状态及其含义，再写代码。除非题目明确要求，练习不考算法选择。

## 第一组　赋值、变量与执行顺序（对应第 1—3 章）

1. 绿宝石余额原为 3，又收到 1 颗。把 $emeralds'=emeralds+1$ 翻译成代码，并解释为什么 `emeralds = 1;` 不对。
2. 初始 `x=3、y=8`。要求把 `y` 当前的值复制到 `x`，写出语句，并指出读取谁、写入谁、哪个旧值消失。
3. 依次让 `x` 保存 5 和 9。写出两句代码，画出 `x` 的生命轨迹，并说明一个普通变量同一时刻保存几个值。
4. 旧分数为 `score`，奖励为 `bonus`。写出“新分数等于旧分数加奖励”的赋值，并区分旧值、右边结果和新值。
5. 已知 `width=4、height=3`，先计算 `area`，再把 `width` 改成 10。写出代码，并说明 `area` 是否自动变成 30。
6. 四个整数变量名原为 `a、b、c、d`，其中一个保存苹果库存。为它选择能表达含义的新名字，并写出“售出一个苹果”的更新。
7. `apples=12`。任务要求同时保留售出前和售出后的库存。设计两个变量，并把保存与更新排成安全顺序。
8. `diamonds` 依次经历“初始 5、增加 3、减少 2”。写出代码和完整状态轨迹，指出每次被覆盖的旧值。
9. 当前正在处理的箱子值为 7，此前总和为 4，已处理箱数为 1。设计 `current、sum、count`，再写出纳入当前箱子的两次更新。
10. 烤炉初始 `fuelLoaded=0、foodLoaded=0、furnaceOn=0`，且启动前燃料和食物必须就位。写出安全的三句更新，并说明其中哪两句可以互换、哪一句不能提前。

## 第二组　条件与循环

11. 把绝对值分段关系翻译成 `if/else`，结果保存在 `absoluteValue`。
12. 成绩 `score`：90 分及以上为 A，60—89 为 B，其余为 C。写出互斥分支。
13. 对数组下标 $i=0,1,\ldots,n-1$，判断每个元素是否为偶数。写出循环骨架。
14. 按 $a_i=2i+1$ 生成前 `n` 个奇数。
15. 处理闭区间 $[3,7]$ 中的每个整数，写出循环头并列出实际取值。
16. 输出整数 1 到 `n`。分别写出以 `i<=n` 和以停止位置表示的两种循环条件。
17. 把数组每个元素扩大两倍并写回原位置。说明每轮直接改变哪一格。
18. 判断数组中是否至少存在一个值等于 `target`，使用布尔变量 `found`。
19. 判断数组中是否所有值都非负，使用“发现反例”的方式更新 `allNonNegative`。
20. 运费规则：重量不超过 5 收 10；不超过 10 收 18；其余收 30。把分段关系翻译成有序条件。

## 第三组　累加、计数与最值

21. 把 $S=a_0+a_1+\cdots+a_{n-1}$ 翻译为累加器与循环。
22. 求一组数的乘积。确定 `product` 的初始值并解释理由。
23. 统计数组中正数的个数。状态变量必须表示“目前已找到的正数个数”。
24. 统计值等于 `target` 的元素个数。指出条件满足时增加的是元素值还是个数。
25. 求非空数组最大值，要求对全负数组也正确。
26. 求最小值及其第一次出现的位置。设计数值卡和位置卡。
27. 求最大值最后一次出现的位置。选择合适的比较条件。
28. 求整数数组平均数并保留小数。处理 `n==0` 的情况。
29. 求数组中所有偶数元素的和。区分“偶数之和”与“偶数个数”。
30. 一次扫描同时计算总和与非零元素个数。写出两个状态各自的更新规则。

## 第四组　数组移动

31. 把数组位置 `position` 的值替换为 `value`。说明这与插入有什么不同。
32. 有效长度为 `n`、容量足够，在 `position` 插入 `value`。写出右移循环、写入和长度更新。
33. 删除 `position` 的元素。写出左移循环和长度更新。
34. 在数组末尾追加 `value`。说明为什么移动循环执行零次。
35. 在数组开头插入 `value`。列出原有元素应按什么顺序右移。
36. 把下标 `[left,right]` 的元素整体向右移动一格。写出安全循环。
37. 把下标 `[left,right]` 的元素整体向左移动一格。写出安全循环。
38. 数组为 `[1,2,3,4,?]`，整体右移一格。写出逐条赋值并列出每一帧。
39. 插入前先检查 `n==capacity`。分别写出“已满”和“可插入”的处理结构。
40. 3 行 4 列数组中，把每格写成 `row + column`。写出两层循环。

## 第五组　多状态顺序更新

41. 交换 `x` 与 `y`，使用 `temp`，并说明每句之后三个变量的值。
42. 三个变量循环移动：$x'=y$、$y'=z$、$z'=x$。保存必要旧值后更新。
43. 用 `previous2`、`previous1`、`current` 生成斐波那契下一项，并安全滚动状态。
44. 数列满足 $a_0=2$，$a_i=a_{i-1}+3$。用数组生成前 `n` 项。
45. 公式 $R=(a+b)(c-d)/2$。拆成可检查的中间结果，避免整数除法误解。
46. 坐标从 `(row,column)` 向右移动一格，同时保存旧坐标用于日志。
47. 已知区间端点 `left`、`right`，若顺序颠倒就交换，使最终满足 `left<=right`。
48. 状态关系为 $p'=q$、$q'=p+q$。不能让第二句错误地使用新 `p`，写出安全代码。
49. 对库存数组同时求总和、空箱数和最大值位置。列出所有过程状态及其初始化。
50. 综合任务：数组有效长度为 `n`。先统计总和，再在位置 `position` 插入 `value`，最后删除第一个零。只写状态设计和三个阶段的代码骨架，不使用复杂算法。

---

# 答案要点

1. `emeralds = emeralds + 1;`；它读取旧余额 3，算出 4，再写回。`emeralds = 1;` 只会用 1 覆盖 3。
2. `x = y;`；读取 `y` 中的 8，写入 `x`，旧 `x=3` 消失，`y` 仍为 8。
3. `int x = 5; x = 9;`；轨迹为“未定义 → 5 → 9”。同一时刻只保存当前的一个值，写入 9 后 5 不再留在 `x` 中。
4. `score = score + bonus;`；右边的 `score` 是旧分数，右边结果是旧分数加奖励，写回后它成为 `score` 的新值。
5. `int area = width * height; width = 10;`；`area` 仍为 12。只有再次执行 `area = width * height;` 才会得到 30。
6. 可命名为 `apples` 或 `appleCount`，更新为 `apples = apples - 1;`。名字应说明格子保存的是苹果库存。
7. `int oldApples = apples; apples = apples - 1;`；必须先保存 12，再把 `apples` 更新为 11。
8. `int diamonds=5; diamonds=diamonds+3; diamonds=diamonds-2;`；轨迹 `未定义→5→8→6`，两次更新分别覆盖旧值 5 和 8。
9. `int current=7, sum=4, count=1; sum=sum+current; count=count+1;`；状态从 `(7,4,1)` 变为 `(7,11,2)`。
10. `fuelLoaded=1; foodLoaded=1; furnaceOn=1;`；前两句在这个模型中互不依赖，可以互换；启动依赖两项装载完成，不能提前。
11. `if (x>=0) absoluteValue=x; else absoluteValue=-x;`。
12. 先判断 `score>=90`，再判断 `score>=60`，最后 `else`；顺序不能反过来。
13. `for (int i=0; i<n; i++)`，循环体判断 `a[i] % 2 == 0`。
14. `for (int i=0; i<n; i++) a[i]=2*i+1;`。
15. `for (int i=3; i<=7; i++)`，取值为 3、4、5、6、7。
16. `for (int i=1; i<=n; i++)`；或把停止位置写成 `i<n+1`。
17. `a[i] = a[i] * 2;`；每轮只写当前 `a[i]`。
18. `found` 初始为 `false`，发现 `a[i]==target` 后改为 `true`，可提前停止。
19. `allNonNegative` 初始为 `true`，发现 `a[i]<0` 后改为 `false`。
20. `if (weight<=5)`、`else if (weight<=10)`、`else`；较小上界先判断。
21. `sum` 初始 0；循环中执行 `sum += a[i];`。
22. `product` 初始 1；循环中执行 `product *= a[i];`。
23. `positiveCount` 初始 0；若 `a[i]>0`，执行 `positiveCount++`。
24. `targetCount++`；增加的是满足条件的次数，不是 `a[i]`。
25. `maximum=a[0]`，从 `i=1` 开始，看见更大者才更新。
26. `minimum=a[0]`、`minPosition=0`；使用 `<`，更新时两个变量一起改。
27. 使用 `>=` 更新数值和位置，使相等的后出现元素替换位置。
28. 先求 `sum`；`n==0` 时报告无数据，否则计算 `static_cast<double>(sum)/n`。
29. `evenSum` 初始 0；仅在 `a[i]` 为偶数时执行 `evenSum += a[i]`。
30. `sum=0`、`nonZeroCount=0`；每轮总和都更新，非零时计数器加一。
31. `a[position]=value;`；它覆盖原元素，不移动数据，也不改变 `n`。
32. 从 `i=n` 到 `position+1` 执行 `a[i]=a[i-1]`，再写新值，最后 `n++`。
33. 从 `i=position` 到 `n-2` 执行 `a[i]=a[i+1]`，最后 `n--`。
34. 写 `a[n]=value; n++;`；`position==n` 时没有挡路元素。
35. 旧下标 `n-1,n-2,...,0` 依次右移，必须从右向左。
36. `for (int i=right; i>=left; i--) a[i+1]=a[i];`。
37. `for (int i=left; i<=right; i++) a[i-1]=a[i];`，并确保 `left-1` 合法。
38. 顺序为 `a[4]=a[3]`、`a[3]=a[2]`、`a[2]=a[1]`、`a[1]=a[0]`；中间会暂时出现重复值。
39. `if (n==capacity)` 拒绝插入；`else` 中执行右移、写入和 `n++`。
40. 外层 `row<3`，内层 `column<4`，写 `a[row][column]=row+column`。
41. `temp=x; x=y; y=temp;`；`temp` 保护旧 `x`。
42. 可先保存 `oldX=x`，再执行 `x=y; y=z; z=oldX;`。
43. `current=previous2+previous1; previous2=previous1; previous1=current;`。
44. 先设 `a[0]=2`，再从 `i=1` 执行 `a[i]=a[i-1]+3`。
45. `sum=a+b; difference=c-d; product=sum*difference; result=product/2.0;`。
46. `oldRow=row; oldColumn=column; column=column+1;`，移动前检查新列边界。
47. 若 `left>right`，使用临时变量交换两个端点。
48. `int current=p+q; p=q; q=current;`；先算出依赖两个旧值的新 `q`。
49. `sum=0`、`emptyCount=0`、`maximum=a[0]`、`maxPosition=0`；每轮分别按含义更新。
50. 阶段一用累加器；阶段二检查容量并从右向左插入；阶段三先找第一个零的位置，再从左向右删除并修改长度。三个阶段都使用当前有效 `n`，每次长度变化后重新确认有效范围。
