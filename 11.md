# 第十一章　“对每一个 $i$”就是循环

皇家自动农场有十二块耕地，编号从 0 到 11。托马斯要让 R-01 给每块地浇一次水，于是写下：

```cpp
for (int i = 0; i <= 12; i++) {
    water(i);
}
```

前十二次浇水都很顺利。第十三次，R-01 走到编号 12 的位置，对着围墙启动水泵。墙后正好站着一只苦力怕。

几秒钟后，农场多了一处不在设计图上的池塘。

“我说的是十二块地！”托马斯喊。

“程序要求处理编号 0 到 12，共十三个位置。”R-01 回答。

Notch 看着被炸开的围墙：“数学里一句‘对每一个’很省纸。代码要把从哪里开始、什么时候停止、每次怎样前进全部说出来。少说一项，苦力怕就负责补充案例。”

---

## 一、数学把重复动作压成了一句话

数学常写：

\[
a_i=2i+1,\qquad i=0,1,\ldots,n-1
\]

它的意思是：对允许范围内的每一个 $i$，都按照同一个关系计算 $a_i$。

若 $n=5$，被处理的下标是：

```text
0  1  2  3  4
```

对应结果：

```text
1  3  5  7  9
```

人能从省略号看懂“中间照做”。R-01 不会执行省略号。程序必须把重复过程拆成四件事：

1. 当前从哪个下标开始；
2. 什么时候还可以继续；
3. 本轮处理谁；
4. 本轮结束后，下标怎样变化。

这四件事合在一起，才是循环。

---

## 二、循环变量是“当前正在处理谁”的地址牌

在下面的循环中：

```cpp
for (int i = 0; i < n; i++) {
    a[i] = 2 * i + 1;
}
```

`i` 不是最终答案，也不是数组长度。它保存当前这一轮正在处理的下标。

把 `for` 头部读成动作：

```text
i = 0      先把当前下标设为 0
i < n      每轮开始前，检查当前下标是否有效
i++        本轮结束后，移动到下一个下标
```

循环体：

```cpp
a[i] = 2 * i + 1;
```

表示只修改当前下标对应的那一格。

当 `i` 变化时，同一行代码会依次指向不同位置。这正是“对每一个 $i$”落到程序中的样子。

---

## 三、逐帧走过五个下标

令 `n = 5`。开始前，数组五格尚未填写：

```text
?  ?  ?  ?  ?
```

| 轮次 | 进入循环时 `i` | `i < 5` | 写入位置 | 写入值 | 本轮后 `i` |
|---|---:|---|---|---:|---:|
| 1 | 0 | 真 | `a[0]` | 1 | 1 |
| 2 | 1 | 真 | `a[1]` | 3 | 2 |
| 3 | 2 | 真 | `a[2]` | 5 | 3 |
| 4 | 3 | 真 | `a[3]` | 7 | 4 |
| 5 | 4 | 真 | `a[4]` | 9 | 5 |
| 检查停止 | 5 | 假 | 不执行 | - | - |

数组状态也逐步形成：

```text
[1, ?, ?, ?, ?]
[1, 3, ?, ?, ?]
[1, 3, 5, ?, ?]
[1, 3, 5, 7, ?]
[1, 3, 5, 7, 9]
```

循环停止时 `i == n`。这个值是第一个无效下标，所以不能再进入循环体。

---

## 四、`i < n` 为什么恰好执行 $n$ 次

数组有 $n$ 个元素时，有效下标是：

\[
0,1,2,\ldots,n-1
\]

条件 `i < n` 允许的正好是这些整数。

以 `n = 5` 为例：

- `0 < 5`，处理第 1 个元素；
- `1 < 5`，处理第 2 个元素；
- ……
- `4 < 5`，处理第 5 个元素；
- `5 < 5` 为假，停止。

托马斯写成 `i <= n` 时，`i == n` 仍满足条件，于是多执行一轮并访问 `a[n]`。但最后一个有效位置是 `a[n-1]`。

“数组长度是 12”和“最后一个下标是 11”并不矛盾。长度在数有多少格，下标在给格子编号。零号格也占一格，它没有因为名字是零就免费住进内存。

---

## 五、循环的三个位置各管什么

标准 `for` 循环写作：

```cpp
for (初始化; 继续条件; 每轮后的变化) {
    重复执行的动作;
}
```

### 初始化：准备第一轮

```cpp
int i = 0
```

只执行一次。它说明第一个要处理的是谁。

### 继续条件：进门前检查

```cpp
i < n
```

每轮开始前都检查。条件为假时，循环体一次也不会执行。

### 每轮后的变化：走向下一位

```cpp
i++
```

本轮动作完成后执行。如果忘记改变 `i`，程序会一直处理同一格。

### 循环体：当前这一轮做什么

```cpp
a[i] = 2 * i + 1;
```

它应该只描述一个位置上的重复动作。循环负责让这个动作落到所有目标位置。

---

## 六、少一次、多一次和永远不结束

循环边界错误主要有三种画面。

### 少一次

```cpp
for (int i = 0; i < n - 1; i++) {
    water(i);
}
```

只处理 `0` 到 `n-2`，最后一块地没人管。最后一株小麦并不会因为排在末尾就自动学会喝水。

### 多一次

```cpp
for (int i = 0; i <= n; i++) {
    water(i);
}
```

多处理下标 `n`。若访问数组，这就是越界。

### 永远不结束

```cpp
for (int i = 0; i < n; i--) {
    water(i);
}
```

当 `n > 0` 时，`i` 从 0 变成 -1、-2……，始终小于 `n`。方向与停止条件互相拆台。

设计循环时，不能只看三段语法是否齐全，要检查它们是否共同把 `i` 推向停止位置。

---

## 七、从任意区间开始

并非所有循环都从 0 开始。若数学要求处理下标 $l$ 到 $r$，并且两个端点都包含，可以写：

```cpp
for (int i = l; i <= r; i++) {
    process(a[i]);
}
```

前提是 $l$ 与 $r$ 都在有效范围内。

若采用左闭右开的范围 $[l,r)$，则写：

```cpp
for (int i = l; i < r; i++) {
    process(a[i]);
}
```

两种写法没有谁天生更高级。关键是数学区间、循环条件和实际处理的下标必须一致。

把范围先写成具体下标，常能避免混乱。例如要处理 `2,3,4,5`，可以先问：

```text
第一个下标：2
最后一个有效下标：5
停止位置：6
```

于是既可使用 `i <= 5`，也可使用 `i < 6`。

---

## 八、循环也能生成状态，而不只是访问数组

“对每一个”还可以驱动累加、计数和检查：

```cpp
int sum = 0;
for (int i = 0; i < n; i++) {
    sum += a[i];
}
```

这里 `i` 表示当前元素，`sum` 表示已处理部分的总和。每一轮同时发生两种状态变化：

| 轮次 | `i` 指向谁 | `sum` 记录什么 |
|---|---|---|
| 开始前 | 尚未处理 | 空范围的和 0 |
| 第 1 轮后 | 已处理 `a[0]` | `a[0]` |
| 第 2 轮后 | 已处理到 `a[1]` | `a[0]+a[1]` |
| 结束后 | 已处理到 `a[n-1]` | 全部元素之和 |

循环变量负责“现在轮到谁”，状态变量负责“到目前为止得到了什么”。两者不要混成一件事。

---

## 九、错误博物馆

### 展品一：把长度当成最后下标

```cpp
for (int i = 0; i <= n; i++)
```

错误来自忘记有效下标结束于 `n-1`。

### 展品二：初始化在错误位置

```cpp
for (int i = 1; i < n; i++) {
    a[i] = 0;
}
```

若目标是清空全部数组，`a[0]` 被漏掉。除非第一个元素已经单独处理，否则应从 0 开始。

### 展品三：变化方向相反

```cpp
for (int i = 0; i < n; i--)
```

`i` 离停止位置越来越远。

### 展品四：循环体外使用 `i`

若 `i` 定义在 `for` 头部，它的作用范围通常只在循环内。循环结束后再访问 `a[i]`，既可能无法编译，也可能企图访问停止位置 `n`。

---

## 十、翻译训练

1. 数学范围 $i=0,1,\ldots,7$ 应写成怎样的循环头？
2. `for (int i=2; i<6; i++)` 实际处理哪些下标？执行几轮？
3. 数组长度为 10，最后一个有效下标是多少？
4. 找出 `for (int i=0; i<=n; i++)` 的越界轮次。
5. 不写代码，填写 `n=3` 时每次条件检查与 `i` 的变化表。
6. 给 `i++` 配音，说明它发生在本轮循环体执行之后。

参考要点：第一题可写 `for (int i=0; i<8; i++)`；第二题处理 `2,3,4,5`；第三题是 9；第四题在 `i==n` 时越界。

---

R-01 修好围墙后，从 0 号地走到 11 号地，每块只浇一次。到达停止位置 12 时，它先检查条件，没有跨出农场。

托马斯把循环写成了一句动作说明：

> 从第一个目标开始；每轮先确认当前目标仍在范围内；处理它；再移动到下一个目标。

数学中的省略号终于不再是魔法。它只是把一段规则相同、下标不断变化的时间压缩在纸上，而循环负责把这段时间重新展开。
