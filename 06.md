# 第六章　三个变量同时变化怎么办

北线矿车到站后，仓库的三条卸货轨道却堵住了。

左轨停着煤炭车，编号 4；中轨停着铁矿车，编号 9；右轨停着金矿车，编号 15。检修员要求三辆车循环换位：中轨车去左轨，右轨车去中轨，左轨车绕到右轨。

托马斯在纸上写得很快：

\[
x'=y,
\qquad y'=z,
\qquad z'=x
\]

人眼一看，结果应是：

```text
(4, 9, 15) → (9, 15, 4)
```

R-01 收到的程序却是依次修改三张轨道牌。它先把 `x` 改成 9，再把 `y` 改成 15，最后让 `z` 读取 `x`。此时 `x` 已经是 9，于是结果成了：

```text
(9, 15, 9)
```

编号 4 的煤炭车在调度系统里失踪，两个编号 9 同时要求进入左轨。真正的铁矿车和冒牌铁矿车互不相让，堵得后面一只运输羊都开始啃铁轨。

“我只是照着三个公式写了三句。”托马斯说。

R-01 回答：“公式使用的是三个旧值。你的第三句使用了第一句制造的新值。”

---

## 一、数学的一撇，程序要用内存来实现

数学里的撇号很省事。`x'` 与 `x` 是两个符号，所以旧 `x=4` 和新 `x=9` 可以同时写在纸上，互不覆盖。

程序若仍使用同一个变量 `x`，写入新值时，旧值就会消失。于是数学中的：

```text
旧状态：(x, y, z)
新状态：(y, z, x)
```

到了机器手里，必须决定两件事：

- 哪些旧值会在未来再次用到；
- 它们被覆盖前要搬到哪里。

先看错误代码的内存动画：

| 步骤 | `x` | `y` | `z` | 丢失了什么 |
|---|---:|---:|---:|---|
| 初始 | 4 | 9 | 15 | 无 |
| `x = y` | 9 | 9 | 15 | 旧 `x=4` |
| `y = z` | 9 | 15 | 15 | 旧 `y=9` 已不再需要 |
| `z = x` | 9 | 15 | 9 | 读到新 `x=9` |

第一句刚执行，第三句需要的旧 `x` 就被毁了。事故发生在第一句，不是在最后一句。最后一句只是去现场取数据，发现仓库早已空了。

---

## 二、先找出必须保存的旧值

这次更新有三笔依赖：

```text
新 x 需要旧 y
新 y 需要旧 z
新 z 需要旧 x
```

画成箭头，恰好绕成一圈：

```text
旧 x → 新 z
旧 z → 新 y
旧 y → 新 x
```

任何一个变量若先被覆盖，它的旧值都在圆环另一处欠着用途。只靠“找一个绝对安全的第一句”行不通，因为圆环里每个旧值都有人要。

不过我们不必保存三个。选择一个地方剪开圆环就够了。若先保存旧 `x`，那么可以按 `x←y、y←z、z←temp` 依次移动。

代码出现前，把动作完整说出：

- 已知状态：`x=4、y=9、z=15`；
- 新状态目标：`x=9、y=15、z=4`；
- 必须记住：即将先被覆盖、最后还要使用的旧 `x=4`；
- 更新顺序：保存旧 `x`，让 `x` 接收旧 `y`，让 `y` 接收旧 `z`，最后把备份放进 `z`；
- 各步读到的源值在读取时仍然安全；
- 四次写入后停止。

```cpp
int temp = x;
x = y;
y = z;
z = temp;
```

逐帧状态表如下：

| 执行语句 | `x` | `y` | `z` | `temp` |
|---|---:|---:|---:|---:|
| 初始 | 4 | 9 | 15 | 未定义 |
| `temp = x` | 4 | 9 | 15 | 4 |
| `x = y` | 9 | 9 | 15 | 4 |
| `y = z` | 9 | 15 | 15 | 4 |
| `z = temp` | 9 | 15 | 4 | 4 |

中间出现两个 9、两个 15 都没关系。重复只是搬运途中产生的副本；只要尚未读取的源数据没有被覆盖，最终状态就能正确到达。

---

## 三、循环移动箱子的画面

Notch 没让托马斯只盯着数字。他在地上摆了三只箱子和一辆空手推车。

第一步，把左轨箱子 4 放上手推车。左轨原位仍可暂时看见 4，因为赋值是复制，不是物理剪切；但我们已经有了安全副本。

第二步，把中轨箱子的编号 9 复制到左轨牌。左轨旧编号 4 被覆盖，可手推车上还有 4。

第三步，把右轨编号 15 复制到中轨。旧 9 消失也无妨，因为 9 已经进入左轨。

第四步，把手推车上的 4 写到右轨。

每一步都遵循同一句话：

> 先确保源值已在安全位置或不再需要，再覆盖目标。

托马斯问：“这是不是交换两次？先交换 `x、y`，再交换 `y、z`？”

Notch 让他画表。两次交换若顺序合适，确实可能达到某个旋转结果，但思路会变成追着当前值猜位置，而且稍换目标方向就要重新试。直接从“新变量需要哪个旧变量”出发，更容易看清数据的去向。

本章不是教某种搬箱算法。三个箱子只是让“同时变化”露出它藏起来的旧状态。

---

## 四、另一种稳妥办法：先算出完整新状态

当更新关系更复杂，或我们不想精打细算只保存哪一个旧值，可以为三个新值都准备新格子。

仍然先回答问题：三个公式都读取旧状态；新格子不会覆盖旧格子；因此可以先分别计算 `newX、newY、newZ`，等三项全部准备好，再统一写回；六次赋值后停止。

```cpp
int newX = y;
int newY = z;
int newZ = x;
x = newX;
y = newY;
z = newZ;
```

状态分成两个阶段：

| 阶段 | 旧 `x,y,z` | 新状态格子 |
|---|---|---|
| 初始 | `(4,9,15)` | 未定义 |
| 计算新状态后 | `(4,9,15)` | `(9,15,4)` |
| 全部写回后 | `(9,15,4)` | `(9,15,4)` |

这种写法多用几个格子，却最贴近数学中的撇号：`newX` 就是 \(x'\)，`newY` 就是 \(y'\)，`newZ` 就是 \(z'\)。当关系变成长算式时，它尤其好检查。

例如：

\[
x'=y+1,
\quad y'=z+x,
\quad z'=x-y
\]

若直接原地更新，后两式很容易读到新 `x` 或新 `y`。先算三个 `new` 变量，则每个右边都明确读取旧状态，之后再写回。空间多了，时间关系反而清楚。

---

## 五、从依赖图决定更新顺序

三个变量并不总要临时格。看另一组目标：

\[
x'=x+1,
\qquad y'=x+y,
\qquad z'=y+z
\]

每个右边都指旧值。若原地更新，`y'` 需要旧 `x`，所以不能先改 `x`；`z'` 需要旧 `y`，所以不能先改 `y`。安全顺序是先 `z`，再 `y`，最后 `x`。

已知 `x=2、y=5、z=10`，写代码前确认：旧 `y` 先被 `z` 使用，旧 `x` 再被 `y` 使用；更新 `z` 不会破坏别人需要的数据；更新 `y` 时旧 `x` 仍在；最后才更新 `x`；三句后停止。

```cpp
z = y + z;
y = x + y;
x = x + 1;
```

| 步骤 | `x` | `y` | `z` |
|---|---:|---:|---:|
| 初始 | 2 | 5 | 10 |
| 更新 `z` | 2 | 5 | 15 |
| 更新 `y` | 2 | 7 | 15 |
| 更新 `x` | 3 | 7 | 15 |

若顺着公式书写次序从 `x` 开始，结果会变成 `(3,8,18)`，后面每一步都吃到了提前更新的新值。

因此决定顺序的方法不是“从第一行写到第三行”，而是：先偿还对旧值的依赖。如果依赖能排成没有回头路的链，就逆着危险方向更新；如果依赖绕成环，就保存一个或多个旧值，把环剪开。

---

## 六、错误博物馆：第一句毁掉了后两句的证据

第一件展品是最初的循环移动：

```cpp
x = y;
y = z;
z = x;
```

标签写着：`z` 需要旧 `x`，而旧 `x` 在第一句被覆盖。

第二件展品看起来有 `temp`，却保存得太晚：

```cpp
x = y;
int temp = x;
y = z;
z = temp;
```

`temp` 保存的是新 `x=9`，不是旧 `x=4`。变量名没有能力修改历史。

第三件展品保存了正确值，却在中途把它改掉：

```cpp
int temp = x;
x = y;
temp = z;
y = z;
z = temp;
```

最后 `z` 得到 15，因为 `temp` 的旧 `x` 已被新任务覆盖。一个临时格同一时刻仍只能装一个值。想让它保管旧 `x` 到最后，就别在任务完成前拿它顺便放别的东西。

R-01 给展柜加了一句说明：“临时变量也需要岗位纪律。让一个格子同时兼职两段历史，通常会导致其中一段失业。”

---

## 七、翻译训练

### 训练一：还原目标

代码：

```cpp
int temp = a;
a = b;
b = c;
c = temp;
```

把它翻译成数学关系：

\[
a'=b,
\qquad b'=c,
\qquad c'=a
\]

右边都指执行前的旧值。

### 训练二：选择要保存的值

目标是反方向旋转：

\[
x'=z,
\qquad y'=x,
\qquad z'=y
\]

可以先保存旧 `z`，然后按 `z=y、y=x、x=temp` 更新。也可以保存旧 `y` 或旧 `x`，但必须重新安排安全顺序。先画状态表，不要靠试运行碰运气。

### 训练三：补完整状态

初始 `(a,b,c)=(1,2,3)`，执行左移：

| 步骤 | `a` | `b` | `c` | `temp` |
|---|---:|---:|---:|---:|
| 初始 | 1 | 2 | 3 | 未定义 |
| 保存 | 1 | 2 | 3 | 1 |
| 改 `a` | 2 | 2 | 3 | 1 |
| 改 `b` | 2 | 3 | 3 | 1 |
| 改 `c` | 2 | 3 | 1 | 1 |

### 训练四：判断能否只靠重排

若新 `p` 需要旧 `q`，新 `q` 需要旧 `r`，新 `r` 需要旧 `p`，依赖构成环，不能找到一个完全不破坏数据的第一句。要么保存一个旧值剪开环，要么先把全部新状态算进新变量。

三条轨道终于疏通。煤炭车、铁矿车和金矿车各自进入新位置，运输羊失去了啃铁轨的理由，开始啃调度表。

托马斯把三变量更新总结为一句话：

> 数学把所有新值放在同一时刻；程序必须先认清每个新值欠哪些旧值，再决定保存什么、按什么顺序偿还。

R-01 检查后说：“总结正确。调度表已被羊吃掉，建议另存一份旧状态。”