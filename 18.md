# 第十八章　复制方向决定数据能否活下来

红石工坊要把四箱零件整体向右挪一格：

```text
原位置：0  1  2  3  4
内容：  A  B  C  D  ?
```

托马斯让 R-01 从左边开始搬。机器人先把 A 复制到 1 号格，再把 1 号格复制到 2 号格，继续向右。搬完后：

```text
A  A  A  A  A
```

“B、C、D 呢？”

“它们依次被前一个 A 覆盖。”

“你为什么一直复制 A？”

“因为你要求我读取刚刚写过的格子。”

Notch 在地上画出源区域和目标区域：“移动不是只看往哪边去，还要看读和写是否重叠。方向选错，一个数据就能像村民谣言一样占满整条走廊。”

---

## 一、复制一句话里藏着两个位置

赋值：

```cpp
a[to] = a[from];
```

包含两个动作：

1. 读取源位置 `from` 的当前值；
2. 把副本写进目标位置 `to`，覆盖目标旧值。

源值不会因为被读取而消失，目标旧值却会在写入时消失。因此每次复制前都要问：

> 目标格中的旧值，后面还要不要读取？

若不需要，放心覆盖；若还需要，必须先把它搬走，或改变复制顺序。

---

## 二、不重叠时，方向通常不重要

假设把左边三格复制到完全分开的右边区域：

```text
源：   A B C
目标：       ? ? ?
```

每个目标格都不是后续步骤的源格。无论先复制 A 还是先复制 C，都不会破坏尚未读取的数据。

| 步骤 | 读取源 | 写入目标 | 是否覆盖未读源 |
|---|---|---|---|
| 复制 A | 源 0 | 目标 5 | 否 |
| 复制 B | 源 1 | 目标 6 | 否 |
| 复制 C | 源 2 | 目标 7 | 否 |

真正危险的是源区域与目标区域重叠。数组插入、删除和同一走廊内搬箱子都属于这种情况。

---

## 三、整体右移为什么从右边开始

目标是：

```text
初始：A B C D ?
结果：A A B C D
```

旧 A 去 1 号格，旧 B 去 2 号格，旧 C 去 3 号格，旧 D 去 4 号格。

若先写 1 号格，旧 B 会被 A 覆盖。正确顺序应先保护最右边的 D：

```cpp
a[4] = a[3];
a[3] = a[2];
a[2] = a[1];
a[1] = a[0];
```

逐帧状态：

| 动作 | 0 | 1 | 2 | 3 | 4 |
|---|---|---|---|---|---|
| 初始 | A | B | C | D | ? |
| 搬 D | A | B | C | D | D |
| 搬 C | A | B | C | C | D |
| 搬 B | A | B | B | C | D |
| 搬 A | A | A | B | C | D |

每一步都先读取尚未被覆盖的源值。目标在源右边，所以从右向左复制。

通用写法：

```cpp
for (int i = right; i >= left; i--) {
    a[i + 1] = a[i];
}
```

`i` 表示当前源下标。循环先处理最大的源下标，再逐步向左。

---

## 四、整体左移为什么从左边开始

现在反过来，要把：

```text
A B C D
```

左移一格，让有效结果成为：

```text
B C D
```

正确顺序是：

```cpp
a[0] = a[1];
a[1] = a[2];
a[2] = a[3];
```

逐帧：

| 动作 | 0 | 1 | 2 | 3 |
|---|---|---|---|---|
| 初始 | A | B | C | D |
| 搬 B | B | B | C | D |
| 搬 C | B | C | C | D |
| 搬 D | B | C | D | D |

目标在源左边，从左向右处理时，写入只发生在已经读过或不再需要的旧位置。

若从右向左，先把 D 写进 2 号格，下一步再读取 2 号格时只能读到 D，最终 D 会向左扩散。

---

## 五、用“读写欠债”决定方向

Notch 给每个旧值挂上一张“尚未读取”的红牌。复制前，红牌表示程序以后还欠它一次读取。

以右移为例：

```text
A B C D ?
```

若先写 `a[1]`，就覆盖了仍挂红牌的 B。这叫提前销毁欠债数据。

从最右边开始时，`a[4]` 是空目标；复制 D 后，D 的读取债务已经还清，即使以后别处被覆盖也无妨。接下来同理处理 C、B、A。

可以把判断过程写成三问：

1. 本次写入会覆盖哪个旧值？
2. 那个旧值以后还要作为源读取吗？
3. 若还要，能否先处理它？

答案比“右移背倒序、左移背正序”更可靠，因为它也适用于多个变量同步更新。

---

## 六、移动两格时原则不变

假设把 `[A,B,C]` 整体向右移动两格：

```text
初始：A B C ? ?
目标：A B A B C
```

对应关系：

```text
旧 2 → 新 4
旧 1 → 新 3
旧 0 → 新 2
```

仍从最大源下标开始：

```cpp
for (int i = 2; i >= 0; i--) {
    a[i + 2] = a[i];
}
```

| `i` | 复制 | 写入是否破坏未读源 |
|---:|---|---|
| 2 | `a[4]=a[2]` | 否 |
| 1 | `a[3]=a[1]` | 否 |
| 0 | `a[2]=a[0]` | 此时旧 `a[2]` 已读完 |

距离改变了，保护尚未读取数据的原则没有变。

---

## 七、临时数组是另一种保护办法

如果复制关系很复杂，也可以先把所有源数据复制到不重叠的临时区域，再写回目标：

```cpp
for (int i = 0; i < length; i++) {
    temp[i] = a[source + i];
}
for (int i = 0; i < length; i++) {
    a[target + i] = temp[i];
}
```

这样源与目标的覆盖顺序不再互相影响，但需要额外存储空间。对简单的一格左移或右移，选择正确方向更直接。

这里不讨论库函数或性能选择。本章只训练一个底层动作：旧值被覆盖前，程序是否已经完成所有必要读取。

---

## 八、错误博物馆

### 展品一：右移时从左向右

```cpp
for (int i = left; i <= right; i++) {
    a[i + 1] = a[i];
}
```

刚写入的值会成为下一次读取的“新源”，首元素一路复制。

### 展品二：左移时从右向左

末尾元素一路向左扩散，原来的中间数据依次丢失。

### 展品三：以为复制会清空源格

`a[2] = a[1]` 后两个位置都可能保存同一个值。赋值是复制，不是搬走并留空。

### 展品四：只看最终下标，不看中间覆盖

纸上对应关系全部正确，不代表任意执行顺序都正确。程序每写一格，下一步面对的数组已经变了。

---

## 九、翻译训练

1. 把 `[1,2,3,4]` 整体右移一格，列出安全赋值顺序。
2. 把 `[1,2,3,4]` 的后 3 个元素左移一格，画状态表。
3. 解释为什么非重叠复制通常不受方向影响。
4. 对 `a[i+2]=a[i]` 的连续复制，若源和目标重叠，应从哪边开始？
5. 找出错误代码中第一次被提前覆盖的旧值：

```cpp
for (int i = 0; i < 4; i++) {
    a[i + 1] = a[i];
}
```

6. 给 `a[3]=a[2]` 配音，同时说明源格与目标格发生了什么。

参考要点：第一题应从旧下标 3、2、1、0 依次复制；第五题第一次丢失的是旧 `a[1]`。

---

R-01 重新搬运零件。这次它先站到 D 箱旁，从右向左逐箱复制。B、C、D 都活到了最终位置。

托马斯没有再把方向当成一条需要死记的规矩。他只看着每次写入前的目标格，问那里的旧数据是否还欠程序一次读取。

> 复制方向的任务不是让代码整齐，而是让每个旧值在被覆盖前完成自己的最后一次工作。
