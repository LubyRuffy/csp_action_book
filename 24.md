# 第二十四章　从代码还原数学关系

皇家仓库的账房换了新门。旧门拆下时，工人顺手把墙上的说明牌也拆了。第二天，只剩一段运行了半年的程序：

```cpp
int total = 0;
int emptyCount = 0;
for (int i = 0; i < n; i++) {
    total += a[i];
    if (a[i] == 0) emptyCount++;
}
```

账房村民问：“它到底在算货款、重量，还是本月员工迟到次数？”

托马斯看见 `total`，回答：“总数。”

“什么的总数？”

他又看见 `emptyCount`：“空箱子个数。”

“哪几个箱子？有没有算到最后一个？”

托马斯没说话。R-01 提醒：“变量名是线索，不是公证书。若程序把苹果放进名叫 `diamonds` 的格子，内存不会报警。”

Notch 把代码抄到黑板上：“上一章从动作生成代码。今天反过来。我们要根据初始化、更新和边界，给程序重新写数学说明书。”

---

## 一、先找谁在变化

还原代码时，不要先猜题目故事。先圈出赋值左边，因为左边指出哪些状态随时间变化。

上面代码中会变化的是：

- `total`：每轮加入 `a[i]`；
- `emptyCount`：只在 `a[i]==0` 时增加；
- `i`：从 0 走到 `n-1`。

数组 `a[i]` 只被读取，没有写入，因此程序不会修改库存。

从更新式可得：

\[
total_{i+1}=total_i+a_i,
\qquad total_0=0
\]

处理完全部元素：

\[
total=\sum_{i=0}^{n-1}a_i
\]

计数器满足：

\[
emptyCount=\#\{i\mid 0\le i<n,\ a_i=0\}
\]

数学符号中的 `#` 表示集合中的元素个数。代码里的 `emptyCount++` 正是在每次条件成立时，把这个个数增加一。

---

## 二、状态表比变量名更可靠

取数组 `4,0,7,0,3`，实际执行如下：

| 处理后范围 | 当前值 | `total` | `emptyCount` | 数组是否改变 |
|---|---:|---:|---:|---|
| 尚未处理 | — | 0 | 0 | 否 |
| `a[0]` | 4 | 4 | 0 | 否 |
| `a[0..1]` | 0 | 4 | 1 | 否 |
| `a[0..2]` | 7 | 11 | 1 | 否 |
| `a[0..3]` | 0 | 11 | 2 | 否 |
| `a[0..4]` | 3 | 14 | 2 | 否 |

每一行结束时，可以说：

> `total` 等于已经处理部分的总和，`emptyCount` 等于已经处理部分中零的个数。

这句话在循环开始前成立：空范围的和与个数都是 0。每处理一个新元素，更新后仍成立。循环结束时，“已经处理部分”变成整个数组，于是过程变量自然成为最终答案。

这种在每轮结束都成立的状态说明，有时叫不变量。这里不要求背术语，重要的是能说清变量在任意时刻记住什么。

---

## 三、从最大值代码还原候选关系

账房还有一段：

```cpp
int best = a[0];
int where = 0;
for (int i = 1; i < n; i++) {
    if (a[i] > best) {
        best = a[i];
        where = i;
    }
}
```

先看初始化：候选值来自第一个元素，候选位置是 0。循环从 1 开始，说明零号元素已经被纳入比较。

再看条件：只有遇到更大的值才替换。因此处理到下标 `i` 后：

\[
best=\max(a_0,a_1,\ldots,a_i)
\]

并且：

\[
a_{where}=best
\]

若最大值重复出现，条件使用严格大于 `>`，相等时不更新，所以 `where` 保留第一次出现的位置。若改成 `>=`，相等时也更新，最后会留下最后一次出现的位置。一个符号改变的不只是判断真假，还改变“哪个位置代表最大值”的数学约定。

状态检查：

| `i` | `a[i]` | 更新前 `(best,where)` | 条件 | 更新后 |
|---:|---:|---|---|---|
| 初始 | 4 | — | — | `(4,0)` |
| 1 | 7 | `(4,0)` | 真 | `(7,1)` |
| 2 | 7 | `(7,1)` | 假，因为不是更大 | `(7,1)` |
| 3 | 2 | `(7,1)` | 假 | `(7,1)` |

数学说明书必须写明：返回最大值及其第一次出现的位置。只写“求最大值”会漏掉代码中 `where` 的工作。

---

## 四、从条件顺序还原分段关系

R-01 找到运费程序：

```cpp
double fee;
if (weight <= 10) {
    fee = weight * 2;
} else if (weight <= 30) {
    fee = 20 + (weight - 10) * 1.5;
} else {
    fee = 50 + (weight - 30);
}
```

第一条件覆盖 `weight<=10`。第二条件只会在第一条件失败后检查，因此它的实际范围是 `10<weight<=30`。最后的 `else` 表示前两段都失败，即 `weight>30`。

还原为：

\[
fee(w)=
\begin{cases}
2w,&w\le10,\\
20+1.5(w-10),&10<w\le30,\\
50+(w-30),&w>30.
\end{cases}
\]

第二段中的 20 是前十单位已产生的费用，第三段中的 50 是前 30 单位累计费用。若只看 `else if (weight<=30)` 而忘记前面已经排除的范围，就会误写成所有 `weight<=30` 都走第二段。

条件链的数学区间不是只由本行决定，还由前面哪些条件已经失败共同决定。

---

## 五、从下标还原处理区间

看这段：

```cpp
int sum = 0;
for (int i = left; i <= right; i++) {
    sum += a[i];
}
```

循环从 `left` 开始，条件含等号，所以处理到 `right`，数学区间是闭区间：

\[
sum=\sum_{i=left}^{right}a_i
\]

元素个数为 `right-left+1`。

若代码是：

```cpp
for (int i = left; i < right; i++)
```

则处理 `[left,right)`，不含 `right`。代码到数学时，`<` 与 `<=` 必须翻译成端点是否包含。说“处理 left 到 right”太含糊，正好足够让机器人多算或少算一个箱子。

再看：

```cpp
for (int i = 1; i < n; i++)
```

它跳过零号元素。可能因为零号已经用于初始化，也可能因为每轮要读取 `a[i-1]`。必须结合循环体判断，不能见到 1 就宣布作者不喜欢零号箱。

---

## 六、区分答案变量与过程变量

以下代码中，`i` 不是题目答案，它只是指出当前处理位置；`current` 也不一定是答案，它可能只是保护旧值。

还原时可以给变量分工：

| 代码特征 | 常见含义 | 判断依据 |
|---|---|---|
| 从 0 开始，每轮加 `a[i]` | 总和 | 初始化与更新共同决定 |
| 从 0 开始，条件真时加 1 | 满足条件的个数 | 加的是 1，不是元素值 |
| 从 `a[0]` 开始，遇更大才改 | 当前最大值 | 候选更新规则 |
| 从 0/false 开始，找到后变 true | 是否存在 | 状态只记录真假 |
| `i` 每轮加 1 | 当前下标 | 控制遍历范围 |
| 保存后很快用于写回 | 临时旧值 | 生命周期短且保护覆盖 |

变量最后恰好保存答案，不代表它从一开始就知道答案。`best` 在每一步只是“已看部分的最大值”；循环结束后，已看部分扩大到全部数据，它才成为全局最大值。

---

## 七、错误博物馆：反向翻译误读区

### 展品一：相信名字，不看更新

```cpp
int sum = 0;
if (a[i] > 0) sum++;
```

尽管叫 `sum`，它统计的是正数个数。代码的行为由赋值决定，不由变量证件上的姓名决定。

### 展品二：忽略初始值

```cpp
int product = 0;
product *= a[i];
```

更新看似求乘积，但从 0 开始会始终为 0。反向说明不能只抄“连乘”，还要指出程序实际得到恒为 0，并诊断初始状态错误。

### 展品三：把条件链看成独立条件

`else if` 的范围包含“前面条件均为假”这一隐藏前提。丢掉它，分段区间会重叠。

### 展品四：漏写位置约定

`>` 保留第一次最大位置，`>=` 保留最后一次。只还原最大值，不解释位置，是读丢了一半状态。

### 展品五：把读取当修改

`sum += a[i]` 修改的是 `sum`，不是 `a[i]`。判断谁变化，仍然先看赋值左边。

---

## 八、翻译训练

### 1. 还原求和范围

```cpp
for (int i = 2; i < n - 1; i++) sum += a[i];
```

答案：

\[
sum=\sum_{i=2}^{n-2}a_i
\]

前提是 `sum` 先初始化为 0。

### 2. 还原计数集合

```cpp
if (a[i] % 2 == 0 && a[i] > 0) count++;
```

它统计正偶数的个数，不是所有偶数之和。

### 3. 还原布尔含义

```cpp
bool allPositive = true;
if (a[i] <= 0) allPositive = false;
```

处理完全部元素后，`allPositive` 表示是否所有元素都大于 0。代码通过寻找反例实现“全部”。

### 4. 还原分段式

```cpp
if (x < 0) y = -x;
else y = x;
```

对应 \(y=|x|\)。请说明 `x=0` 进入 `else`，结果仍为 0。

### 5. 判断首次还是末次

最大值更新条件为 `a[i]>=best`，位置变量会留下最大值最后一次出现的位置。

### 6. 给程序写一句说明书

对本章第一段代码，合格说明是：“读取下标 0 到 `n-1` 的库存，计算总库存，并统计其中值为 0 的箱子数；不修改原数组。”

---

## 九、说明牌重新挂上墙

托马斯最终给三段旧程序补齐了说明：输入是什么，循环范围是什么，每个状态在任意时刻表示什么，输出是值还是位置，重复值采用第一次还是最后一次。

账房村民检查完，把新说明牌钉在门旁。牌上没有写“这是一段优秀代码”，只写了可以验证的数学关系。

R-01 评价：“说明书不再依赖变量名的自我介绍，可信度提高。”

Notch 在黑板上收尾：

> 从代码还原数学，先看初始状态，再看每轮怎样更新，最后看循环何时停止。

代码是数学关系在时间里的展开。沿着执行时间反方向走，把中间状态重新压回求和、集合、区间和分段关系，就能看见它究竟算了什么。若压不回去，先画状态表。内存不会替变量撒谎。