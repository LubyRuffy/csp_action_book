# 第二十五章　错误代码博物馆

皇家自动化工坊的错误博物馆重新开馆。托马斯负责导览，R-01 负责演示，Notch 只提出一个要求：“展品可以运行，但绝不能接上真实仓库。”

托马斯检查控制台，看见总开关旁写着：

```text
DEMO
```

便放心拉下拉杆。

隔壁仓库立刻传来箱子滑动、计价钟狂响和村民集体“哼”的声音。原来标签只表示这根线名叫 `DEMO`，并不表示它真的连接演示台。R-01 平静报告：“六件错误程序已接入生产环境。”

Notch 把安全闸推到底：“很好。今天每件展品都有了事故现场。别只改出正确答案，要找状态第一次偏离的位置。”

---

## 一、参观方法：错误不是最后一行才出生

面对错误代码，先写数学目标，再画内存状态，最后比较每一帧。

检查顺序是：

1. 初始状态是否代表“尚未处理任何数据”；
2. 当前语句读取的是旧值还是已经更新的新值；
3. 写入会覆盖什么；
4. 条件对应哪个数学范围；
5. 循环是否覆盖恰好需要的下标；
6. 数据类型能否保存结果。

最终答案错误，只是事故被发现的时刻。真正的错误往往早一帧已经发生。调试的任务是抓到“第一帧不对”，而不是对最后一个数字进行审讯。

---

## 二、展品一：交换变复制

目标：初始 `x=3,y=5`，最终 `x=5,y=3`。错误代码：

```cpp
x = y;
y = x;
```

状态表：

| 帧 | `x` | `y` | 丢失的信息 |
|---|---:|---:|---|
| 初始 | 3 | 5 | 无 |
| `x=y` | 5 | 5 | 旧 `x=3` |
| `y=x` | 5 | 5 | 已无法恢复 3 |

第一帧错误发生在 `x=y` 写入后。数学希望两个新值同时依赖两个旧值，代码却先毁掉其中一个。

修复：

```cpp
int temp = x;
x = y;
y = temp;
```

`temp` 不是第三个参与交换的答案，它只是给旧 `x` 提供临时住处。

真实事故中，三号库与五号库的门牌都被改成五号。五号库因此收到两份早餐，三号库收到一份关于“数据不可逆”的讲义。

---

## 三、展品二：插入时向右复制，却从左边开工

目标：在 `[1,3,5,7]` 的位置 1 插入 2。数学结果为 `[1,2,3,5,7]`。

错误移动：

```cpp
for (int j = position; j < n; j++) {
    a[j + 1] = a[j];
}
```

状态表：

| 动作 | 数组有效画面 |
|---|---|
| 初始 | `1 3 5 7 ?` |
| `a[2]=a[1]` | `1 3 3 7 ?` |
| `a[3]=a[2]` | `1 3 3 3 ?` |
| `a[4]=a[3]` | `1 3 3 3 3` |

第一步就覆盖了尚未搬走的 5。下一步读到的是新复制的 3，于是 3 像误接红石时钟一样一路繁殖。

修复方向：

```cpp
for (int j = n; j > position; j--) {
    a[j] = a[j - 1];
}
a[position] = 2;
n++;
```

从右端空格向左工作，每次覆盖的目标格都不再保存待读取的数据。数学里的数字会自动让路，数组里的数字需要明确的搬家次序。

---

## 四、展品三：全是负数，最大值却是零

库存差额为 `[-8,-3,-11]`。数学最大值是 `-3`。错误程序先写：

```cpp
int maximum = 0;
```

随后只在 `a[i]>maximum` 时更新。所有负数都不大于 0，于是程序报告最大差额为 0，也就是凭空发明了一个不存在的箱子。

| 检查值 | 检查前 `maximum` | 是否更新 | 检查后 |
|---:|---:|---|---:|
| 初始 | 0 | — | 0 |
| -8 | 0 | 否 | 0 |
| -3 | 0 | 否 | 0 |
| -11 | 0 | 否 | 0 |

修复方式是让候选值来自真实数据：

```cpp
int maximum = a[0];
for (int i = 1; i < n; i++) {
    if (a[i] > maximum) maximum = a[i];
}
```

初始值不是随便找一个看起来中立的数，而是“尚未比较其他元素时，已看部分的最大值”。对于最大值，0 并不中立；它只是零。

---

## 五、展品四：边界多放行一次

仓库有四格，有效下标为 0、1、2、3。错误循环：

```cpp
for (int i = 0; i <= n; i++) {
    sum += a[i];
}
```

当 `n=4` 时，`i` 依次取 0、1、2、3、4。最后一次读取 `a[4]`，它在有效数组之外。

| `i` | 条件 `i<=4` | 读取位置 | 是否合法 |
|---:|---|---|---|
| 0 | 真 | `a[0]` | 是 |
| 1 | 真 | `a[1]` | 是 |
| 2 | 真 | `a[2]` | 是 |
| 3 | 真 | `a[3]` | 是 |
| 4 | 真 | `a[4]` | 否 |
| 5 | 假 | 停止 | — |

应写 `i<n`。`n` 表示元素个数，不是最后一个下标。真实演示中，R-01 打开四号墙板，惊醒了一只专门参与边界测试的苦力怕。安全闸之所以需要推到底，通常有充分理由。

---

## 六、展品五：平均数少了半箱

总库存 5，箱数 2，平均数应为 2.5。错误代码：

```cpp
double average = sum / count;
```

若 `sum` 和 `count` 都是 `int`，右边先做整数除法，`5/2` 得到 2，再把 2 写成 `2.0`。

| 阶段 | 值 | 类型 |
|---|---:|---|
| 读取 `sum`、`count` | 5、2 | `int`,`int` |
| 计算 `5/2` | 2 | `int` |
| 写入 `average` | 2.0 | `double` |

修复：

```cpp
double average = sum * 1.0 / count;
```

小数必须在除法发生时就有资格存在。事后换一个更大的格子，只能保存已经被截断的结果。村民提出把消失的半箱记到托马斯工资里，证明类型错误有时能产生非常具体的人事后果。

---

## 七、展品六：递推式读到了混合时刻

目标窗口 `(older,newer)=(3,5)` 向前滚动成 `(5,8)`。错误代码：

```cpp
older = newer;
newer = older + newer;
```

| 帧 | `older` | `newer` | 第二句将读到什么 |
|---|---:|---:|---|
| 初始 | 3 | 5 | 应读旧 3 与旧 5 |
| 第一行后 | 5 | 5 | 只能读新 5 与旧 5 |
| 第二行后 | 5 | 10 | 错误新项 10 |

修复：

```cpp
int current = older + newer;
older = newer;
newer = current;
```

先让完整旧状态生成新项，再替换旧窗口。临时变量来晚一行，就和消防水到达时只剩错误报告一样，形式完整，作用为零。

---

## 八、把六件错误放在同一张地图上

| 展品 | 数学目标 | 第一处状态错误 | 根因 | 修复动作 |
|---|---|---|---|---|
| 交换 | 两值同时互换 | 旧 `x` 被覆盖 | 旧值未保存 | 使用 `temp` |
| 插入 | 右侧整体右移 | 未搬的 5 被覆盖 | 复制方向错误 | 从右向左 |
| 最大值 | 候选来自数据 | 凭空加入 0 | 初始状态错误 | 用 `a[0]` 初始化 |
| 循环 | 处理 `0..n-1` | 读取 `a[n]` | 端点多包含 | 使用 `i<n` |
| 平均数 | 保留小数 | 除法先截断 | 运算类型错误 | 让除法含 `double` |
| 递推 | 新项依赖两个旧项 | 旧 `older` 消失 | 更新顺序错误 | 先算 `current` |

这些错误看起来分别属于语法、数组、循环和数列，内存视角下却只有几类：初始状态不真，旧状态丢失，处理范围不对，或新结果装不进当前类型。

“粗心”不是有用诊断。若只说学生粗心，下次他会更紧张地重犯同一种状态错误。指出“你在第二帧覆盖了后面还要读的 5”，才给得出可执行的修复。

---

## 九、代码从哪里重新长出来

每件展品的正确代码都能从五个问题生成：

- 当前已知哪些状态？
- 最终关系要求什么？
- 哪个旧值后面还要读取？
- 写入方向和边界是什么？
- 结果需要什么数据类型？

以删除位置 `position` 为例。数学目标是让后面元素依次左移，旧长度减少一。目标格在左边，读取格在右边；从左向右移动时，写入不会破坏下一次还要读取的更右元素。

```cpp
for (int j = position; j < n - 1; j++) {
    a[j] = a[j + 1];
}
n--;
```

这不是需要背的“删除模板”。方向来自依赖关系，边界来自最后一次合法复制 `a[n-1]` 到 `a[n-2]`，长度变化来自有效记录少了一个。

---

## 十、翻译训练：修复前先画第一处偏离

### 1. 交换展品

初始 `a=2,b=9`，错误交换后状态是什么？答案：`(9,9)`。哪个值先丢失？旧 `a=2`。

### 2. 插入展品

`[2,4,6,?]` 在位置 1 插入 3。只写移动后的中间状态，不写最终答案：先 `2,4,6,6`，再 `2,4,4,6`，然后才能写入 3。

### 3. 最大值展品

数组 `[-4,-9,-2]`。使用 `a[0]` 初始化后的候选轨迹为 `-4→-4→-2`。

### 4. 边界展品

处理闭区间 `[left,right]` 时可以写 `i<=right`，因为 `right` 本身是合法下标；处理长度为 `n` 的整个数组时写 `i<n`，因为 `n` 不是合法下标。

### 5. 类型展品

解释 `double x=7/2` 与 `double y=7/2.0`。前者先得到整数 3，后者得到 3.5。

### 6. 递推展品

把安全更新三句配音，必须出现“两个旧值”“临时格”“替换”三个意思。

### 7. 反向诊断

若一段计数代码每次条件成立时执行 `count+=a[i]`，它实际累加满足条件元素的值，不是统计个数。要数个数，应加一。

---

## 十一、闭馆后的修复

安全闸复位后，三号库找回门牌，插入传送带停止复制 3，最大值报表承认负数也能成为最大值，苦力怕被请回边界外，平均数恢复小数，育种站更正下一轮供应。

托马斯把 `DEMO` 标签撕下来，沿着红石线一直查到真实接口。他终于不再因为代码“看起来差不多”就拉下开关。

Notch 问：“今天六个错误里，哪个最值得记？”

“不是某一段错误代码。”托马斯说，“是找到状态第一次不对的那一帧。”

R-01 在闭馆记录上写道：

> “每个错误都严格执行了。机器没有偏离代码；代码先偏离了人类没有展开的想法。”

错误博物馆收藏的不是笑话。每件展品都在提醒同一件事：答案错了以后，别只改最后一行。回到初始状态，沿时间轴逐帧走，看看哪一个旧值被覆盖，哪一个边界被越过，哪一个小数没能活下来。