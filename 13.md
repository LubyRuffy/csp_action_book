# 第十三章　分段函数怎样变成 `if`

村民交易所换了新的运费牌。

牌子写得很清楚：货物不超过 10 千克，收 2 颗绿宝石；超过 10 千克但不超过 20 千克，收 5 颗；超过 20 千克，收 9 颗。

托马斯让 R-01 接管收费柜台。第一位村民搬来一箱 8 千克的胡萝卜，机器人收了 2 颗。第二位搬来 15 千克马铃薯，收了 5 颗。第三位铁匠推来一只 26 千克的铁砧，R-01 盯着秤看了一会儿，说：“请支付 2 颗、5 颗和 9 颗绿宝石，共 16 颗。”

铁匠“哼”了一声。这个“哼”比普通村民的更低，通常表示谈判即将进入铁砧阶段。

“为什么三档全收？”托马斯赶紧问。

“你的命令依次询问：是否超过 20，是否超过 10，是否为正数。三个条件都成立。”

“可它只能属于一档！”

“程序中没有写‘只能’。”

Notch 把铁砧从收费台上挪开：“数学里的分段线已经替你保证每个输入选一段。程序里，这个保证得由条件范围和执行结构亲自完成。”

---

## 一、数学把不同情况摞在一起

分段函数不是一个输入同时算许多答案，而是先看输入落在哪个范围，再使用那一段规则。

这次运费可以写成：

\[
fee(w)=
\begin{cases}
2,&0<w\le 10\\
5,&10<w\le 20\\
9,&w>20
\end{cases}
\]

当 \(w=8\) 时，只使用第一段；当 \(w=15\) 时，只使用第二段；当 \(w=26\) 时，只使用第三段。

数学视角会直接把 26 放进“\(w>20\)”的盒子，得到 9。它不会先收费 9，再发现 26 也大于 10，于是追加 5，最后又因为 26 是正数补收 2。那不是分段收费，是村民暴动生成器。

把分段函数翻译成程序，需要回答两件事：

1. 每一段的进入条件是什么？
2. 选中一段后，还要不要继续检查后面的段？

如果各段互斥，并且只应执行一段，就要让程序在选中后跳过其余分支。

---

## 二、条件判断时，内存里发生了什么

秤把重量放进变量 `weight`。运费变量 `fee` 起初还没有最终值。R-01 每检查一个条件，都产生一个临时的真假结果，但这个结果不会修改 `weight`。

以 `weight=15` 为例，内存可以画成：

| 时刻 | `weight` | 正在判断 | 判断结果 | `fee` |
|---|---:|---|---|---:|
| 称重后 | 15 | 尚未判断 | — | 未确定 |
| 检查第一段 | 15 | `weight <= 10` | 假 | 未确定 |
| 检查第二段 | 15 | `weight <= 20` | 真 | 5 |
| 分支结束 | 15 | 不再检查第三段 | — | 5 |

条件为真并不会把别的条件从数学世界里变成假。15 当然仍然小于某些更大的数，也仍然大于 0。真正阻止多段执行的是分支结构：前面一段选中后，后面的段不再进入。

`if` 可以理解成一道门：条件为真，执行门里的动作；条件为假，绕过去。`else if` 是“前面的门没进去，才来检查这道门”；`else` 则是前面都没进去时的最后通道。

---

## 三、从运费表长出代码

代码出现前，托马斯先把状态和顺序写完整：

- 已知状态：`weight` 已经是一个正数，三档范围依次是“不超过 10”“超过 10 且不超过 20”“超过 20”。
- 要记住的状态：只需用 `fee` 保存选中的运费；重量不会在判断中改变。
- 更新顺序：先检查最前一段；不属于它才检查第二段；前两段都不属于时进入最后一段。
- 原来的值是否还需要：`weight` 每个条件都会读取，但不会被覆盖；`fee` 只写入一次。
- 停止条件：一旦某个分支成立，本组判断结束，不再执行其他分支。

由这份动作说明，代码自然是：

```cpp
int fee;
if (weight <= 10) {
    fee = 2;
} else if (weight <= 20) {
    fee = 5;
} else {
    fee = 9;
}
```

第二个条件为什么只写“`weight <= 20`”，没有再写“`weight > 10`”？因为能走到第二个分支，已经说明第一个条件为假，也就是 `weight > 10`。前面的失败替后面留下了一条已知信息。

这不等于省掉关键条件。完整逻辑仍然是：

\[
weight>10\quad\text{并且}\quad weight\le20
\]

只是第一半已经由执行路径保证了。

三个代表重量的逐帧选择如下：

| `weight` | 第一问：`<=10` | 第二问：`<=20` | 进入分支 | `fee` |
|---:|---|---|---|---:|
| 8 | 真 | 不再问 | 第一段 | 2 |
| 15 | 假 | 真 | 第二段 | 5 |
| 26 | 假 | 假 | `else` | 9 |

代码没有直接“计算”9，而是先用条件确定输入属于哪一段，再把该段的结果写进 `fee`。这正是分段函数到程序过程的展开。

---

## 四、绝对值：两段恰好背向而行

绝对值的数学定义是：

\[
|x|=
\begin{cases}
x,&x\ge0\\
-x,&x<0
\end{cases}
\]

对正数和零，保留原值；对负数，取相反数。

这里的内存风险比运费多一点，因为结果可以写回 `x` 本身。写代码前要明确：

- 已知状态：`x` 已经有值。
- 要记住的状态：判断只需要当前的 `x`；进入负数分支后，右边会先读取旧 `x` 再取相反数。
- 更新顺序：先判断是否小于 0，只有负数才改写。
- 旧值是否还需要：计算 `-x` 时需要旧值，但同一条赋值会先算右边，因此不必另设临时变量。
- 停止条件：完成一次判断后结束；这不是循环。

对应过程很短：

```cpp
if (x < 0) {
    x = -x;
}
```

假设 `x=-6`：

| 时刻 | `x` | 条件 `x < 0` | 动作 |
|---|---:|---|---|
| 判断前 | -6 | 真 | 进入分支 |
| 计算右边 | -6 | 已判断 | 算出 `-(-6)=6` |
| 写回后 | 6 | 不再判断 | 旧值 -6 被覆盖 |

如果 `x=4`，条件为假，分支内不执行，`x` 仍是 4。数学写了两段，程序不一定非得写完整的两条赋值；当某一段的动作是“保持不变”时，什么也不做就是它的代码动作。

---

## 五、条件顺序为什么会改变答案

皇家考试院又送来一张成绩等级表：

\[
level(s)=
\begin{cases}
A,&s\ge90\\
B,&80\le s<90\\
C,&60\le s<80\\
D,&s<60
\end{cases}
\]

托马斯先问“是否及格”，再问“是否达到 80”，最后问“是否达到 90”。如果使用一条互斥分支链，第一个条件写成“`score >= 60`”，那么 96 分会立刻进入“及格”那一段，后面更精确的 A、B 条件根本没有机会检查。

广条件像一张大网。它放在前面，会把本该交给后面细分的输入一起捞走。

正确的检查顺序应从要求最高、范围最窄的上界开始：先 90，再 80，再 60，最后处理剩余情况。

写代码前，状态安排如下：

- 已知状态：`score` 在 0 到 100 之间，各等级互不重叠。
- 要记住的状态：`level` 保存最终等级。
- 更新顺序：从最高门槛向下检查，第一次满足时确定结果。
- 旧分数不会修改，后续无需保存副本。
- 停止条件：进入一个分支后结束整条分支链。

于是得到：

```cpp
char level;
if (score >= 90) {
    level = 'A';
} else if (score >= 80) {
    level = 'B';
} else if (score >= 60) {
    level = 'C';
} else {
    level = 'D';
}
```

对于 `score=86`：

| 检查顺序 | 条件 | 结果 | `level` |
|---:|---|---|---|
| 1 | `score >= 90` | 假 | 未确定 |
| 2 | `score >= 80` | 真 | B |
| 后续 | 不再检查 | — | B |

条件顺序不是装饰。当前面的条件和后面的条件有包含关系时，顺序会决定谁先拿到这个输入。

---

## 六、多个 `if` 不等于一条分支链

R-01 收铁砧运费时，把三个问题当成彼此独立的问题。独立的 `if` 会各自判断，前一个成立不会阻止后一个继续。

这在某些任务里正是需要的。比如玩家同时满足“生命值低”和“没有盾牌”，程序可以同时亮起两条警告。两件事可以一起发生，就应分别判断。

但运费档位要求三选一。把三段写成三个独立判断，26 千克会满足多个宽泛条件，`fee` 可能被反复覆盖，或者总费用被重复增加。

判断该用哪种结构，可以先问：

> 一个输入允许同时属于多个情况吗？

- 允许同时执行多个动作：使用彼此独立的判断。
- 只允许选择一段：使用互斥的分支链，或把条件写成真正互不重叠的范围。

不是见到三个条件就机械地排成某种形状。结构来自情况之间的关系。

---

## 七、错误博物馆：每一段看着都对，合起来却收费三次

错误博物馆把那张 26 千克铁砧收据挂在了正中央。

事故发生前的状态是：`weight=26`，`totalFee=0`。程序依次遇到三个独立条件，每次条件成立都把对应费用加入总额。它的实际轨迹是：

| 步骤 | 判断 | 是否成立 | `totalFee` |
|---|---|---|---:|
| 初始 | 尚未判断 | — | 0 |
| 第一段 | `weight > 20` | 是 | 9 |
| 第二段 | `weight > 10` | 是 | 14 |
| 第三段 | `weight > 0` | 是 | 16 |

每个比较单独看都是真的，错误在于数学目标要求“选择一档”，代码却表达成“所有成立的规则都执行”。

第二件展品是一条有裂缝的边界：第一段使用“低于 10”，第二段使用“高于 10”。重量正好等于 10 时，两边都进不去。数学分段若漏掉端点，程序不会替你把 10 四舍五入到附近的情况。

第三件展品让两个区间在端点重叠：第一段包含 10，第二段也包含 10，再使用独立判断。于是 10 千克同时获得两张账单。边界上的等号放在哪边，必须和数学定义一致。

第四件展品把成绩的宽条件放在前面：先检查 `score>=60`，后检查 `score>=90`，并且使用互斥分支链。90 分以上的学生全被第一道门收走，皇家考试院得到了一批“只是及格”的满分卷。R-01 评价：“条件执行完全正确。庆典气氛未写入程序。”

---

## 八、翻译训练：条件不只要真，还要选对人

### 训练一：数学到条件

把下面关系翻译成选择过程：

\[
f(x)=
\begin{cases}
0,&x<0\\
1,&0\le x\le5\\
2,&x>5
\end{cases}
\]

先检查 `x<0`；否则再检查 `x<=5`；前两项都不成立时进入最后一段。第二段不必重复写 `x>=0`，因为到达那里已经说明第一段为假。

### 训练二：代码到状态

某条分支链先问“温度是否至少 30”，否则问“是否至少 20”，最后进入低温分支。温度 24 会经过哪些门？

它先在 30 这道门前得到假，再在 20 这道门前得到真，执行第二段，后面的分支不再检查。

### 训练三：寻找空洞

两个情况分别写成 \(x<5\) 和 \(x>5\)。哪个输入没有归属？

答案是 \(x=5\)。修复时要根据题意决定等号属于左段还是右段。

### 训练四：寻找重叠

两个独立条件分别是 \(x\le5\) 与 \(x\ge5\)。哪个输入会同时满足？

答案仍是 \(x=5\)。如果只能执行一个动作，需要去掉一边的等号，或者改用明确的互斥分支。

### 训练五：给判断配音

“如果成绩至少 90，等级记为 A；否则如果至少 80，记为 B。”这里的“否则”保存了什么信息？

它没有保存到普通变量里，却保存在线路上：能进入第二次判断，说明成绩小于 90。因此第二段的真实范围是 \(80\le score<90\)。

---

## 九、分段函数不是把大括号套在公式外面

收费系统重新启动。26 千克铁砧上秤后，R-01 先检查“不超过 10”，结果为假；再检查“不超过 20”，仍为假；最后进入剩余分支，收费 9 颗绿宝石。

铁匠付了钱，临走前把铁砧留在柜台上。

“这是小费吗？”托马斯问。

“不是，”铁匠说，“这是提醒。下次再收 16 颗，我就把提醒放到你脚上。”

托马斯立刻记住了本章内容。

数学分段给出的是输入与结果的对应关系。程序还需要把选择过程展开：先问哪个条件，条件是否覆盖或重叠，选中以后是否继续，以及最终结果写进哪个格子。

条件本身可以都算对，结构仍可能表达错任务。把分段函数翻译成代码时，不能只问“这个条件真不真”，还要问“这个输入应该由哪一段负责”。
