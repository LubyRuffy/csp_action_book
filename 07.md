# 第七章　求和公式怎样变成累加器

三条卸货轨道恢复后，Notch 把托马斯派到南边自动农场。那里的收割机一天吐出五箱小麦，数量分别是：

```text
6  4  9  3  8
```

农场主要求 R-01 报告总数。托马斯心算得出 30，在纸上写：

\[
S=6+4+9+3+8
\]

随后他给收割机装了一块显示牌，让机器人每经过一箱就把箱中数量写上去。

显示牌依次出现 6、4、9、3、8，最后停在 8。

农场主看着五箱小麦，又看着“今日总产量：8”，陷入了一种只有纳税季才会出现的复杂沉默。

R-01 报告：“所有箱子均已读取。”

“读取了为什么没加起来？”托马斯问。

“因为显示牌只保存当前写入值。你要求我把每箱数量写上去，没有要求我记住此前总和。”

又是一个旧值问题。只是这次要保护的不是某个固定旧数，而是不断增长的“目前总和”。

---

## 一、数学一眼看到总和，机器一次只见一箱

求和式把五个数同时摊在纸上：

\[
S=a_0+a_1+a_2+a_3+a_4
\]

人可以从任何地方开始凑数，甚至把 6 和 4 先配成 10。R-01 沿传送带工作时，每一刻只处理当前箱子。它需要一个格子记住已经处理部分的结果：

```text
sum = 目前已经读过的箱子数量之和
```

这个定义比“`sum` 是答案”更有用。开始时，机器人还没读任何箱子，所以“已读部分”是空的，总和应为 0。读完第一箱，`sum` 表示第一箱之和；读完前三箱，它表示前三箱之和；等全部处理完，它才成为最终答案。

数学结果被改写成一个过程状态：

\[
\text{新 sum}=\text{旧 sum}+\text{当前箱数量}
\]

这就是累加器。它不是某种神秘语法，而是一个反复保存“到目前为止”的变量。

---

## 二、为什么从 0 开始

托马斯问：“既然第一箱是 6，直接让 `sum=6` 不行吗？”

“可以，”Notch 说，“但那要求仓库至少有一箱，还要从第二箱开始循环。用 0 表示尚未加入任何数字，过程更统一。”

0 在加法里有一个合适的性质：

\[
0+x=x
\]

它不会凭空改变第一项。`sum=0` 不是随便挑的吉利数字，而是对初始状态的准确描述。

若一开始写 `sum=1`，最终会得到 31，等于在传送带启动前先登记了一箱不存在的小麦。村民们通常很喜欢多报的库存，直到 Notch 要他们把多出来的那一份搬上来。

逐帧状态如下：

| 已处理范围 | 当前 `a[i]` | `sum` 更新前 | `sum` 更新后 |
|---|---:|---:|---:|
| 空 | — | 0 | 0 |
| `a[0]` | 6 | 0 | 6 |
| `a[0..1]` | 4 | 6 | 10 |
| `a[0..2]` | 9 | 10 | 19 |
| `a[0..3]` | 3 | 19 | 22 |
| `a[0..4]` | 8 | 22 | 30 |

每一行结束时都可以读成：`sum` 正好等于表中“已处理范围”的总和。这句话在整个循环里反复成立，也让我们知道机器没有忘记前面的箱子。

---

## 三、从求和符号到循环

若箱子有 `n` 个，数学会写：

\[
S=\sum_{i=0}^{n-1}a_i
\]

求和符号把重复动作压成了一个大写字母。展开后，它其实说：让 `i` 依次取 0、1、一直到 `n-1`，每次把 \(a_i\) 加进总和。

代码出现前，要把隐藏信息补齐：

- 已知：数组 `a` 中有 `n` 个数量，有效下标是 0 到 `n-1`；
- 要记住：`sum` 保存已经处理元素的和，`i` 指向当前元素；
- 初始状态：一个元素都没处理，`sum=0`，第一格下标为 0；
- 更新动作：先读取旧 `sum` 与 `a[i]`，相加后写回 `sum`；
- 旧值是否还需保存：同一条赋值会先算右边，不需额外 `temp`；
- 重复顺序：处理当前格，再让 `i` 增加；
- 停止条件：`i==n` 时，所有有效格都处理完。

于是得到核心代码：

```cpp
int sum = 0;
for (int i = 0; i < n; i++) {
    sum += a[i];
}
```

`sum += a[i]` 是 `sum = sum + a[i]` 的简写。给它配音时仍要说完整：读取目前总和，加入当前元素，再把新总和存回 `sum`。

代码里的 `for` 对应“让每一个下标依次出现”，`sum` 对应“已经展开的部分和”，`a[i]` 对应求和符号中的当前项。公式没有直接变成代码；它先变成了“反复更新部分和”的状态过程。

---

## 四、累加器为什么不能每次重新开始

托马斯第一次修正时，把 `sum=0` 写进了循环内部。他觉得每次加法都从干净的格子开始，看起来很整洁。

```cpp
for (int i = 0; i < n; i++) {
    int sum = 0;
    sum += a[i];
}
```

结果每一轮都新建一个 `sum`，先清零，再只加当前箱。轨迹又回到了 `6、4、9、3、8`。循环结束后，内部变量甚至已经离开可使用范围。

清零应该发生一次，因为“尚未处理任何元素”只在循环开始前出现一次。进入第二轮时，机器已经处理过第一箱，若再把 `sum` 清零，就等于把历史抹掉。

对比两种状态：

| 轮次 | 正确 `sum` 开始值 | 错误 `sum` 开始值 | 当前项 |
|---|---:|---:|---:|
| 0 | 0 | 0 | 6 |
| 1 | 6 | 0 | 4 |
| 2 | 10 | 0 | 9 |
| 3 | 19 | 0 | 3 |
| 4 | 22 | 0 | 8 |

累加器之所以有用，正因为它能把上一轮的结果带到下一轮。每轮都清空它，就像每读一页账本都把前一页烧掉，然后惊讶于月底只剩最后一笔。

---

## 五、求乘积时，初始值为什么不能还是 0

农场旁边的压缩机还有一个奇怪指标：把三批甘蔗数量 `2、3、4` 相乘，得到压缩系数：

\[
P=2\times3\times4=24
\]

过程同样需要一个累积变量 `product`：

\[
\text{新 product}=\text{旧 product}\times\text{当前数}
\]

但初始值必须是 1。因为：

\[
1\times x=x
\]

若从 0 开始，状态会是 `0→0→0→0`，后面所有数据都被初始的 0 吃掉。

写代码前确认：数组和长度已知；`product` 记已处理元素的乘积；未处理任何元素时用 1；每轮读取旧乘积和当前项，乘完写回；`i` 从 0 走到 `n-1`；到 `n` 停止。

```cpp
int product = 1;
for (int i = 0; i < n; i++) {
    product *= a[i];
}
```

状态表：

| 当前项 | `product` 更新前 | 更新后 |
|---:|---:|---:|
| — | 1 | 1 |
| 2 | 1 | 2 |
| 3 | 2 | 6 |
| 4 | 6 | 24 |

求和从 0 开始、求积从 1 开始，不是两条互不相干的背诵规则。统一的问题是：

> 尚未处理任何数据时，哪个值既准确描述当前状态，又不会干扰第一项？

---

## 六、错误博物馆：总和是怎么被弄丢的

第一件展品把覆盖当累加：

```cpp
sum = a[i];
```

它让 `sum` 只保存当前项，前面的部分和被覆盖。最终得到最后一个元素，不是总和。

第二件展品把加法写反了目标：

```cpp
a[i] += sum;
```

这会修改数组当前格，却没有更新 `sum`。赋值只直接改变左边，不能因为两个变量都参与了加法就期待它们互相理解。

第三件展品初始化为第一项，却仍从 0 开始循环：

```cpp
int sum = a[0];
for (int i = 0; i < n; i++) {
    sum += a[i];
}
```

`a[0]` 被算了两次。对 `6、4、9、3、8`，结果是 36。若选择 `sum=a[0]`，循环必须从 `i=1` 开始；若从 `i=0` 开始，`sum` 就应从 0 开始。初始状态和循环范围必须配套。

第四件展品在循环外多加一次最后元素。R-01 评价：“人类为了确保没有漏算，常采用再算一次的方法。该策略确实消除了‘漏算’，并制造了‘重复算’。”

---

## 七、翻译训练

### 训练一：补状态表

对 `5、-2、7` 求和：

| 当前项 | `sum` 前 | `sum` 后 |
|---:|---:|---:|
| — | 0 | 0 |
| 5 | 0 | 5 |
| -2 | 5 | 3 |
| 7 | 3 | 10 |

负数不会破坏累加器；它只是让目前总和减少。

### 训练二：从代码还原数学

```cpp
int total = 0;
for (int i = 2; i < 6; i++) {
    total += a[i];
}
```

它表示：

\[
total=a_2+a_3+a_4+a_5
\]

不要自动翻译成整个数组之和。代码从 2 开始，在 6 之前停止。

### 训练三：找出丢失的状态

若程序每轮只写 `sum=a[i]`，缺少的是“旧 `sum` 参与新 `sum` 的计算”。改写目标关系：

\[
\text{newSum}=\text{oldSum}+a_i
\]

再从关系生成赋值。

### 训练四：选择初始值

- 求一批数量的和：初始 `sum=0`；
- 统计乘积：初始 `product=1`；
- 若题目明确已有第一项放进累积状态，则循环从下一项开始。

自动农场的显示牌终于停在 30。农场主数完五箱，确认没有少，也没有多出一箱需要解释来源。

托马斯在登记簿上写：

> 数学的总和是最终关系；代码里的 `sum` 是一段不断增长的记忆。它从“什么都没加”的 0 出发，每遇到一项，就把过去和现在合成新的目前总和。

R-01 在旁边补了一行：“本日小麦总产量 30。此前显示的 8 已覆盖，未对农业经济造成永久影响。”