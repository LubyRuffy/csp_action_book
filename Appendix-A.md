# 附录 A　数学到代码常见映射表

这张表不是模板清单。使用时应先说清“过程中的状态代表什么”，再选择代码结构。

| 数学表达或自然语言 | 程序中的状态 | 更新动作 | 常见代码骨架 | 首要检查 |
|---|---|---|---|---|
| 新值由旧值算出 | 同一个变量的旧值与新值 | 先算右边，再写左边 | `x = x + delta;` | 旧值以后还需不需要 |
| 两个量同时交换 | `x`、`y`、旧 `x` | 保存、覆盖、恢复 | `temp=x; x=y; y=temp;` | 第一次覆盖会毁掉谁 |
| 对每个 $i$ | 当前下标 `i` | 每轮处理一个位置 | `for (int i=0; i<n; i++)` | 起点、终点、变化方向 |
| 区间 $[l,r]$ | 当前位置 `i` | 从 `l` 走到 `r` | `for (int i=l; i<=r; i++)` | 两端是否包含 |
| 区间 $[l,r)$ | 当前位置 `i` | 从 `l` 走到 `r-1` | `for (int i=l; i<r; i++)` | 停止位置是否为首个无效点 |
| 分情况 | 条件与结果 | 只执行命中的分支 | `if / else if / else` | 条件是否重叠、顺序是否影响 |
| $S=\sum a_i$ | 已处理部分的和 `sum` | `sum += a[i]` | 累加器从 0 开始 | 循环是否覆盖全部目标 |
| $P=\prod a_i$ | 已处理部分的积 `product` | `product *= a[i]` | 乘积从 1 开始 | 初始值能否保持首项 |
| “有几个满足” | 已找到的个数 `count` | 满足一次就 `count++` | 条件嵌入循环 | 加的是个数还是元素值 |
| $\max(a_0,\ldots)$ | 目前最大值 `maximum` | 看见更大者才更新 | 用第一个元素初始化 | 空数组与全负数 |
| 最大值的位置 | 值卡与位置卡 | 新冠军出现时一起更新 | 同一 `if` 中改两个变量 | `>` 与 `>=` 保留哪个位置 |
| 平均数 | 总和、数量、平均值 | 先求和，再做浮点除法 | `double(sum) / n` | `n==0` 与整数除法 |
| “至少存在一个” | `found` | 初始假，找到后改真 | `bool found=false;` | 找到后是否需要继续 |
| “所有都满足” | `allValid` | 初始真，发现反例改假 | 反例检测 | 空集合约定与提前停止 |
| 数列递推 | 前一项或前几项 | 由旧状态算出新项 | 先算 `current` 再滚动 | 更新顺序是否破坏旧值 |
| 数组插入 | 有效长度与插入位置 | 右移、写入、长度加一 | 从右向左复制 | 容量与位置合法性 |
| 数组删除 | 有效长度与删除位置 | 左移、长度减一 | 从左向右复制 | 最后一轮不能读 `a[n]` |
| 重叠区域右移 | 尚未读取的源值 | 先搬最右端 | 倒序循环 | 目标会不会覆盖未来源 |
| 重叠区域左移 | 尚未读取的源值 | 先搬最左端 | 正序循环 | 下一源是否仍保持旧值 |
| 二维位置 | `row`、`column` | 分别更新行列 | `a[row][column]` | 行列顺序和两个边界 |

## 使用这张表时的五问

1. 最终答案在程序运行中应改写成什么“目前状态”？
2. 尚未处理任何数据时，这个状态应该是什么？
3. 每处理一个新数据，状态怎样变化？
4. 本次写入会覆盖哪个旧值，那个旧值以后还需要吗？
5. 什么条件说明所有目标都已处理？

如果五问说不清，不要急着挑代码骨架。模板只能替你打字，不能替你决定每个格子在什么时刻保存什么。

## 常见误译速查

| 误译 | 实际发生 | 修正方向 |
|---|---|---|
| 把 `=` 当数学等号 | 误以为关系会持续成立 | 读成“计算右边，写入左边” |
| `x=y; y=x;` 交换 | 旧 `x` 第一行后丢失 | 先保存旧值 |
| 最大值从 0 开始 | 全负数得到不存在的 0 | 用第一个元素初始化 |
| `double a=sum/n;` | 右边先做整数除法 | 除法前转换操作数 |
| `i<=n` 遍历数组 | 多访问 `a[n]` | 使用 `i<n` |
| 插入时直接写目标格 | 原元素被替换 | 先从右向左腾位置 |
| 删除时写 0 | 长度与位置都没变化 | 左移补位并 `n--` |
| 二维数组写 `[x][y]` | 行列可能颠倒 | 先写坐标到行列的翻译表 |
