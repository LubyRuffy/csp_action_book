# 第二十二章　一个公式太长时，怎样拆成几步

皇家仓库收到一张结算单。六箱萤石粉，每箱 25 枚绿宝石；其中两箱受潮，每箱扣 8 枚；修复后的货款再打九折，最后加 12 枚铁路运费。

账房给出的公式是：

\[
T=(q\times p-b\times d)\times(100-r)\div100+f
\]

托马斯把所有符号塞进一行，让 R-01 计算。机器人报出：

```text
应付：122 枚绿宝石
```

账房村民当场把柜台窗户关上了。正确结果应是 132.6 枚。R-01 坚持自己的整数 122 非常完整，完整到一枚小数都没有。

Notch 看了代码：

```cpp
int total = q * p - b * d * (100 - r) / 100 + f;
```

“公式没抄完整，”他说，“括号丢了，类型也丢了。你把一条长公式当成了搬运符号，而不是一串需要检查的计算动作。”

---

## 一、数学把多层动作压在一行里

先代入数据：

```text
q=6, p=25, b=2, d=8, r=10, f=12
```

公式的层次是：

1. 算原货款 `q×p`；
2. 算损坏扣款 `b×d`；
3. 用原货款减扣款；
4. 对剩余货款打九折；
5. 加固定运费。

数学括号把这些关系压缩为：

\[
((6\times25)-(2\times8))\times0.9+12
\]

人可以沿着括号读。计算机也能按运算优先级算一行，但初学阶段若一行出了问题，很难看出究竟是货款、扣款、折扣还是运费先偏离。

长表达式不是更高级的数学，只是把多个中间状态藏起来了。拆开它，相当于把藏在括号里的房间逐个点亮。

---

## 二、每个中间结果都需要一个格子

这张账单需要四个过程变量：

- `gross`：尚未扣损坏费用的原货款；
- `damageCost`：受潮货物共扣多少；
- `afterDamage`：扣款后的货款；
- `afterDiscount`：打折后的货款。

最终 `total` 再加运费。

这些名字不是为了把程序写长，而是让每个格子回答“现在算到哪里”。逐帧状态如下：

| 执行动作 | `gross` | `damageCost` | `afterDamage` | `afterDiscount` | `total` |
|---|---:|---:|---:|---:|---:|
| 初始 | ? | ? | ? | ? | ? |
| `6×25` | 150 | ? | ? | ? | ? |
| `2×8` | 150 | 16 | ? | ? | ? |
| `150-16` | 150 | 16 | 134 | ? | ? |
| `134×0.9` | 150 | 16 | 134 | 120.6 | ? |
| `120.6+12` | 150 | 16 | 134 | 120.6 | 132.6 |

现在若答案不对，可以检查状态第一次出错的那一行。账房也能明确争论“受潮扣款是 16”，而不是对着一串括号发出越来越用力的“哼”。

---

## 三、从计算顺序生成代码

代码出现前先确认：已知六个输入量；需要保存可检查、会重复使用的中间结果；每一步只依赖前面已经算好的格子；旧值不会被覆盖，因为每层使用不同变量；折扣可能产生小数，所以相关结果使用 `double`，除数写成 `100.0`。

```cpp
double gross = q * p;
double damageCost = b * d;
double afterDamage = gross - damageCost;
double afterDiscount = afterDamage * (100 - r) / 100.0;
double total = afterDiscount + f;
```

逐行配音：

> 先算所有货物原价。再单独算受潮扣款。两者相减，得到可打折的货款。按折扣率计算新货款，最后加固定运费。

这五行与原公式表达同一关系。不同之处是，代码把关系摊开成了五个可观察时刻。

---

## 四、括号到底保护了什么

托马斯的错误代码中：

```cpp
q * p - b * d * (100 - r) / 100 + f
```

乘除先于加减，所以折扣只作用在 `b*d` 上。它实际表达：

\[
q p-\frac{bd(100-r)}{100}+f
\]

这不是原公式。括号不是装饰，它在声明“先把哪一整块当成下一步的输入”。

如果一定要写成一行，至少应是：

```cpp
double total = (q * p - b * d)
             * (100 - r) / 100.0 + f;
```

但一行正确，不代表一行适合检查。入门代码首先要让执行过程看得见。皇家账房不会因为代码少占三行纸，就原谅少收十枚绿宝石。

---

## 五、整数除法为什么把折扣吃掉

假设写成：

```cpp
int rate = 10;
double factor = (100 - rate) / 100;
```

右边两个数都是整数，`90/100` 先做整数除法，结果是 0，然后 0 才被放进 `double factor`。准备一个小数格子，不能让已经被截掉的小数复活。

状态表很短，却很致命：

| 动作 | 右边参与运算的类型 | 右边结果 | `factor` 写入值 |
|---|---|---:|---:|
| `(100-10)` | `int` | 90 | 尚未写入 |
| `90/100` | `int/int` | 0 | 尚未写入 |
| 赋给 `double` | 已得到 0 | 0 | 0.0 |

改成 `100.0` 后，除法中有一个 `double`：

```cpp
double factor = (100 - rate) / 100.0;
```

结果才是 0.9。类型也属于状态。它决定格子能保存什么，以及计算过程中小数是否能活到下一步。

---

## 六、重复使用的部分值得命名

另一张矿石保险单要求同时计算税费和赔付，两者都要使用“原价减损坏扣款”：

\[
N=qp-bd
\]

\[
tax=N\times t/100,
\qquad payout=N-tax
\]

可以把 `N` 保存为 `net`：

```cpp
double net = q * p - b * d;
double tax = net * taxRate / 100.0;
double payout = net - tax;
```

如果不保存，就会重复抄写 `q*p-b*d`。抄得越多，漏一个括号的机会越多。中间变量的价值有两种：一是让计算可检查，二是给重复使用的结果留一份可靠副本。

当然，不是每一个 `2+3` 都必须单独建格子。判断标准是：这个中间结果是否代表一个清楚阶段，是否需要复用，是否容易单独出错。变量不是按公式长度收费，也不该为了显得认真而成群出现。

---

## 七、错误博物馆：长公式展柜

### 展品一：括号搬丢了

目标是 `(a+b)*c`，代码却写 `a+b*c`。乘法先做，数学关系已经改变。修复方法不是“再算一次看看”，而是先给计算分层。

### 展品二：小数格来得太晚

```cpp
double average = sum / count;
```

若 `sum`、`count` 都是 `int`，除法先截断。可写 `sum * 1.0 / count`，或先把一个量转为 `double`。

### 展品三：中间结果使用旧版本

```cpp
double net = gross - damage;
damage = newDamage;
double total = net + delivery;
```

`net` 不会因 `damage` 后来改变而自动更新。若账单应使用新扣款，必须在更新后重新计算 `net`。

### 展品四：变量名字与公式阶段不符

把扣款后的金额命名为 `gross`，机器不会报错，人却容易在后面再次扣款。好名字应说明格子此刻保存的是哪一层结果。

### 展品五：一行重复三遍同一长式

即使每次都正确，也难以确认三份是否真的相同。把公共部分存入变量，比训练肉眼找括号更可靠。

---

## 八、翻译训练

### 1. 数学到分步代码

\[
A=(x+y)\times(x-y)+k
\]

一种清楚的翻译是：

```cpp
int sumPart = x + y;
int differencePart = x - y;
int productPart = sumPart * differencePart;
int answer = productPart + k;
```

请说明每个变量对应哪个括号阶段。

### 2. 补状态表

取 `x=7,y=2,k=3`。四步结果依次是 `9,5,45,48`。哪一步开始偏离，最终答案就从哪一步失真。

### 3. 代码到公式

```cpp
double base = length * width;
double waste = base * rate / 100.0;
double usable = base - waste;
```

还原为：

\[
usable=length\times width\times(1-rate/100)
\]

并说明 `base`、`waste` 是过程变量，`usable` 是最终结果。

### 4. 找类型错误

`double half = 1 / 2;` 得到 0.0。改为 `1.0/2`。请用“先计算右边，再写左边”解释，不能只说“C++ 就这样”。

### 5. 自己拆一步

公式 `cost=(weight-5)*extra+base` 中，若 `weight` 可能不超过 5，还缺少分情况判断，不能仅靠拆表达式解决。先确定适用条件，再计算超出重量。

---

## 九、账房重新开窗

R-01 按五个中间格重新计算。显示屏依次出现 150、16、134、120.6、132.6。账房村民每看见一个数就核对一遍，最后把窗户推开了半格，表示程序暂时恢复信用。

托马斯把原来那行长代码保留在错误博物馆里。它并非因为长而有罪，而是因为它藏住了两个错误：折扣作用范围不对，整数除法又吞掉小数。

Notch 在结算单背面写道：

> 长公式先拆成有意义的中间结果；每一步确认依赖、顺序和类型，再把结果交给下一步。

数学的一对括号可以跨过几层计算。代码若想让人检查，就应把每一层变成能看见的状态。少写几行从来不是仓库结算的目标，少丢几枚绿宝石才是。