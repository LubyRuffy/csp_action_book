# 第八章　“有几个”怎样变成计数器

自动农场刚交完小麦，仓库西墙就传来一声闷响。

一只苦力怕在“严禁苦力怕靠近”的木牌下面完成了爆炸。木牌碎得很彻底，禁令的执行效果也因此得到了客观评价。

受波及的六只货箱，损坏程度记录为：

```text
0  3  0  2  5  0
```

其中 0 表示完好，大于 0 表示损坏级别。仓库只问一件事：有几个箱子受损？

托马斯让 R-01 把所有大于 0 的记录加起来。机器人算出：

\[
3+2+5=10
\]

维修村民立刻拿来十份报价单。

“明明只有三个坏箱子！”托马斯说。

“你的程序累计的是损坏级别。”R-01 回答，“问题询问箱子个数。数值 5 表示一个损坏程度为 5 的箱子，不表示五个箱子。”

维修村民把多余报价单收回去时，表情像是刚失去了七项传统权益。

---

## 一、找到哪些，与一共有几个

数学上，我们先判断每个记录是否满足条件：

\[
a_i>0
\]

满足条件的位置是 1、3、4，共三个。这里有两种不同结果：

- 原数据告诉我们“这个箱子损坏多严重”；
- 计数结果告诉我们“到目前为止遇到几个损坏箱子”。

因此程序需要的不是求和器，而是计数器：

```text
count = 已经检查过的箱子中，满足条件的个数
```

开始前一个箱子也没检查，满足条件的个数当然是 0。每检查一格，先判断条件；若满足，`count` 增加 1；若不满足，`count` 保持不变。

数学关系可以写成：

\[
C=\#\{i\mid a_i>0\}
\]

符号 `#` 表示集合中元素的个数。程序要把这个“有几个”展开成一连串真假判断和状态更新。

---

## 二、计数器到底在记什么

R-01 逐个走过六只箱子。它不是等走完以后才突然知道 3，而是让 `damagedCount` 依次变化：

| 下标 `i` | `a[i]` | `a[i] > 0` | `damagedCount` 更新前 | 更新后 |
|---|---:|---|---:|---:|
| 开始 | — | — | 0 | 0 |
| 0 | 0 | 假 | 0 | 0 |
| 1 | 3 | 真 | 0 | 1 |
| 2 | 0 | 假 | 1 | 1 |
| 3 | 2 | 真 | 1 | 2 |
| 4 | 5 | 真 | 2 | 3 |
| 5 | 0 | 假 | 3 | 3 |

注意，`damagedCount=2` 的时刻并不是“当前箱子的值为 2”，而是“前四个位置里已经发现两个坏箱子”。变量值恰好和某只箱子的损坏级别相同，只是巧合。若变量名只叫 `x`，这种巧合很容易把人带进沟里；叫 `damagedCount`，格子的工作就清楚多了。

每一轮结束后，都能说：

> `damagedCount` 等于从下标 0 到当前下标之间，损坏箱子的数量。

处理完最后一格，“当前范围”恰好变成全部六格，所以过程状态自然成为最终答案。

---

## 三、为什么满足一次只增加一次

托马斯指着损坏级别 5：“它坏得很严重，为什么也只加一？”

“因为它仍然只占一个位置。”Notch 说，“问题统计对象，不统计对象身上的数值。”

计数更新关系是：

\[
\text{newCount}=
\begin{cases}
\text{oldCount}+1,&\text{当前元素满足条件}\
\text{oldCount},&\text{否则}
\end{cases}
\]

条件决定“是否更新”，对象的出现决定“增加一”。如果题目改问“所有损坏级别之和”，才应把 `a[i]` 加进去。

代码出现前先把命令翻译完整：

- 已知：数组保存六个损坏级别，`n=6`；
- 要记住：`damagedCount` 表示已检查部分中损坏箱子的个数；
- 初始值：还没检查，个数为 0；
- 当前条件：`a[i] > 0`；
- 更新动作：条件为真时，把旧计数加 1 后写回；为假时不写；
- 旧计数需要参与右边计算，但一条赋值内部会先读取，不需额外保存；
- `i` 从 0 走到 `n-1`，`i==n` 时停止。

```cpp
int damagedCount = 0;
for (int i = 0; i < n; i++) {
    if (a[i] > 0) {
        damagedCount++;
    }
}
```

`damagedCount++` 是把当前计数增加一。它放在 `if` 的大括号里，说明只有条件为真时才执行。代码的位置安排了这次更新发生的条件。

---

## 四、条件换了，计数器的工作方式不变

统计偶数时，当前条件变成：

```text
a[i] % 2 == 0
```

统计正数时，条件是：

```text
a[i] > 0
```

统计等于指定数字 `target` 的元素时，条件是：

```text
a[i] == target
```

三种任务只替换判断，计数器的状态含义和更新方式相同：开始为 0，每满足一次就加一。

例如记录：

```text
2  7  2  -1  4  2
```

统计偶数得到 4，因为位置 0、2、4、5 满足；统计正数得到 5，因为除了 -1 都满足；统计数字 2 得到 3。

把三种状态放在一起看：

| `i` | `a[i]` | `evenCount` | `positiveCount` | `twoCount` |
|---|---:|---:|---:|---:|
| 初始 | — | 0 | 0 | 0 |
| 0 | 2 | 1 | 1 | 1 |
| 1 | 7 | 1 | 2 | 1 |
| 2 | 2 | 2 | 3 | 2 |
| 3 | -1 | 2 | 3 | 2 |
| 4 | 4 | 3 | 4 | 2 |
| 5 | 2 | 4 | 5 | 3 |

同一个元素可以同时满足多个条件，所以三个计数器都可能更新。它们分别保存不同问题的答案，不会因为都叫“个数”就自动合并。

若只统计偶数，写代码前仍要说明数组、计数含义、初始 0、偶数条件、满足时加一、下标范围与停止位置，然后才写：

```cpp
int evenCount = 0;
for (int i = 0; i < n; i++) {
    if (a[i] % 2 == 0) {
        evenCount++;
    }
}
```

换条件不是背一份新模板，而是重新说清“什么算一次满足”。

---

## 五、计数与求和可以同时存在，但别混岗

维修部后来还问：坏箱子有几个，损坏级别总和是多少？这次需要两个格子：

```text
damagedCount = 已发现的坏箱个数
damageSum = 已发现的损坏级别总和
```

遇到一个大于 0 的记录时，前者加 1，后者加当前值。

代码前的状态设计是：两者开始都为 0；同一个条件控制两次更新；`damagedCount` 记录对象数量，`damageSum` 记录数值总量；所有元素检查完停止。

```cpp
int damagedCount = 0;
int damageSum = 0;
for (int i = 0; i < n; i++) {
    if (a[i] > 0) {
        damagedCount++;
        damageSum += a[i];
    }
}
```

对 `0、3、0、2、5、0`，最终是 `damagedCount=3、damageSum=10`。两个答案都正确，只是回答不同问题。

| 遇到的损坏值 | `damagedCount` | `damageSum` |
|---:|---:|---:|
| 初始 | 0 | 0 |
| 3 | 1 | 3 |
| 2 | 2 | 5 |
| 5 | 3 | 10 |

变量名相当于岗位牌。让 `count` 加损坏级别，就是让门卫顺便负责维修预算；数字可能算出来，岗位含义已经乱了。

---

## 六、错误博物馆：把数字加起来，不是把个数加一

第一件展品正是事故代码：

```cpp
if (a[i] > 0) {
    damagedCount += a[i];
}
```

它统计损坏级别总和。对于每个满足条件的位置，应该登记“又找到一个”，因此更新量是 1。

第二件展品把计数语句放在条件外：

```cpp
if (a[i] > 0) {
}
damagedCount++;
```

无论箱子是否损坏，每轮都会加一，最终得到箱子总数 6。空大括号虽然看起来像在认真判断，实际什么也没控制。

第三件展品每轮清零：

```cpp
for (int i = 0; i < n; i++) {
    damagedCount = 0;
    if (a[i] > 0) damagedCount++;
}
```

最终结果只取决于最后一只箱子是否损坏，历史全部丢失。

第四件展品把 `=` 写成 `==`：

```cpp
if (a[i] = 0) {
    damagedCount++;
}
```

它不是比较，而是在条件里把当前元素改成 0；赋值表达式得到 0，又会被当作假。箱子记录被擦掉，计数还不增加，一次事故解决了两个本来不存在的问题。比较相等必须写 `==`。

---

## 七、翻译训练

### 训练一：数学到状态

任务：统计数组中小于 0 的数有几个。

应设计 `negativeCount`，含义是“已检查部分中的负数个数”，初始为 0，条件为 `a[i] < 0`，满足时增加 1。

### 训练二：代码到问题

```cpp
int count = 0;
for (int i = 0; i < n; i++) {
    if (a[i] == 7) count++;
}
```

它回答“数组中有几个元素等于 7”，不是“这些元素的和是多少”，也不是“7 是否出现”。

### 训练三：补状态表

数组 `1、4、6、3`，统计偶数：

| 当前值 | 条件 | `count` 前 | `count` 后 |
|---:|---|---:|---:|
| 1 | 假 | 0 | 0 |
| 4 | 真 | 0 | 1 |
| 6 | 真 | 1 | 2 |
| 3 | 假 | 2 | 2 |

### 训练四：辨认岗位

若变量每次执行 `result += a[i]`，它在累加数值；若每次满足条件执行 `result++`，它在计数。不要只根据名字猜，观察更新时加进去的究竟是“当前值”还是“一个”。

维修村民最终只带走三份报价单，不过又添加了“重新打印费”。托马斯在事故报告上写：

> 求和保存的是已经收集的数值总量；计数保存的是条件已经成立过多少次。遇到一个满足对象，计数器增加一，不管对象身上的数字有多大。

R-01 检查六只箱子后确认：“损坏箱数为 3，损坏级别总和为 10。两个数字现已分居不同变量，暂未继续争夺同一块显示牌。”