# 第二十三章　给代码配音

皇家仓库新装了一套远程指挥台。操作员看不见地下传送带，只能听托马斯通过铜管朗读程序，再按他说的动作控制 R-01。

第一批货是五个箱子的库存：`4,0,7,0,3`。任务是统计总库存和空箱数。

托马斯对着铜管念：

“`sum` 加等于 `a` 方括号 `i`。如果 `a` 方括号 `i` 双等于零，`emptyCount` 加加。”

地下沉默了几秒。

操作员回话：“请问‘方括号’要推动哪根拉杆？”

托马斯又解释：“就是把它们算一下。”

R-01 的声音从管道里传来：“‘它们’未绑定具体内存位置。‘算一下’未指定读取、判断与写回顺序。”

最后，传送带把所有箱子原样送回，却在报表上写了两个巨大的问号。Notch 看着托马斯：“你会念符号，但还不会给代码配音。能让机器工作的不是符号名称，而是连续动作。”

---

## 一、不要读符号，要读内存动作

这句代码：

```cpp
sum += a[i];
```

若读作“sum 加等于 a i”，只是把键盘上的字符搬进空气。真正的动作是：

> 读取目前的总和，再读取当前下标 `i` 指向的库存，把两者相加，用新总和覆盖 `sum`。

它回答了四件事：读哪些旧值，做什么计算，写入哪里，哪个旧值被覆盖。

再看：

```cpp
emptyCount++;
```

准确配音是：

> 读取目前已经发现的空箱数，加一，再写回计数格。

“加一”不是给当前库存加一，也不是给循环下标加一。左边的格子决定谁变化。

R-01 把两张卡贴在铜管旁：

```text
右边：提供当前旧数据
计算：产生一个新结果
左边：接收结果并覆盖旧内容
```

---

## 二、一轮循环是一小段故事

任务开始前，`sum=0`，因为还没加入任何库存；`emptyCount=0`，因为还没检查任何箱子。`i` 表示当前站在哪个箱子前。

核心代码是：

```cpp
int sum = 0;
int emptyCount = 0;
for (int i = 0; i < n; i++) {
    sum += a[i];
    if (a[i] == 0) {
        emptyCount++;
    }
}
```

不要一口气说“循环求和计数”。给某一轮完整配音：

> 机器人来到 `i` 号箱。先把这一箱的库存加入目前总和。然后检查这一箱是否为零；若是，就把空箱记录增加一。处理完后，`i` 向右移动，直到 `i` 不再小于箱子数 `n`。

状态表把声音变成画面：

| 本轮 `i` | 当前 `a[i]` | 加入前 `sum` | 加入后 `sum` | 判断是否为空 | 轮末 `emptyCount` |
|---:|---:|---:|---:|---|---:|
| 初始 | — | — | 0 | 尚未判断 | 0 |
| 0 | 4 | 0 | 4 | 否 | 0 |
| 1 | 0 | 4 | 4 | 是 | 1 |
| 2 | 7 | 4 | 11 | 否 | 1 |
| 3 | 0 | 11 | 11 | 是 | 2 |
| 4 | 3 | 11 | 14 | 否 | 2 |

最后 `sum=14`，`emptyCount=2`。这两个答案不是循环结束时突然出现的。它们在每一轮都代表“到目前为止”的结果。

---

## 三、给条件语句配音

看到：

```cpp
if (a[i] > maximum) {
    maximum = a[i];
    maxPosition = i;
}
```

不能只说“如果大于最大值就更新”。这会漏掉最容易犯错的同步状态。

准确配音是：

> 比较当前库存和目前最大库存。若当前库存更大，就把当前库存复制进最大值格，同时把当前下标复制进最大位置格；否则两格都保持不变。

这里 `maximum` 与 `maxPosition` 描述同一个候选箱。值更新而位置不更新，会让报表声称“七件库存位于零号箱”，而零号箱实际只有四件。村民不会因为最大值算对一半，就支付另一半审计费。

以 `4,0,7,0,3` 为例：

| 检查位置 | 当前值 | 检查前 `maximum` | 条件 | 检查后 `maximum` | `maxPosition` |
|---:|---:|---:|---|---:|---:|
| 初始化 0 | 4 | — | 直接采用 | 4 | 0 |
| 1 | 0 | 4 | 假 | 4 | 0 |
| 2 | 7 | 4 | 真 | 7 | 2 |
| 3 | 0 | 7 | 假 | 7 | 2 |
| 4 | 3 | 7 | 假 | 7 | 2 |

配音时说出“否则不变”，能提醒我们条件不成立也是一种明确状态。

---

## 四、给数组移动配音

下午要把新记录 5 插到位置 2。原数组是：

```text
4 0 7 3 ?
```

R-01 收到：

```cpp
for (int j = n; j > position; j--) {
    a[j] = a[j - 1];
}
a[position] = 5;
n++;
```

逐句配音：

> 从旧数据后面的空格开始。把左边相邻格的内容复制到当前格，然后向左走；只要还在插入位置右边，就继续。空位腾出后，把 5 写入插入位置，最后把有效长度增加一。

`j--` 不能读成“j 减减”就结束。它是在说机器人下一步检查左边一个位置。`j>position` 不能读成“大于”，还要说出停止画面：当 `j` 到达插入位置时，不再移动，否则刚腾出的空位也会被继续覆盖。

状态变化：

| 动作 | `a[0]` | `a[1]` | `a[2]` | `a[3]` | `a[4]` | `n` |
|---|---:|---:|---:|---:|---:|---:|
| 初始 | 4 | 0 | 7 | 3 | ? | 4 |
| `a[4]=a[3]` | 4 | 0 | 7 | 3 | 3 | 4 |
| `a[3]=a[2]` | 4 | 0 | 7 | 7 | 3 | 4 |
| 写入位置 2 | 4 | 0 | 5 | 7 | 3 | 4 |
| `n++` | 4 | 0 | 5 | 7 | 3 | 5 |

中间出现两个 7，是因为赋值复制数值，不会把源格清空。配音若说成“把 7 搬走”，反而会在脑中错误地把 `a[2]` 想成空格。更准确的词是“读取并复制”。

---

## 五、从配音反推代码

Notch 不再给代码，只念动作：

> 准备一个从零开始的损坏箱计数。依次检查每个箱子；若当前库存小于零，就把计数增加一。

托马斯先找状态：`brokenCount` 记录目前发现多少个负数；`i` 指向当前箱；条件是 `a[i]<0`；更新是 `brokenCount++`；范围为 `0` 到 `n-1`。

```cpp
int brokenCount = 0;
for (int i = 0; i < n; i++) {
    if (a[i] < 0) {
        brokenCount++;
    }
}
```

配音不是代码完成后的表演。它本身就是数学到代码中间那层“状态动作语言”。当一句人话能精确到读取谁、修改谁、何时重复，代码结构已经显出来了。

---

## 六、怎样给一整段代码配音

面对多行代码，可以按六个问题组织声音：

1. 每个变量的工作是什么？
2. 初始状态为什么合理？
3. 循环变量当前指向谁？
4. 本轮读取哪些旧值？
5. 条件成立时更新哪些格子？
6. 什么时候移动，什么时候停止？

例如：

```cpp
int count = 0;
for (int i = 1; i < n; i++) {
    if (a[i] == a[i - 1]) {
        count++;
    }
}
```

完整配音：

> `count` 记录目前发现的相邻相等对数，开始为零。从一号元素开始，因为每轮还要读取左边的 `i-1`。若当前元素等于左邻元素，就发现一对，计数加一。随后向右检查，直到 `i=n` 时停止。

这段数学说明是：

\[
count=\#\{i\mid 1\le i<n,\ a_i=a_{i-1}\}
\]

能从代码说到动作，再从动作说回数学关系，才算双向翻译。

---

## 七、错误博物馆：失声展厅

### 展品一：只念运算符

“`x` 等于 `x` 加一”没有说清右边使用旧 `x`，也没说写回会覆盖。正确配音应包含“读取、计算、写回”。

### 展品二：把复制说成移动

`a[j+1]=a[j]` 不会清空 `a[j]`。若配音成“搬走”，状态表就会凭空出现空格。代码做的是读取左格数值，并把副本写入右格。

### 展品三：省略循环边界

“检查所有元素”仍不够。对数组应说出从哪个下标开始、最后处理哪个、何时停止。边界含糊时，苦力怕会替你决定多检查一格。

### 展品四：把答案名当成含义

变量叫 `sum` 也可能被错误地写成计数器。含义要由初始化和更新共同验证：从 0 开始，每轮加入 `a[i]`，它才是目前总和。

### 展品五：条件成立时只说一半

最大值与位置必须同步更新。配音若只提最大值，很容易漏写位置。口头说明也需要保持状态成组变化。

---

## 八、翻译训练

### 1. 给代码配音

```cpp
product *= a[i];
```

参考：读取目前乘积和当前元素，相乘，把新乘积写回 `product`。

### 2. 指出错误用词

有人把 `x=y` 说成“把 `y` 搬进 `x`”。哪里不准确？`y` 本身不变，应说复制 `y` 当前的值到 `x`。

### 3. 从声音写代码

“若当前库存小于十，就把缺货箱数增加一。”

```cpp
if (a[i] < 10) {
    lowStockCount++;
}
```

### 4. 从代码写数学

```cpp
sum += a[i] * a[i];
```

循环处理全部元素时，最终关系是：

\[
sum=\sum_{i=0}^{n-1}a_i^2
\]

前提是 `sum` 从 0 开始。

### 5. 补完停止条件

“从右端开始向左复制，直到……”插入位置右边的最后一个元素已移动，`j` 到达 `position` 时停止，不能再覆盖插入格。

### 6. 给状态表还原一句代码

若一行执行后只有 `count` 从 3 变 4，其他格不变，最可能的更新是 `count++`，但还需结合上下文判断它受什么条件控制。

---

## 九、铜管里的第二次指挥

托马斯重新拿起铜管。

“准备两个格子，都写零。第一个记已经加入的库存总数，第二个记已经发现的空箱数。机器人从零号箱开始，每到一箱，读取当前库存，加进总数；若库存等于零，把空箱数加一。处理后向右移动，走到箱子数量时停止。”

地下的齿轮依次响起。报表很快送回：

```text
总库存：14
空箱：2
```

操作员说：“这次我知道每根拉杆什么时候动了。”

R-01 说：“这次人类语言包含了可执行状态变化。”

Notch 在指挥台上刻下一句：

> 给代码配音，不是朗读符号，而是说清每一步读取什么、改变什么、何时重复、在哪里停止。

托马斯发现，一旦能把程序讲成连续动作，代码不再是一堵由括号和分号砌成的墙。它变回了仓库里的箱子、手里的旧值、脚下的下标，以及时间轴上一步接一步的真实变化。