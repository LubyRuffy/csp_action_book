# 附录 E　教师使用指南

本书适合已经接触变量、条件、循环和数组，但经常出现“数学会做，代码不会写”的学生。教学重点不是增加模板数量，而是训练学生在代码执行过程中持续更新当前状态。

## 一、每章建议课堂流程

### 1. 先演事故，不先报术语

教师先展示错误动作，例如直接执行 `a[1]=3`，让学生亲眼看见 5 消失。不要马上说“这叫覆盖”，先问：

- 哪一格变了？
- 原来的值去了哪里？
- 下一步还需不需要它？

学生对事故产生解释需求后，再引入“旧值、覆盖、临时变量、移动方向”等概念。

### 2. 只展示一帧

不要一开始给完整代码。每次只执行一句，让学生填写下一行状态表。若学生直接报最终答案，追问：“你说的是最后，还是这句刚执行完？”

### 3. 先说动作，再写符号

要求学生给代码配音：

```text
读取谁 → 算什么 → 写到哪里 → 覆盖谁
```

只有动作说清楚后才允许写代码。这样能区分真正理解与背过相似片段。

### 4. 双向翻译

同一案例至少做两次：

- 从数学或任务描述生成状态表，再生成代码；
- 从代码还原每个变量的含义、循环范围和最终数学关系。

只会单向套模板的学生，遇到变量名变化时仍会失去方向。

## 二、课堂提问库

### 赋值与变量

- 等号右边读取的是执行前还是执行后的值？
- 一句赋值直接改变几个格子？
- 左边旧值何时消失？
- `y=x` 是复制一次，还是建立永久关系？
- 这个变量为什么必须存在？若删掉它，哪一步会忘记信息？

### 循环与边界

- 当前 `i` 代表第几个元素还是下标？
- 条件最后一次为真时，`i` 是多少？
- 停止时的 `i` 能否访问数组？
- 少一次会漏谁，多一次会访问谁？
- `i` 的变化是否正在靠近停止条件？

### 累加、计数与最值

- 尚未处理任何数据时，状态应该是什么？
- `sum` 与 `count` 分别加什么？
- 最大值为什么不应随意从 0 开始？
- 最大值与位置为何要同步更新？
- 当前状态描述的是全部答案，还是已处理部分的答案？

### 数组移动与递推

- 这次写入会覆盖哪个旧值？
- 该旧值后面还要读取吗？
- 若从另一个方向移动，第一次丢失的是谁？
- 长度在什么时候修改最容易解释？
- 新状态依赖哪些旧状态？哪些必须先保存？

## 三、常见错误诊断表

| 学生表现 | 可能缺失的理解 | 诊断任务 | 教学处理 |
|---|---|---|---|
| 把 `x=x+1` 当错方程 | 赋值方向与时间 | 给一句赋值配音 | 用透明格演示先读后写 |
| 预测 `y` 会随 `x` 自动变化 | 一次计算与持续关系混淆 | 执行 `y=x; x=9;` | 分别画两个变量轨迹 |
| 交换得到两个相同值 | 旧值保存意识 | 找第一句后消失的值 | 禁止直接给 `temp`，先让学生命名丢失数据 |
| 累加只得到最后一项 | 状态更新未使用旧值 | 比较 `sum=a[i]` 与 `sum+=a[i]` | 写“目前总和”的口头定义 |
| 全负数最大值为 0 | 初始化不表示已处理状态 | 用纯负测试数据 | 让首元素先当候选者 |
| 循环多一次 | 长度与末下标混淆 | 列出所有 `i` | 在表中单列失败条件检查 |
| 插入后数据重复 | 复制方向破坏未读源 | 标出第一次覆盖 | 给每个未读旧值挂“欠债卡” |
| 删除后末尾重复仍被输出 | 逻辑长度未更新 | 标出有效范围 | 区分容量和长度 |
| 递推出现翻倍或错位 | 新旧状态混用 | 每句后填写三变量表 | 强制先算 `current` |
| 二维访问转置 | 横纵与行列映射混乱 | 给坐标写翻译表 | 使用 `row/column` 变量名 |

## 四、不上机口头测评

每次 3—5 分钟，教师给出两三行代码，学生不得只报最终值，必须完整配音。

示例：

```cpp
int x = 3;
int y = x + 2;
x = 9;
```

合格回答应包含：`y` 在第二行读取当时的 `x=3`，得到 5；第三行只覆盖 `x`，所以最终 `y` 仍为 5。

交换测评可问：

```cpp
x = y;
y = x;
```

学生应指出第一句后旧 `x` 消失，而不是只说“交换失败”。

## 五、纸面逐帧测评

给一张空状态表，让学生完成以下项目：

1. 每句执行后填写全部变量；
2. 圈出本句被写入的格子；
3. 划掉被覆盖且无法恢复的旧值；
4. 标出循环当前已处理范围；
5. 在停止检查处写明条件为何为假。

评分时，过程分应高于最终答案分。最终值正确但中间状态混乱，通常来自心算或猜测，不能视为掌握。

## 六、章节掌握标准

### 入门掌握

学生能：

- 区分数学等号与程序赋值；
- 指出一句赋值读取谁、写入谁；
- 画出单变量的变化轨迹；
- 列出简单循环实际访问的下标。

### 稳定掌握

学生能：

- 为求和、计数、最值解释状态含义与初始值；
- 在全负数、空数据和边界位置上主动测试；
- 根据“旧值以后是否需要”决定是否使用临时变量；
- 正确解释插入与删除的移动方向。

### 迁移掌握

学生能在未见过的简单任务中：

- 先设计状态表，再生成代码；
- 从代码反向写出数学关系；
- 找到状态第一次偏离目标的语句；
- 不依赖变量名和固定模板，仍能说清更新顺序。

## 七、建议课时与节奏

| 内容 | 建议课时 | 课堂重点 |
|---|---:|---|
| 序章与第 1—3 章 | 3—4 | 赋值、格子、执行时间 |
| 第 4—6 章 | 3 | 保存旧值与多状态顺序 |
| 第 7—10 章 | 4 | 累加、计数、最值、类型 |
| 第 11—14 章 | 4 | 循环边界、条件、量词 |
| 第 15—19 章 | 5—6 | 数组格子、插入删除、二维坐标 |
| 第 20—22 章 | 3 | 递推旧状态与表达式拆分 |
| 第 23—26 章 | 4 | 双向翻译、诊断、综合任务 |

不要为了赶目录压缩状态表练习。学生若在前三部分没有形成逐帧习惯，后面只会把更长的代码当成更长的咒语。

## 八、教师自检

备课结束前，检查本节是否做到：

- [ ] 故事事故真的导致了知识需求；
- [ ] 代码出现前已经说明状态和顺序；
- [ ] 至少一次让学生预测下一帧；
- [ ] 展示了一个必然出错的反例；
- [ ] 练习包含代码到状态的反向任务；
- [ ] 没把复杂算法识别混入本书范围；
- [ ] 笑点帮助记忆概念，而不是遮住概念。

教学目标不是让学生说“这段代码我见过”，而是让他面对一段没见过的短代码时，仍能把每个格子在每个时刻的变化讲清楚。
