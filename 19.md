# 第十九章　二维坐标怎样变成二维数组

皇家仓库新建了一面五行六列的储物墙。主管说：“把红石火把放在第 2 行、第 4 列。”

托马斯给 R-01 的命令却是：

```cpp
wall[4][2] = 1;
```

机器人把火把装到了第 4 行、第 2 列。那里恰好是苦力怕隔离柜的开门开关。

隔离柜打开以后，R-01 一边追赶苦力怕，一边报告：“行列参数已严格按代码顺序使用。”

Notch 拉下总电闸：“纸上的横纵坐标、口语里的行列和代码中的两个下标，必须先统一。二维数组不是一张会体谅你方向感的地图。”

---

## 一、一维下标不够说明一面墙

一排格子只需要一个位置：

```cpp
a[i]
```

一面储物墙有多行、多列。仅说“第 8 格”并不能直接说明它位于哪一行哪一列，除非另做换算。

二维数组使用两个下标：

```cpp
wall[row][column]
```

第一个下标选行，第二个下标选这一行中的列。

若数组定义为：

```cpp
int wall[5][6];
```

它有 5 行、每行 6 列。有效范围是：

```text
row:    0,1,2,3,4
column: 0,1,2,3,4,5
```

`wall[2][4]` 表示 2 号行中的 4 号列。两个下标共同构成地址牌，调换顺序会指向另一格。

---

## 二、先把格子画出来

用坐标标记一张 3 行 4 列的小表：

| `row \ column` | 0 | 1 | 2 | 3 |
|---:|---:|---:|---:|---:|
| 0 | `[0][0]` | `[0][1]` | `[0][2]` | `[0][3]` |
| 1 | `[1][0]` | `[1][1]` | `[1][2]` | `[1][3]` |
| 2 | `[2][0]` | `[2][1]` | `[2][2]` | `[2][3]` |

固定 `row=1`，改变 `column`，会在同一行里从左向右走：

```text
[1][0] → [1][1] → [1][2] → [1][3]
```

固定 `column=2`，改变 `row`，会在同一列里从上向下走：

```text
[0][2] → [1][2] → [2][2]
```

理解二维下标时，不要只念“两个中括号”。要说清第一张地址牌选哪一排，第二张地址牌选这一排里的哪一格。

---

## 三、为什么需要两层循环

任务是把每个格子都写成它所在的行号与列号之和：

\[
a_{r,c}=r+c
\]

数学中的“对每一行的每一列”包含两重重复。程序需要两个循环变量：

- `row`：当前处理哪一行；
- `column`：当前处理这一行的哪一列。

代码出现前先确定边界：共有 `rowCount` 行、`columnCount` 列，下标都从 0 开始。

```cpp
for (int row = 0; row < rowCount; row++) {
    for (int column = 0; column < columnCount; column++) {
        a[row][column] = row + column;
    }
}
```

外层循环选一行，内层循环把这一行的所有列处理完。然后外层换到下一行，内层 `column` 重新从 0 开始。

以 2 行 3 列为例：

| 执行顺序 | `row` | `column` | 写入位置 | 写入值 |
|---:|---:|---:|---|---:|
| 1 | 0 | 0 | `a[0][0]` | 0 |
| 2 | 0 | 1 | `a[0][1]` | 1 |
| 3 | 0 | 2 | `a[0][2]` | 2 |
| 4 | 1 | 0 | `a[1][0]` | 1 |
| 5 | 1 | 1 | `a[1][1]` | 2 |
| 6 | 1 | 2 | `a[1][2]` | 3 |

最终：

```text
0 1 2
1 2 3
```

---

## 四、数学坐标 `(x,y)` 与数组 `[row][column]`

平面坐标常写 `(x,y)`：

- `x` 表示横向位置；
- `y` 表示纵向位置。

二维数组常写：

```cpp
grid[row][column]
```

若地图的行向下增加、列向右增加，常见对应关系是：

```text
row    ← y
column ← x
```

于是坐标 `(x=4,y=2)` 对应：

```cpp
grid[2][4]
```

不能看到 `(4,2)` 就机械地写 `grid[4][2]`。数学坐标按横、纵说话，数组按行、列说话。

最安全的办法是先写翻译表：

| 地图概念 | 程序变量 | 数组位置 |
|---|---|---|
| 纵向坐标 `y` | `row` | 第一个下标 |
| 横向坐标 `x` | `column` | 第二个下标 |

变量名写成 `row`、`column`，通常比 `i`、`j` 更能阻止方向事故。

---

## 五、上下左右怎样变成下标变化

假设当前位置是：

```text
row = 2
column = 3
```

并约定行号向下增大、列号向右增大：

| 移动 | 新行 | 新列 | 变化 |
|---|---:|---:|---|
| 上 | 1 | 3 | `row - 1` |
| 下 | 3 | 3 | `row + 1` |
| 左 | 2 | 2 | `column - 1` |
| 右 | 2 | 4 | `column + 1` |

例如查看上方一格之前，先计算目标位置：

```cpp
int nextRow = row - 1;
int nextColumn = column;
```

这里用新变量是为了让旧位置仍然清楚。若目标有效，再访问：

```cpp
if (nextRow >= 0 && nextRow < rowCount &&
    nextColumn >= 0 && nextColumn < columnCount) {
    cout << grid[nextRow][nextColumn];
}
```

本章只讨论一次坐标变化与边界检查，不进入路径搜索。

---

## 六、二维边界要同时检查四面

一维数组只检查下标是否在 `[0,n)`。二维数组需要同时保证：

```text
0 <= row < rowCount
0 <= column < columnCount
```

任意一个条件失败，位置都在数组外。

以 3 行 4 列为例：

| 位置 | 行是否合法 | 列是否合法 | 能否访问 |
|---|---|---|---|
| `[2][3]` | 是 | 是 | 能 |
| `[3][1]` | 否 | 是 | 不能 |
| `[1][4]` | 是 | 否 | 不能 |
| `[-1][2]` | 否 | 是 | 不能 |

边界外不是隐藏地图，也不是等待解锁的秘密房间。它只是没有分配给这个二维数组的内存。

---

## 七、状态表追踪一次位置更新

R-01 从 `[2][3]` 向上移动一格。若直接改 `row`，过程很短：

```cpp
row = row - 1;
```

但仍可逐帧解释：

| 时刻 | `row` | `column` | 当前含义 |
|---|---:|---:|---|
| 初始 | 2 | 3 | 位于 `[2][3]` |
| 计算目标 | 1 | 3 | 目标为 `[1][3]` |
| 边界检查后 | 1 | 3 | 目标有效，确认移动 |

若还需要旧位置做日志，应先保存：

```cpp
int oldRow = row;
int oldColumn = column;
row = row - 1;
```

旧状态是否需要保存，仍取决于后面的代码会不会使用它。二维坐标没有逃离本书的核心问题，只是要同时管理两个格子。

---

## 八、错误博物馆

### 展品一：行列写反

```cpp
wall[column][row] = 1;
```

若数组约定是 `[row][column]`，这会访问转置位置，甚至可能越界。

### 展品二：两个循环都用行数

```cpp
for (int column = 0; column < rowCount; column++)
```

非方形数组会漏列或越界。列循环应使用 `columnCount`。

### 展品三：边界写成 `<=`

```cpp
row <= rowCount
```

`row == rowCount` 已是第一行越界位置。有效条件应为 `< rowCount`。

### 展品四：只检查行，不检查列

行合法不能证明整格合法。二维地址的两张牌必须同时有效。

### 展品五：把右移写成 `row++`

右移改变列，不改变行。给代码配音能很快发现动作与坐标不一致。

---

## 九、翻译训练

1. 4 行 6 列数组的最后一个合法位置是什么？
2. 坐标 `(x=3,y=1)` 对应哪个数组下标？
3. 从 `[2][2]` 向左一格，`row` 与 `column` 各怎样变化？
4. 写出遍历 3 行 5 列所有格子的两层循环头。
5. 判断 `[3][0]` 对 3 行 4 列数组是否合法。
6. 给 `grid[row][column]` 配音，必须分别说明两个下标。
7. 找出这段代码中的边界错误：

```cpp
for (int row = 0; row <= rowCount; row++) {
    for (int column = 0; column < columnCount; column++) {
        grid[row][column] = 0;
    }
}
```

参考要点：第一题是 `[3][5]`；第二题是 `[1][3]`；第三题列减一；第五题不合法，因为最大行下标为 2。

---

苦力怕最终被重新关回隔离柜。托马斯在控制台上贴了两张标签：第一个中括号写“行”，第二个写“列”。

二维数组没有增加新的魔法。它只是要求程序同时说清两件事：位于哪一行，以及这一行里的哪一列。每次移动和访问，也都必须让两张地址牌保持在合法范围内。
