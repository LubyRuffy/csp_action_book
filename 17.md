# 第十七章　删除一个元素，方向为什么反过来

皇家仓库的损坏检测器发现 1 号记录无效：

```text
下标：0  1  2  3  4
内容：2  7  4  9  5
```

托马斯命令：“删除 1 号格里的 7。”

R-01 把 `a[1]` 改成 0：

```text
2  0  4  9  5
```

“这不叫删除，这叫把坏记录改成了一个看起来很健康的零。”

“数组格子仍然存在。请定义‘删除’之后其余元素应当处于什么位置。”

主管给出目标：

```text
2  4  9  5
```

Notch 指着固定的五个格子：“删除不会让木架少一格。程序要做的是让后面的有效记录向左补位，再把有效长度减一。”

---

## 一、删除的是逻辑成员，不是物理格子

数组容量固定时，下标不会因为删除而消失。删除位置 `position` 的含义是：

- `a[position]` 不再属于有效序列；
- 它后面的元素各向左一格；
- 有效长度 `n` 减一。

初始状态：

| `a[0]` | `a[1]` | `a[2]` | `a[3]` | `a[4]` | `n` |
|---:|---:|---:|---:|---:|---:|
| 2 | 7 | 4 | 9 | 5 | 5 |

目标状态的有效部分是：

| `a[0]` | `a[1]` | `a[2]` | `a[3]` | `n` |
|---:|---:|---:|---:|---:|
| 2 | 4 | 9 | 5 | 4 |

原来的 1 号格不会被拔走。它会接收旧 `a[2]` 的值。

---

## 二、第一步：右边邻居向左覆盖

删除 `a[1]` 时，先执行：

```cpp
a[1] = a[2];
```

数组变成：

```text
2  4  4  9  5
```

两个 4 暂时同时存在。源格 `a[2]` 没有自动清空，这和插入时暂时出现两个 5 是同一件事。

接着：

```cpp
a[2] = a[3];
a[3] = a[4];
```

逐帧表：

| 动作 | `a[0]` | `a[1]` | `a[2]` | `a[3]` | `a[4]` |
|---|---:|---:|---:|---:|---:|
| 初始 | 2 | 7 | 4 | 9 | 5 |
| 4 左移 | 2 | 4 | 4 | 9 | 5 |
| 9 左移 | 2 | 4 | 9 | 9 | 5 |
| 5 左移 | 2 | 4 | 9 | 5 | 5 |

最后两格都保存 5，但只有前四格仍然有效。执行：

```cpp
n--;
```

之后有效下标是 `0` 到 `3`。`a[4]` 中的旧内容可以保留，也可以为了演示清成某个值；它不再属于逻辑数组。

---

## 三、为什么删除通常从左向右移动

左移时，每次需要把右邻居读出来写到左边：

```text
旧 a[i+1] → 新 a[i]
```

如果从删除位置开始向右走，写入的目标在源的左边，不会破坏下一步要读取的右侧数据。

错误地从右向左处理会发生什么？以 `[2,7,4,9,5]` 删除位置 1 为例：

```cpp
a[3] = a[4];
a[2] = a[3];
a[1] = a[2];
```

第一句后 `a[3]` 已变成 5；第二句读取的就是这个新 5，而不是旧 9。结果一路复制成：

```text
2  5  5  5  5
```

错误状态表：

| 动作 | `a[1]` | `a[2]` | `a[3]` | `a[4]` | 丢失值 |
|---|---:|---:|---:|---:|---|
| 初始 | 7 | 4 | 9 | 5 | 无 |
| `a[3]=a[4]` | 7 | 4 | 5 | 5 | 旧 9 |
| `a[2]=a[3]` | 7 | 5 | 5 | 5 | 4 将被连带破坏 |

所以：

> 向左移动一段重叠数据时，从左向右复制。

插入右移与删除左移看似方向相反，背后却是同一原则：先读取那些即将被覆盖、以后仍然需要的旧值。

---

## 四、从动作得到通用代码

已知：

- 当前有效长度为 `n`；
- 删除位置为 `position`；
- 合法位置满足 `0 <= position < n`。

需要填补的位置从 `position` 到 `n-2`。每格读取右邻居：

```cpp
for (int i = position; i < n - 1; i++) {
    a[i] = a[i + 1];
}
n--;
```

为什么条件是 `i < n - 1`？最后一次合法复制是：

```cpp
a[n - 2] = a[n - 1];
```

当 `i == n - 1` 时，再读 `a[i+1]` 就会访问 `a[n]`，已经越过旧数组的有效末尾。

循环中使用的是旧长度 `n`。搬完以后再 `n--`，边界最容易解释。

---

## 五、删除末尾元素为什么不用搬

若 `position == n - 1`，被删除的正是最后一个有效元素。循环条件一开始为假：

```text
position < n - 1
n - 1 < n - 1  → 假
```

无需覆盖任何格子，只执行 `n--`。最后一格仍可能保留原值，但它已经落在新的有效范围之外。

这说明“删除”主要由逻辑长度定义。清空最后一格不是删除成立的必要条件；不再把它算入有效序列才是。

---

## 六、删除开头和只剩一个元素

### 删除开头

`position = 0` 时，后面所有元素都要左移：

```text
旧 a[1] → a[0]
旧 a[2] → a[1]
...
```

循环仍然适用。

### 只剩一个元素

若 `n = 1` 且删除位置为 0，移动循环执行零次，随后 `n` 变成 0。此时数组没有有效元素，不能再访问 `a[0]` 并称它为当前记录。

### 空数组

若 `n = 0`，不存在合法删除位置。必须先拒绝操作：

```cpp
if (position < 0 || position >= n) {
    cout << "删除位置无效\n";
}
```

机器不会把“删掉不存在的东西”理解成人类式的淡定忽略，除非程序明确规定。

---

## 七、插入和删除放在一起看

| 操作 | 要保护的数据 | 移动方向 | 复制顺序 | 长度变化 |
|---|---|---|---|---:|
| 插入 | 插入点及其右侧旧元素 | 向右 | 从右向左 | `n++` |
| 删除 | 删除点右侧旧元素 | 向左 | 从左向右 | `n--` |

插入先腾空位，再写新值；删除先让后续数据补位，再缩短有效范围。

两个过程都没有让物理格子增加或减少。变化的是格子内容和“前多少格有效”这项状态。

---

## 八、错误博物馆

### 展品一：用零代替删除

```cpp
a[position] = 0;
```

零可能本来就是合法数据，而且后面的元素没有补位，长度也没变化。

### 展品二：从右向左左移

先覆盖了左边步骤以后还要读取的源值，最后一个值可能向左复制整段。

### 展品三：忘记 `n--`

展示内容看似已移动，但程序仍认为旧的最后一格有效，于是末尾出现重复记录。

### 展品四：循环执行到 `i < n`

最后一轮会读取 `a[n]`。正确上限是 `i < n - 1`。

### 展品五：先 `n--` 却继续沿用旧边界想法

先缩长度并非绝对不能写，但会让“当前 `n` 表示旧长度还是新长度”变得混乱。入门阶段应先搬运、后减长度，让每一步含义稳定。

---

## 九、翻译训练

1. 从 `[3,8,2,6]` 删除位置 1，补全逐帧状态表。
2. 删除位置 0 时，按顺序列出复制语句。
3. 删除最后一个元素时，为什么循环执行零次？
4. 解释删除左移为何从左向右，而插入右移为何从右向左。
5. 找出下面代码的越界位置：

```cpp
for (int i = position; i < n; i++) {
    a[i] = a[i + 1];
}
```

6. 给 `n--` 配音，不要说“数组少了一个物理格子”。

参考要点：第一题经历 `[3,2,2,6]`、`[3,2,6,6]`，随后有效长度变为 3；第五题在 `i=n-1` 时读取 `a[n]`。

---

仓库主管看着有效记录 `[2,4,9,5]`，问最后一格为什么还躺着一个 5。托马斯回答：“那格还在，但已经不属于当前记录。”

R-01 点亮新的长度牌 `n=4`。

删除不是让格子凭空消失。它是让后面的状态向左接班，并重新划定哪里才是有效世界。
