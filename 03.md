# 第三章　一行代码不是一句数学，它是一段时间

皇家自动化工坊第三天的课程，在一声闷响中开始。

自动烤炉原本只需三个动作：装入煤炭、放入生牛肉、启动开关。托马斯确认三件事都写进了程序，便让 R-01 执行。

机器人先放牛肉，接着启动烤炉，最后才装煤。启动时燃料槽还是空的，补料活塞立刻进入紧急模式，把一块煤从烟囱反向弹了出去。煤穿过半面玻璃墙，准确落进村民的汤锅。

“我写的三件事一件不少。”托马斯说。

R-01 指向墙上的洞：“顺序造成的差异已获得实体展示。”

Notch 检查程序。三条命令确实都在，只是排列成了：

```cpp
foodLoaded = 1;
furnaceOn = 1;
fuelLoaded = 1;
```

启动那一行执行时，`fuelLoaded` 仍是 0。最后补上煤，无法改变几秒前已经发生的启动事故。

“数学可以把几条关系写在同一张纸上，”Notch 说，“程序却住在时间里。第一行做完，第二行面对的世界已经不是原来那个世界。”

---

## 3.1 程序为什么必须按顺序执行

数学常把几个关系并排写出：

\[
x=3,\qquad y=x+2,\qquad z=4y
\]

人可以整体阅读，再得到 $y=5、z=20$。纸面关系没有要求读者真的看见一个格子先出现、另一个格子后出现。

程序执行下面三行时，却有明确时间线：

```cpp
int x = 3;
int y = x + 2;
int z = y * 4;
```

第一行完成前，`x` 还没准备好；第二行完成前，`y` 也不存在。计算机只能沿着代码从前往后走。

| 时刻 | 刚执行的语句 | `x` | `y` | `z` |
|---:|---|---:|---:|---:|
| 0 | 尚未开始 | 未定义 | 未定义 | 未定义 |
| 1 | `int x = 3;` | 3 | 未定义 | 未定义 |
| 2 | `int y = x + 2;` | 3 | 5 | 未定义 |
| 3 | `int z = y * 4;` | 3 | 5 | 20 |

程序中的“时间”不一定指几秒或几毫秒。这里关心的是执行次序：第 0 步、第 1 步、第 2 步。哪怕两行只相隔极短时间，后面那行仍然会读到前面已经造成的新状态。

烤炉程序也是如此。`furnaceOn = 1;` 不会先阅读后文，发现煤将在下一行到达，然后礼貌地等一会儿。执行来到它时，燃料槽是什么状态，它就按什么状态启动。

---

## 3.2 第一行执行后，世界已经改变

托马斯把烤炉状态重新设为：

```text
fuelLoaded = 0
foodLoaded = 0
furnaceOn = 0
```

正确目标是先让燃料和食物到位，再启动。写代码前，他们先回答：

- 现在已知：三个状态都是 0；
- 需要改变：燃料、食物和开关状态；
- 哪些条件必须先满足：启动前，前两个状态应为 1；
- 旧值以后是否需要：这里不需要保留三个 0；
- 停止条件：完成三次写入。

于是安全顺序是：

```cpp
fuelLoaded = 1;
foodLoaded = 1;
furnaceOn = 1;
```

它不是三句同时成立的说明，而是三次连续写入：

| 步骤 | `fuelLoaded` | `foodLoaded` | `furnaceOn` | 烤炉当前状态 |
|---:|---:|---:|---:|---|
| 初始 | 0 | 0 | 0 | 空炉，关闭 |
| 装煤后 | 1 | 0 | 0 | 有燃料，仍关闭 |
| 放牛肉后 | 1 | 1 | 0 | 材料齐备，仍关闭 |
| 启动后 | 1 | 1 | 1 | 正常工作 |

第一行执行后，`fuelLoaded` 已经从 0 变成 1。第二行不是在初始状态上单独执行，而是在“煤已经装好”的世界里继续。第三行再接手“煤和牛肉都已到位”的世界。

可以把每条语句想成一次交接：

\[
\text{旧状态}
\rightarrow
\text{执行一条语句}
\rightarrow
\text{新状态}
\]

新状态随即成为下一条语句的旧状态。程序就这样向前走，没有一行能假装前面的变化尚未发生。

---

## 3.3 后一行看到的是旧值还是新值

R-01 给托马斯看了一个更安静的例子：

```cpp
int x = 2;
int y = x + 3;
x = 10;
```

托马斯下意识说：“`y` 最后是 13。”

“第二行执行时，`x` 还没有变成 10。”

逐帧看：

| 执行后 | `x` | `y` | 第二行看见的 `x` |
|---|---:|---:|---:|
| `int x = 2;` | 2 | 未定义 | 尚未读取 |
| `int y = x + 3;` | 2 | 5 | 2 |
| `x = 10;` | 10 | 5 | 仍是当时的 2 |

`y` 保存的是第二行执行那一刻算出的 5。后来 `x` 改成 10，不会追进历史里把 `y` 改成 13。

判断后一行读到旧值还是新值，要看更新发生在哪个时间点：

```cpp
int a = 3;
int b = 5;
a = a + b;
b = a + 1;
```

第三行执行后 `a=8`。第四行读取的是新 `a=8`，所以 `b=9`。

| 步骤 | `a` | `b` | 本步读取的值 |
|---:|---:|---:|---|
| 初始化完成 | 3 | 5 | 无 |
| `a = a + b;` 后 | 8 | 5 | 旧 `a=3`、`b=5` |
| `b = a + 1;` 后 | 8 | 9 | 新 `a=8` |

“旧”和“新”不是变量永久自带的标签。对第三行来说，3 是旧 `a`；到了第四行，8 已经是当前 `a`。读代码时必须跟着执行位置移动，而不是一直抱着开头的值不放。

---

## 3.4 三行代码的逐帧动画

Notch 写下三行更新：

```cpp
int x = 2;
x = x + 3;
x = x * 4;
```

数学上的最终结果可以直接写成：

\[
(2+3)\times4=20
\]

程序却要经历两个中间状态：

```text
未定义 → 2 → 5 → 20
```

把每一行配上动作：

1. 准备 `x` 格，写入 2；
2. 读取当前的 2，加 3，把 5 写回；
3. 读取当前的 5，乘 4，把 20 写回。

| 帧 | 执行语句 | `x` 执行前 | 右边结果 | `x` 执行后 |
|---:|---|---:|---:|---:|
| 1 | `int x = 2;` | 未定义 | 2 | 2 |
| 2 | `x = x + 3;` | 2 | 5 | 5 |
| 3 | `x = x * 4;` | 5 | 20 | 20 |

### 一行内部也有时间

第三行看起来只有一句，内部仍要先读后写：

```cpp
x = x * 4;
```

它先读取右边的旧 `x=5`，算出 20，最后才用 20 覆盖左边。整条右边计算完成前，旧值仍可使用。

跨过分号后，情况就不同了。上一句写回的新值立即生效，下一句只能读取更新后的状态。可以记住这个边界：

> 一条赋值内部，先完整计算右边；多条赋值之间，前一句的写入会立刻影响后一句。

状态表就是把这部动画暂停。代码越长，越不该只靠心算硬撑。皇家工坊把这种行为称为“主动制造第二起事故”。

---

## 3.5 同样的语句，换个顺序为什么结果不同

托马斯把刚才的两次更新交换位置：

```cpp
int x = 2;
x = x * 4;
x = x + 3;
```

使用的仍然是“加 3”和“乘 4”，结果却从 20 变成 11。

| 顺序 | 轨迹 | 最终值 |
|---|---|---:|
| 先加 3，再乘 4 | `2 → 5 → 20` | 20 |
| 先乘 4，再加 3 | `2 → 8 → 11` | 11 |

原因不在 C++ 脾气古怪。两种数学关系本来就不同：

\[
(2+3)\times4=20
\]

\[
2\times4+3=11
\]

代码的行序把括号展开成了时间。第一段中，乘法拿到加法后的 5；第二段中，加法拿到乘法后的 8。

并非所有语句交换顺序都会改变结果。若两句修改互不相关的格子：

```cpp
x = 3;
y = 7;
```

调换后最终状态仍可能一样。判断能不能换，不能靠“看起来差不多”，而要检查依赖：后一动作是否需要前一动作产生的值？某个旧值会不会被提前覆盖？某个条件是否必须先满足？

烤炉的启动依赖燃料和食物已经到位，所以启动命令不能向前乱搬。两次互不相关的标签打印则可能交换。顺序问题说到底是数据和条件的欠账问题。

---

## 3.6 红石脉冲不会同时到达所有机关

Notch 带托马斯来到红石实验区。三个红石灯排成一线，信号从左边输入。拉下拉杆以后，第一盏先亮，脉冲继续传播，第二盏再亮，最后轮到第三盏。

从远处看，三盏灯几乎同时亮起。若把速度放慢，每一盏仍然接收前一段线路传来的状态。

代码也一样。三行挨得再近，执行仍有先后：

```cpp
x = 3;
y = x + 2;
z = y * 4;
```

第二行依赖第一行准备好 `x`，第三行依赖第二行准备好 `y`。若把顺序反过来：

```cpp
z = y * 4;
y = x + 2;
x = 3;
```

执行第一行时，`y` 还没有可用值。未来那行代码不能提前给过去供货。

这条规律叫“先准备，后使用”也好，叫因果顺序也好，内存画面都相同：读取发生时，目标格里必须已经有正确的当前值。

红石比人诚实的一点是，它从不说“我以为后面会补上”。线路没接通，信号就过不去；变量没准备好，代码也不能靠上下文猜出未来。

---

## 错误博物馆

### 展品一：未来的值提前使用

```cpp
int y = x + 1;
int x = 5;
```

数学读者可能先看完两行，再推得 `y=6`。C++ 编译器按顺序处理。第一行使用 `x` 时，它尚未在当前作用域中声明，因此代码不能正常编译。

安全顺序是：

```cpp
int x = 5;
int y = x + 1;
```

### 展品二：需要旧值，却修改得太早

任务是先计算原宽度和高度的面积，再把宽度重置为 10。旧宽度为 4，高度为 3。

错误顺序：

```cpp
width = 10;
area = width * height;
```

它得到 30，因为第二行只能看见新宽度 10。正确顺序要先偿还“面积还需要旧宽度”这笔债：

```cpp
area = width * height;
width = 10;
```

| 顺序 | `width` | `height` | `area` |
|---|---:|---:|---:|
| 初始 | 4 | 3 | 未定义 |
| 错误：先改宽度 | 10 | 3 | 未定义 |
| 错误：再算面积 | 10 | 3 | 30 |
| 正确：先算面积 | 4 | 3 | 12 |
| 正确：再改宽度 | 10 | 3 | 12 |

### 展品三：把几行代码当成同时发生

初始 `x=3、y=5`，有人想让两者交换：

```cpp
x = y;
y = x;
```

第一行之后，旧 `x=3` 已经消失；第二行读到的是新 `x=5`。最终两个格子都是 5。

本章先看清事故时间线：

```text
(3, 5) → (5, 5) → (5, 5)
```

如何保护那个仍有用途的旧 3，是下一章的任务。

---

## 翻译训练

### 训练一：补状态表

```cpp
int x = 3;
int y = x + 2;
x = 9;
int z = x + y;
```

| 步骤 | `x` | `y` | `z` |
|---:|---:|---:|---:|
| 第一行后 | 3 | 未定义 | 未定义 |
| 第二行后 | 3 | 5 | 未定义 |
| 第三行后 | 9 | 5 | 未定义 |
| 第四行后 | 9 | 5 | 14 |

说明：`y` 留着旧 `x=3` 参与计算时得到的结果 5，`z` 则读取后来已经变成 9 的 `x`。

### 训练二：比较两种顺序

```cpp
x = x - 2;
x = x * 3;
```

与：

```cpp
x = x * 3;
x = x - 2;
```

初始 `x=6`。第一段轨迹是 `6 → 4 → 12`，第二段是 `6 → 18 → 16`。请把两段分别还原成带括号的数学关系。

### 训练三：找出第一处偏离

目标轨迹是 `2 → 5 → 10`，实际代码却得到 `2 → 4 → 8`：

```cpp
int x = 2;
x = x + 2;
x = x * 2;
```

第一处偏离发生在第二行。它应把 2 加 3，而不是等到最后再怪乘法。

### 训练四：排列烤炉命令

已知启动前必须满足 `fuelLoaded=1` 和 `foodLoaded=1`。把三句排成安全顺序：

```cpp
furnaceOn = 1;
fuelLoaded = 1;
foodLoaded = 1;
```

答案是先完成两项装载，最后启动。煤和牛肉谁先装在这个简化模型中没有依赖，因此前两句可以互换；启动不能提前。

自动烤炉重新运行。煤进入燃料槽，牛肉进入烤制格，开关最后亮起。几秒后，熟牛肉落进成品箱，墙上也没有新增通风口。

托马斯在黑板上写下：

> 后面的代码读取前面已经改变过的世界。顺序不是排版，它决定每一步能看到什么。

R-01 在下面补充：“同样的动作放错时间，通常仍会执行。事故因此格外稳定。”

工坊门口，两名村民正为贴反的领取牌争吵。托马斯需要让两个变量互换，可他已经知道普通赋值只能一行一行发生。怎样在覆盖以前保住旧值，将成为下一章真正的问题。
