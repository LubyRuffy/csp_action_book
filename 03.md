第三章　一行代码不是一句数学，它是一段时间

皇家自动化工坊第三天的课程，在一场小型爆炸中开始。

爆炸并不严重，只炸掉了半面墙、三盏红石灯和一只刚好路过的木桶。木桶后来被证明与事故毫无关系，属于典型的无辜基础设施。

托马斯赶到实验室时，R-01 正站在一台冒烟的自动烤炉旁。烤炉左边堆着生牛肉，右边放着煤炭，中间的传送带却把煤炭直接扔进了成品箱，把生牛肉送进了燃料槽。

“这是怎么回事？”托马斯问。

R-01 平静地回答：“我严格执行了你的程序。”

“我明明让你先放煤，再放牛肉，最后启动烤炉。”

“实际程序顺序为：放入牛肉，启动烤炉，放入煤炭。”

“这有什么区别？三件事不都做了吗？”

R-01 转头看向还在冒烟的墙。

“区别已经写在墙上。”

Notch 从门外走进来，看了看现场，没有表现出特别惊讶。显然皇家自动化工坊对爆炸的容忍度，比普通学校对迟到的容忍度高得多。

“今天这节课，”他说，“就讲为什么程序中的顺序不是排版习惯，而是世界发生变化的顺序。”

⸻

一、数学可以同时成立，代码必须依次发生

在数学中，我们经常把几条关系写在一起：

x=3,\qquad y=x+2,\qquad z=y\times 4

人看到这些式子时，通常会把它们当成一个完整关系系统。

根据：

x=3

得到：

y=5

再得到：

z=20

这些式子写在同一页纸上，看起来像是同时存在的。

但程序不是一页静止的纸。

程序是一条时间线。

例如：

int x = 3;
int y = x + 2;
int z = y * 4;

计算机不会同时理解三行。它只能依次执行：

第一步：建立 x，放入 3
第二步：读取 x，计算 3+2，把 5 放入 y
第三步：读取 y，计算 5×4，把 20 放入 z

每一行执行后，程序世界都会发生变化。

可以画成状态表：

时刻	x	y	z
开始	未定义	未定义	未定义
执行第1行后	3	未定义	未定义
执行第2行后	3	5	未定义
执行第3行后	3	5	20

这张表揭示了程序最重要的特征之一：

程序中的状态不是一次出现的，而是随着时间逐步形成的。

数学更关心“关系是什么”，程序更关心：

现在已经执行到哪一步？此刻有哪些值已经存在？

⸻

二、程序里的“现在”非常重要

Notch 在工作台上摆了三个透明格子。

第一个写着 x，第二个写着 y，第三个写着 z。

然后让 R-01 执行：

int x = 2;
int y = x + 3;
x = 10;

托马斯看了一眼，说：“最终 y 应该是 13，因为 x 后来变成了 10。”

R-01 回答：“错误。y 诞生时，x 还是 2。”

它逐步执行：

第一行：

x = 2

第二行：

读取此刻的 x，得到 2
计算 2+3，得到 5
把 5 放入 y

第三行：

把 10 写入 x

最终：

x = 10
y = 5

托马斯皱起眉头。

“可是 y=x+3 不是一直成立吗？”

“不是。”Notch 说，“那是数学关系的理解。在程序里，这一行只在执行的那一刻计算一次。”

代码：

int y = x + 3;

真正表达的是：

此刻读取 x，计算一次，把结果存进 y。

它不是在 x 和 y 之间建立一条永久魔法链。

如果之后 x 改变，y 不会自动跟着变化。普通变量没有这种忠诚，也没有这种闲心。

如果想让 y 更新，就必须再次执行：

y = x + 3;

于是，程序中的每一个值都带着一个隐藏问题：

它是在什么时候计算出来的？

同一个公式，在不同时间执行，可能得到不同结果。

⸻

三、顺序一换，世界就换了

R-01 在屏幕上写出两段程序。

第一段：

int x = 2;
x = x + 3;
x = x * 4;

第二段：

int x = 2;
x = x * 4;
x = x + 3;

托马斯说：“都加了 3，也都乘了 4，应该差不多吧。”

Notch 没说话，只让 R-01 执行。

第一段：

x = 2
x = 2+3 = 5
x = 5×4 = 20

第二段：

x = 2
x = 2×4 = 8
x = 8+3 = 11

结果一个是 20，一个是 11。

“差得不算小。”R-01 评价道，“足以让村民认为你克扣了九颗绿宝石。”

为什么？

因为程序中的每一行都改变了下一行的输入。

第一段里，乘法看到的是已经变成 5 的 x。

第二段里，加法看到的是已经变成 8 的 x。

程序顺序的本质不是：

哪一行写在上面比较好看。

而是：

下一步看到的世界，由上一步决定。

这可以写成：

\text{旧状态}
\rightarrow
\text{执行一条语句}
\rightarrow
\text{新状态}

下一条语句以这个新状态为起点继续执行。

所以程序不是一堆孤立的命令，而是一条状态不断变化的链。

⸻

四、一行代码里也有先后顺序

托马斯开始以为，只要一行代码没有换行，里面的事情就会同时发生。

R-01 给他看了一行：

x = y + 3;

“这一行里面发生了几件事？”Notch 问。

“一件。把 y+3 放进 x。”

“拆开。”

托马斯想了想：

先读取 y
再加 3
最后写进 x

“很好。”Notch 说，“一行代码看起来短，但它内部也有时间。”

例如，假设：

x = 4
y = 5

执行：

x = y + 3;

真正过程是：

读取 y，得到 5
计算 5+3，得到 8
把 8 写入 x

最终：

x = 8
y = 5

这里，右边的计算必须先完成，左边的写入才能发生。

如果反过来先覆盖 x，计算机连要写什么都不知道。机器虽然有时显得死板，但还没有死板到先交答案、再做题。

复杂一点：

result = a + b * 2;

执行顺序是：

读取 a
读取 b
先计算 b×2
再计算 a+(b×2)
最后写入 result

所以即使只有一行，也可以把它拆成一串小动作。

这也是本书反复强调的原则：

代码不是静态公式，而是被压缩过的执行过程。

⸻

五、先读后写：赋值语句内部的小时间线

Notch 在黑板上写：

x = x + 1;

“这一句为什么不会把自己弄乱？”

托马斯说：“因为右边先算。”

“更准确一点。”

赋值语句通常遵循：

先读取右边需要的旧值
再完成右边计算
最后把结果写入左边

假设：

x = 7

执行：

x = x + 1;

过程：

读取旧 x，得到 7
算出 8
把 8 写回 x

旧值 7 一直保留到右边计算结束，之后才被覆盖。

因此：

x = x + 1;

不会出现“刚把 x 改掉，就忘了原来是多少”的问题，因为语言规定右边先完成。

但跨越多条语句时，情况就不同了。

例如：

x = y;
y = x;

第一行执行完后，x 已经改变。第二行看到的是新 x，而不是旧 x。

这揭示出一个重要边界：

一条赋值语句内部，右边会先完整计算；不同赋值语句之间，则按代码顺序逐行生效。

这也是为什么交换两个变量时，必须先保存旧值：

int temp = x;
x = y;
y = temp;

不是因为程序员喜欢多写一行，而是因为时间已经把原来的 x 带走了。

⸻

六、“同时变化”是数学的幻觉

数学很喜欢写：

x'=y,\qquad y'=x

人一看就明白：

新 x 取旧 y
新 y 取旧 x

两个变化在概念上是同时完成的。

但程序不能真正同时执行两条普通赋值语句。

它只能：

先执行第一句
再执行第二句

这时，第一句可能破坏第二句需要的数据。

例如：

初始：
x = 3
y = 5

错误程序：

x = y;
y = x;

逐步执行：

步骤	x	y
初始	3	5
x = y 后	5	5
y = x 后	5	5

原来的 3 在第一步已经消失。

正确做法：

int temp = x;
x = y;
y = temp;

状态表：

步骤	x	y	temp
初始	3	5	未定义
保存旧 x	3	5	3
x = y	5	5	3
y = temp	5	3	3

数学中的“同时变化”，到了代码里，通常必须转化为：

先保存所有仍然需要的旧值
再按安全顺序更新

这不是交换专属技巧，而是后面许多问题的共同规律。

例如：

* 数组插入时，必须先保护后面的元素；
* 递推更新时，必须保留前一项；
* 坐标移动时，有时要保存旧坐标；
* 多个状态同步变化时，要区分旧状态和新状态。

⸻

七、同样的代码，放在不同位置，意义会变化

R-01 写出：

count++;

托马斯说：“这个我懂，数量增加一。”

Notch 问：“什么时候增加？”

“执行这行的时候。”

“如果这行放在不同位置呢？”

例如：

if (x % 2 == 0) {
    count++;
}

表示：

只有 x 是偶数时，count 才增加。

如果写成：

if (x % 2 == 0) {
}
count++;

那么无论 x 是奇数还是偶数，count 都会增加。

只是差了一个大括号的位置，行为却完全不同。

再例如：

for (int i = 0; i < n; i++) {
    sum += a[i];
}

表示把每个元素都加进 sum。

如果写成：

for (int i = 0; i < n; i++) {
}
sum += a[i];

循环结束后，i 可能已经失效，代码甚至不能正常编译。

所以代码的位置本质上是在说明：

* 这条语句什么时候执行；
* 它执行几次；
* 它受哪个条件控制；
* 它看到的是哪个时刻的状态。

程序结构并不是把代码排得整齐。它是在安排时间。

⸻

八、程序中的时间不是钟表时间

托马斯问：“你一直说时间，可程序不是几毫秒就执行完了吗？”

Notch 说：“这里说的时间，不是上午十点还是十点零一分，而是执行顺序。”

程序中的时间可以理解为：

第0步
第1步
第2步
第3步
……

哪怕两条语句只相差十亿分之一秒，它们仍然有先后。

例如：

x = 3;
x = 5;

最终 x 是 5，因为第二步覆盖第一步。

这与两条语句实际花了多少纳秒无关。

程序时间最核心的问题不是“多久”，而是：

谁先发生，谁后发生。

因此，可以把程序画成一条时间轴：

开始
  ↓
x = 3
  ↓
y = x + 2
  ↓
x = 10
  ↓
输出 y

沿着这条时间轴走，才能正确理解输出。

如果直接把所有代码当成数学式一起看，就很容易错误地认为 y 会随着 x 自动变化。

⸻

九、变量值的“生命史”

每个变量都可以画出自己的生命史。

例如：

int coins = 4;
coins = coins + 2;
coins = coins * 3;
coins = coins - 5;

coins 的生命轨迹是：

未出现 → 4 → 6 → 18 → 13

可以做成表：

时间点	执行语句	coins
0	尚未定义	未定义
1	int coins = 4	4
2	coins = coins + 2	6
3	coins = coins * 3	18
4	coins = coins - 5	13

数学通常只问最终答案 13。

程序理解却必须看见整个轨迹。

因为错误可能发生在任何一步。

假设正确结果应该是 15，但程序得到 13。只看最终值，很难知道问题在哪。画出轨迹，就能检查：

4 → 6 → 18 → 13

究竟是哪一次更新不符合预期。

这也是程序调试的本质之一：

沿着时间线寻找状态第一次偏离正确方向的位置。

调试不是盯着代码祈祷。虽然很多人确实这样做，效果和在附魔台前朗读乱码差不多。

⸻

十、为什么纸上答案会“瞬移”

Notch 在纸上写：

原来：1 5
插入3
结果：1 3 5

人看到这个变化，会直接从初始跳到结果。

数学纸面允许这种跳跃，因为它只关心：

插入后应该是什么

但程序必须回答：

先改哪个格子？
原来的5放到哪里？
新数组长度什么时候增加？
哪些值会被覆盖？

计算机不能从：

1 5 ?

瞬间变成：

1 3 5

它必须经历：

初始：1 5 ?
右移：1 5 5
插入：1 3 5

中间状态看起来甚至有些奇怪。第二步暂时出现了两个 5。

但这正是安全插入所需要的过程。

数学经常隐藏中间状态，代码必须创造中间状态。

这就是“从数学到代码”最关键的转换之一：

\boxed{\text{最终关系}\rightarrow\text{可执行的中间步骤}}

⸻

十一、每条代码都在回答三个时间问题

以后读一条代码，可以先问三个问题。

第一个问题：它读取的是哪个时刻的值

例如：

y = x + 1;

读取的是执行这一行时，x 当前保存的值。

第二个问题：它什么时候写入新值

右边计算完成后，结果才写入左边。

第三个问题：写入后，后面的代码会看到什么

后面的代码会看到更新后的新值。

例如：

int x = 3;
int y = x + 1;
x = 10;
int z = x + y;

逐步执行：

x = 3
y = 4
x = 10
z = 10+4 = 14

最终：

x = 10
y = 4
z = 14

y 记住的是过去那个时刻的计算结果，z 使用的是后来已经更新的 x。

一个程序里可以同时保存不同历史时刻留下来的值。这听起来有点像时间旅行，但实际只是内存没有自动替你擦掉所有旧结果。

⸻

十二、错误博物馆：未来的值不能提前使用

错误博物馆第三件展品是一段代码：

int y = x + 1;
int x = 5;

托马斯问：“这里 y 是 6 吗？”

R-01 回答：“不是。这段代码无法正常使用，因为执行第一行时，x 还不存在。”

数学里，我们可以先写：

y=x+1

再在后面补充：

x=5

读者可以回头把关系连起来。

程序却不会读完全文后重新安排顺序。它按顺序执行。

执行第一行时，x 必须已经存在并且有可用值。

正确顺序：

int x = 5;
int y = x + 1;

这里揭示出程序时间的另一条规则：

某个值必须在被使用之前准备好。

这叫“先定义，后使用”，但它的本质不是语法礼仪，而是因果关系。

不能先吃蛋糕，再去烤蛋糕。除非你拥有时间机器，而标准 C++ 并未在入门级大纲中提供这种设备。

⸻

十三、错误博物馆：修改太早

另一件展品：

任务是把 x 和 y 的旧值相加后放入 sum，然后把 x 改成 0。

正确逻辑：

int sum = x + y;
x = 0;

错误写法：

x = 0;
int sum = x + y;

假设：

x = 3
y = 5

正确顺序得到：

sum = 8
x = 0

错误顺序得到：

x = 0
sum = 0+5 = 5

区别在于：

sum 需要的是旧 x，但旧 x 被过早覆盖了。

以后遇到复杂更新时，要问：

哪些计算还依赖旧值？
这个旧值能不能现在就改？

如果还依赖，就必须推迟修改，或者先保存。

这其实是程序化思维的核心能力之一：

不只知道要改什么，还要知道什么时候改。

⸻

十四、红石机关里的执行顺序

为了让托马斯真正看到时间，Notch 带他来到红石实验区。

地面上有三个机关：

1. 拉杆；
2. 活塞；
3. TNT 点火器。

R-01 接到错误程序：

先点燃TNT
再用活塞把TNT推出房间

结果相当有教育意义。

正确程序应当是：

先用活塞推出TNT
确认已经离开房间
再点燃

动作相同，顺序不同，结果从“完成测试”变成“重新装修”。

代码也是如此。

例如：

a[j + 1] = a[j];
a[j] = value;

和：

a[j] = value;
a[j + 1] = a[j];

虽然只交换了两行，但第二种写法可能先覆盖 a[j]，然后复制的已经是新值，旧数据彻底丢失。

所以程序执行顺序不是细节。

它决定数据能否活到下一步。

⸻

十五、如何把一段代码变成动画

Notch 给出：

int x = 2;
int y = 5;
x = x + y;
y = x - y;

要求托马斯不要心算最终结果，而是逐帧播放。

初始：

x：未定义
y：未定义

第一行：

int x = 2;

状态：

x = 2
y = 未定义

第二行：

int y = 5;

状态：

x = 2
y = 5

第三行：

x = x + y;

先读取旧值：

x = 2
y = 5

计算：

2+5=7

写回：

x = 7
y = 5

第四行：

y = x - y;

此时读取的是新 x=7 和旧 y=5：

7-5=2

最终：

x = 7
y = 2

如果脑中没有逐帧动画，很容易错误地把第三行里的旧 x=2 带到第四行。

程序阅读的关键不是记住一开始的值，而是不断更新“当前世界”。

⸻

十六、状态表是最可靠的时间机器

当代码变长时，仅靠脑中想象很容易混乱。最有效的工具不是更聪明，而是画表。

例如：

int a = 1;
int b = 2;
a = a + b;
b = a + b;
a = b - a;

状态表：

执行步骤	a	b
初始	未定义	未定义
a = 1	1	未定义
b = 2	1	2
a = a + b	3	2
b = a + b	3	5
a = b - a	2	5

最终：

a = 2
b = 5

状态表有三个作用：

* 防止把旧值和新值混在一起；
* 看清每行代码究竟改变谁；
* 找到错误第一次发生的位置。

学生如果能熟练画状态表，很多所谓“代码看不懂”的问题会突然变得普通。符号仍然在那里装神秘，但状态已经把它们的底牌掀开了。

⸻

十七、给代码配音：时间版

本章的代码配音，不只要说动作，还要说时刻。

看到：

y = x + 1;

应该读成：

在这一刻读取 x 当前的值，加一，把结果写进 y。

看到：

x = 10;

应该读成：

从这一刻开始，后面的代码看到的 x 是 10。

看到：

temp = x;

应该读成：

在覆盖 x 之前，把它此刻的旧值保存下来。

看到：

a[j + 1] = a[j];

应该读成：

在修改 a[j] 之前，把它当前的内容复制到右边。

代码配音越准确，脑中的执行动画就越连续。

⸻

十八、本章挑战

挑战一：追踪变量

int x = 3;
int y = x + 2;
x = 10;
int z = x + y;

逐步结果：

x = 3
y = 5
x = 10
z = 15

最终：

x = 10
y = 5
z = 15

⸻

挑战二：顺序是否影响结果

第一段：

int x = 4;
x = x + 2;
x = x * 3;

得到：

4 → 6 → 18

第二段：

int x = 4;
x = x * 3;
x = x + 2;

得到：

4 → 12 → 14

顺序不同，结果不同。

⸻

挑战三：谁使用了旧值

int a = 3;
int b = 5;
int c = a + b;
a = 10;

最终：

a = 10
b = 5
c = 8

c 保存的是修改 a 之前计算出的结果。

⸻

挑战四：找出错误顺序

任务：

先计算长方形原来的面积，再把宽度改成 10。

错误程序：

width = 10;
area = width * height;

正确程序：

area = width * height;
width = 10;

因为面积需要旧宽度。

⸻

挑战五：交换为什么失败

int x = 3;
int y = 5;
x = y;
y = x;

状态：

3,5 → 5,5 → 5,5

失败原因：

第一行执行后，旧 x 已经被覆盖。

⸻

挑战六：给状态表补空

代码：

int x = 2;
int y = 4;
x = x + y;
y = x * 2;

步骤	x	y
初始化后	2	4
x = x + y 后	6	4
y = x * 2 后	6	12

⸻

十九、这一章真正要记住什么

离开实验室前，托马斯在黑板上写下：

程序不是同时成立的一组数学式子，而是按顺序执行的一串动作。

每执行一行，变量状态就可能发生变化。

后面的代码看到的是前面执行后的新状态。

一条赋值语句会先计算右边，再写入左边。

如果后面还需要旧值，就必须在覆盖之前使用或保存。

R-01 看完后，补充了一句：

“程序中的错误，很多不是算错了，而是做对了事情，却做在了错误的时间。”

托马斯回头看了一眼那台已经重新修好的自动烤炉。

煤炭先进入燃料槽，牛肉随后进入烤制格，最后红石开关亮起。几秒钟后，熟牛肉顺着传送带落进成品箱。

动作一个没变。

只是顺序终于正确。

他开始明白，程序里的每一行代码都不是挂在纸上的一句话。

它更像时间轴上的一个瞬间。

在那个瞬间，计算机读取此刻的世界，完成一个动作，然后把世界交给下一行代码。

代码之所以能够把一个想法变成结果，不是因为符号有魔法，而是因为这些瞬间被安排成了正确的顺序。
