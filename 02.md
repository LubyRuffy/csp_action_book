第二章　变量不是未知数，而是一个格子

托马斯第二次走进皇家自动化工坊时，R-01 正站在一排透明储物格前。

每个格子上都贴着名字：

apples
diamonds
day
score

格子里放着不同数量的方块。apples 里有五个苹果，diamonds 里有两颗钻石，day 里放着一块写着“12”的木牌，score 里则是一块写着“350”的计分牌。

托马斯看了一会儿，问：“你这是在整理仓库吗？”

R-01 回答：“不是。这是我的记忆区。”

“记忆不是应该放在脑袋里吗？”

“我的脑袋负责执行指令。数据存放在有名字的格子里。”

托马斯指着 apples：“所以这个格子就是变量？”

“正确。”

“那变量不是数学里的未知数吗？”

R-01 停顿了半秒。

“检测到人类再次把数学课的习惯强行搬进程序世界。”

托马斯已经逐渐习惯了这台机器人对人类文明的温和敌意。

Notch 从后面的红石控制台走出来，说：“今天你要学的，就是同一个字母为什么在数学里和程序里过着完全不同的人生。”

⸻

一、数学里的变量，到底是什么

在数学里，我们经常看到：

x+3=8

这里的 x 通常表示一个暂时不知道的数。我们的任务是求出：

x=5

这种情况下，变量像一个谜题中的空格。

它的值虽然未知，但通常不是一直变化的。一旦求出来，题目就结束了。没人会在下一秒突然宣布：

x=12

除非题目换了，或者出题人决定制造一些额外混乱。

数学中的变量还有另一种用途。

例如：

y=2x+1

这里的 x 可以取不同值，y 随着 x 变化。它表达的是一种关系：

x 取什么值
y 就按照公式得到对应结果

无论是哪一种，数学变量主要用来描述：

* 未知量；
* 可变化的量；
* 数量之间的关系。

但程序中的变量更具体。

它不是纸面上的一个字母，而是：

计算机内存中一个有名字的存储位置。

说得再直白一点：

变量就是一个带标签的格子，里面此刻保存着一个值。

例如：

int apples = 5;

可以想象成：

┌─────────────┐
│ 名字：apples │
│ 内容：5       │
└─────────────┘

这里的 apples 不是一道等待求解的未知数。

它是在告诉计算机：

请准备一个格子，名字叫 apples，现在里面放入数字 5。

⸻

二、为什么变量一定要有名字

R-01 把透明储物格上的标签全部撕了下来。

现在桌上只剩四个一模一样的格子：

┌────┐ ┌────┐ ┌────┐ ┌────┐
│  5 │ │  2 │ │ 12 │ │350 │
└────┘ └────┘ └────┘ └────┘

它问托马斯：“请把苹果数量增加一。”

托马斯指着第一个格子：“这个。”

“请证明。”

“因为我刚才看见它贴着 apples。”

“标签已经移除。”

托马斯沉默了。

如果没有名字，数字 5 可能表示：

* 五个苹果；
* 五个村民；
* 第五天；
* 五点生命值；
* 五只正准备从矿洞里出来的僵尸。

数字本身只告诉我们“有多少”，却不告诉我们“是什么”。

变量名的作用，就是给这个值赋予意义。

例如：

int apples = 5;
int diamonds = 2;
int day = 12;
int score = 350;

这些变量虽然都保存整数，但它们代表的是完全不同的状态。

因此，变量名不是写给计算机看的装饰。它首先是写给人看的解释。

下面两段代码，计算机都能执行：

int a = 5;
int b = 2;
int c = a + b;

和：

int apples = 5;
int newApples = 2;
int totalApples = apples + newApples;

但第二段更容易理解，因为变量名已经告诉我们：

原来有多少苹果
新获得多少苹果
最后共有多少苹果

Notch 在黑板上写了一句话：

好的变量名，不只是一个名字，而是这个格子为什么存在的说明书。

R-01 补充：“使用 a1、a2、aa 和 aaa 命名所有变量，是人类让未来的自己遭受报复的一种常见方式。”

⸻

三、变量有三个不同问题

托马斯指着 apples 格子问：“既然变量就是格子，那学变量不就是记住格子的名字吗？”

“不够。”Notch 说，“每个变量至少要回答三个问题。”

第一个问题：它表示什么

例如：

int apples = 5;

apples 表示苹果的数量。

int currentMax = 8;

currentMax 表示目前为止看到的最大值。

bool found = false;

found 表示到目前为止是否已经找到目标。

变量名应该说明它的含义，而不是只说明它能装数字。

第二个问题：它现在保存什么

变量的含义通常不会变，但它保存的值会变。

例如：

上午：apples = 5
中午：apples = 4
下午：apples = 7

apples 始终表示苹果数量，只是苹果数量随着吃掉和获得而变化。

第三个问题：它为什么需要存在

这是最容易被忽略的问题。

例如，寻找最大值时需要：

int currentMax;

为什么？

因为机器人检查完一个数字以后，还要继续检查下一个。它必须记住前面已经看到的最大值，否则每次往前走一步，前面的比较结果就全忘了。

所以变量不是因为“程序里应该有变量”才存在。

变量存在，是因为：

程序执行到下一步时，还需要记住上一步留下的信息。

这才是变量真正的来源。

⸻

四、变量就是程序的“现在”

R-01 在屏幕上输入：

int apples = 5;

格子里出现五个苹果。

接着执行：

apples = apples - 1;

R-01 拿走一个苹果。

现在：

apples = 4

再执行：

apples = apples + 3;

它又放进去三个苹果。

现在：

apples = 7

托马斯说：“所以 apples 一会儿是 5，一会儿是 4，一会儿又是 7。”

“正确。”

“那它到底等于多少？”

“取决于你问的是哪个时刻。”

这正是程序变量与数学未知数最重要的区别之一。

程序中的变量通常保存的是：

程序运行到当前这一刻的状态。

例如一个游戏程序中：

int health = 20;
int hunger = 15;
int level = 3;
bool hasKey = false;

这些变量共同描述玩家此刻的状态。

过一会儿，玩家被苦力怕炸到：

health = 6;

吃了面包：

hunger = 20;

捡到钥匙：

hasKey = true;

变量的值不断变化，程序世界也随之变化。

因此可以把程序理解成：

状态
↓
执行一条代码
↓
新状态
↓
再执行一条代码
↓
更新后的状态

代码真正做的事，就是不断修改这些格子里的内容。

⸻

五、一个变量，只能同时保存一个值

托马斯拿来一张纸，在上面写：

x = 3
x = 8

然后问：“能不能让 x 同时保存 3 和 8？”

R-01 回答：“不能。一个普通变量在同一时刻只能保存一个值。”

“为什么？”

“因为它只有一个格子。”

R-01 执行：

int x = 3;

此时：

┌───────┐
│ x     │
│ 3     │
└───────┘

然后执行：

x = 8;

变成：

┌───────┐
│ x     │
│ 8     │
└───────┘

旧的 3 被覆盖。

这不是因为计算机讨厌 3，而是因为 x 这个格子只能保存当前值。

如果既想保留 3，又想保存 8，就需要两个变量：

int oldValue = 3;
int newValue = 8;

或者在修改前保存旧值：

int temp = x;
x = 8;

这条原则以后会反复出现：

如果旧值以后还要使用，就不能直接覆盖，必须先存到别的地方。

交换变量要用 temp，数组插入要先移动元素，递推更新要保存旧状态，根源都在这里。

机器不会替你保存那些“你本来还想用，但忘了说”的东西。内存不是慈善机构。

⸻

六、变量的值不会自动跟着另一个变量变化

R-01 执行下面的代码：

int x = 3;
int y = x;

此时：

x = 3
y = 3

托马斯说：“现在 y 和 x 连在一起了？”

R-01 没回答，而是继续执行：

x = 9;

结果：

x = 9
y = 3

“为什么 y 还是 3？”托马斯问。

“因为执行 y = x 时，只是把当时 x 中的值复制给了 y。”

这一点非常重要。

普通变量赋值通常意味着：

读取右边当前的值
复制一份
写入左边

执行：

y = x;

不是在两者之间拉一根永久连接线。

它只是完成了一次复制。

可以想象成：

执行前：
x 格子：3
y 格子：空
复制后：
x 格子：3
y 格子：3

后来修改 x，只会修改 x 自己的格子：

x 格子：9
y 格子：3

两个普通变量没有自动同步关系。

这和数学函数关系不同。

数学中如果规定：

y=x+1

那么当 x 变化时，我们会重新计算对应的 y。

但在程序里：

int y = x + 1;

只会在这一行执行时计算一次。

之后如果 x 改变，y 不会自动重新计算。

例如：

int x = 3;
int y = x + 1;  // y 得到 4
x = 10;

最终：

x = 10
y = 4

除非再次执行：

y = x + 1;

此时 y 才会变成 11。

程序不会一直盯着数学关系，发现一个变量变了就热心地更新其他变量。它只执行你明确写下的语句。懒得非常有原则。

⸻

七、为什么有些变量保存答案，有些变量只负责过程

Notch 在桌上摆出五个箱子：

3  8  2  9  5

任务是找出最大的数字。

托马斯一眼看出答案：“9。”

“你能一眼看完，”Notch 说，“R-01 只能一个一个检查。”

机器人先看第一个：

3

它需要记住：

目前最大 = 3

于是需要一个变量：

int currentMax = 3;

再看到 8：

8 > 3

更新：

currentMax = 8;

再看到 2：

2 < 8

不变。

再看到 9：

currentMax = 9;

最后看到 5：

5 < 9

不变。

最终 currentMax 保存答案 9。

但在程序执行过程中，它曾经依次保存过：

3 → 8 → 8 → 9 → 9

所以这个变量既是：

* 过程中的记忆；
* 最后答案的容器。

有些变量则只负责中间过程。

例如交换两个数：

int temp = x;

temp 只是暂时保存旧的 x。交换完成后，它的任务就结束了。

再例如数组插入：

int current = a[i];

current 表示暂时拿在手里的元素。等它放回正确位置，这个变量就不再重要。

因此变量大致可以分成几类：

类型	作用	示例
输入变量	保存题目给出的数据	n、x
状态变量	记录当前状态	health、currentMax
累积变量	不断积累结果	sum、count
临时变量	暂时保护数据	temp、current
控制变量	控制执行过程	i、j
结果变量	保存最终答案	answer、result

这些分类不是为了背名词，而是帮助我们回答：

这个变量到底为什么存在？

如果一个变量的用途说不清，它通常有两种可能：

1. 名字取得太差；
2. 根本不需要它。

人类程序中经常两种情况同时发生，效率相当惊人。

⸻

八、变量的初始值不是随便填的

R-01 在屏幕上写：

int count = 0;
int sum = 0;
int product = 1;

托马斯问：“为什么 count 和 sum 从 0 开始，product 却从 1 开始？”

“因为初始值必须表示还没有处理任何数据时的正确状态。”

假设我们要统计钻石数量。

还没看到任何钻石时：

count = 0

这是合理的。

如果一开始写：

int count = 1;

相当于机器人在还没检查仓库前，就先凭空报告找到了一颗钻石。村民大概会支持这种统计方式，直到需要真的交货。

求和也是一样。

还没有加入任何数字时：

sum = 0

因为 0 加上任何数，不会改变那个数。

例如：

0 + 3 + 5 + 2 = 10

求乘积时却不同：

product = 1

因为 1 乘任何数，不改变那个数：

1 × 3 × 5 × 2 = 30

如果乘积从 0 开始：

0 × 3 × 5 × 2 = 0

后面所有数据都被 0 吞掉了，堪称乘法世界里的黑洞。

因此，设置变量初始值时要问：

在尚未处理任何数据时，这个变量应该表示什么？

这比背“求和初始化为 0、乘积初始化为 1”更重要，因为它能迁移到更多问题。

⸻

九、变量名应该描述“现在保存的是什么”

托马斯写了一段代码：

int x = 0;

Notch 问：“x 表示什么？”

“数量。”

“什么数量？”

“箱子的数量。”

“什么箱子？”

“损坏箱子的数量。”

“那为什么不叫 brokenBoxCount？”

托马斯觉得这名字有点长。

R-01 回答：“比花三十分钟猜测 x 的含义短。”

好的变量名不一定越长越好，但应该让人知道它当前保存的状态。

例如：

int count;

比 x 好一些，但仍然不够具体。

int evenCount;

说明它统计偶数个数。

int damagedBoxCount;

说明它统计损坏箱子数量。

又例如：

int max;

虽然看得出是最大值，但 max 也是 C++ 标准库中的常用函数名，容易让代码显得别扭。

更好的写法是：

int currentMax;

或者：

int maxValue;

它们说明变量保存的是最大值，而不是最大值的位置。

如果保存位置，应写：

int maxPosition;

值和位置是两个不同概念：

数组：3 8 2 9 5
最大值：9
最大值位置：3

变量名应该把这种差异说清楚。

⸻

十、错误博物馆：变量名字没变，不代表内容没变

错误博物馆的第二件展品，是一段看起来十分平静的代码：

int diamonds = 5;
diamonds = diamonds + 3;
diamonds = diamonds - 2;

一名初学者在旁边写下解释：

diamonds 一直是 5，因为变量名字没有变。

R-01 把它标记为：

错误类型：把变量名当成固定数值

逐步执行：

执行阶段	diamonds
初始化	5
增加 3	8
减少 2	6

变量名 diamonds 始终没变，因为它一直表示“钻石数量”。

变化的是格子中的内容。

就像仓库门牌写着“钻石仓库”，门牌不变，并不意味着里面永远只有五颗钻石。除非这个仓库由某些效率特别稳定的村民负责。

⸻

十一、错误博物馆：把数学关系误认为自动更新

第三件展品：

int width = 4;
int height = 3;
int area = width * height;
width = 10;

学生预测：

area = 30

实际却是：

width = 10
height = 3
area = 12

为什么？

因为：

int area = width * height;

只在执行这一行时计算一次。

当时：

width = 4
height = 3

所以：

area = 12

后来修改 width，不会自动重新计算 area。

如果希望面积更新，必须再次执行：

area = width * height;

这揭示了数学与代码的又一个重要差别。

数学公式：

S=ab

描述一个持续成立的关系。

程序赋值：

area = width * height;

描述一次计算动作。

它的含义是：

现在读取 width 和 height，算出结果，存入 area。

不是：

从此以后，请永远监督 width 和 height，任何一个发生变化就自动更新 area。

普通变量做不到这一点。

⸻

十二、给变量画“生命轨迹”

理解变量最有效的方法，不是盯着代码看，而是记录它每一步怎样变化。

例如：

int coins = 4;
coins = coins + 2;
coins = coins * 3;
coins = coins - 5;

可以画成：

4 → 6 → 18 → 13

或者使用状态表：

执行语句	coins 执行前	计算结果	coins 执行后
coins = 4	未定义	4	4
coins = coins + 2	4	6	6
coins = coins * 3	6	18	18
coins = coins - 5	18	13	13

变量轨迹有两个作用。

第一，它能帮助我们预测程序输出。

第二，它能揭露错误发生在哪一步。

如果期望答案是 15，但轨迹变成：

4 → 6 → 18 → 13

就能逐步检查是哪条更新规则与目标不符，而不是盯着整段代码等待灵感降临。

⸻

十三、变量到底该保存“过去”“现在”还是“未来”

Notch 给托马斯出了一道题：

仓库中依次出现数字 3、8、2、9、5。机器人每次只能看一个数字，但最后要报告最大值。

托马斯写：

int answer = 9;

Notch 问：“你是怎么看见 9 的？”

“我看了整个数组。”

“机器人在第一步还没看见后面。”

这就是程序设计中常见的视角差异。

人看题时，通常站在整个过程之外，可以同时看到全部数据和最终答案。

程序执行时，却始终活在“现在”。

在看到第一个数字时，它只知道：

已经看到：3
未来数字：未知

因此变量不能一开始就保存未来答案，只能保存：

到目前为止的最好结果

例如：

int currentMax = 3;

随着数据出现，它不断更新。

这类变量可以叫作“当前状态”。

它不是最终答案，但只要更新规则正确，处理完全部数据后，它就会自然变成最终答案。

这是从数学结果走向代码过程最关键的转换之一：

最终答案
↓
到目前为止的答案
↓
每处理一个新数据，更新一次

后面学习求和、计数、最大值、最小值、递推时，都会反复使用这个模式。

⸻

十四、写变量前，先回答五个问题

Notch 在黑板上写下五个问题，要求托马斯以后每创建一个变量都先回答。

第一问：它表示什么

不是“它是 int”，而是：

它代表当前拥有的苹果数量

第二问：为什么必须记住它

例如：

后面还要在这个数量上继续增加或减少

第三问：初始时应该是多少

例如还没统计任何偶数：

evenCount = 0

第四问：什么时候更新

例如发现一个偶数时：

evenCount++;

第五问：更新后旧值还需不需要

如果还需要，必须提前保存。

这五个问题比“这里定义一个变量”更接近真正的程序设计。

因为代码中的变量不是从语法书里掉出来的，它们是被问题需要出来的。

⸻

十五、本章挑战：给变量找工作

挑战一

下面的变量各自表示什么？

int sum = 0;
int count = 0;
int currentMax = a[0];
bool found = false;

参考解释：

sum：目前已经累加得到的总和
count：目前已经统计到的数量
currentMax：目前已经看到的最大值
found：目前是否已经找到目标

重点不是它们的数据类型，而是它们在程序过程中承担的工作。

⸻

挑战二

执行下面代码后，x 和 y 各是多少？

int x = 4;
int y = x;
x = x + 3;

过程：

x = 4
y 复制当时的 x，得到 4
x 更新为 7

最终：

x = 7
y = 4

⸻

挑战三

执行下面代码后，area 是多少？

int width = 5;
int height = 2;
int area = width * height;
height = 10;

答案：

area = 10

不是 50。

因为 area 只在第三行计算了一次。修改 height 后，并没有重新执行面积计算。

⸻

挑战四

统计数组中正数的个数，应该设计什么变量？

变量名：positiveCount
含义：目前已经找到的正数个数
初始值：0
更新时间：每发现一个正数时加1

对应代码骨架：

int positiveCount = 0;
if (当前数字 > 0) {
    positiveCount++;
}

这里暂时不关注循环，只关注变量为什么存在、什么时候改变。

⸻

挑战五

求一组数的乘积，下面哪个初始值正确？

int product = 0;

还是：

int product = 1;

应该选择：

int product = 1;

因为还没处理任何数据时，乘积状态应当保持后续数字不变。

⸻

挑战六

下面代码中，哪一个变量只是临时保存旧值？

int temp = x;
x = y;
y = temp;

答案是：

temp

它的任务只是保护原来的 x，避免在覆盖后丢失。

⸻

十六、给代码配音

看到：

int score = 100;

不要只读成：

定义一个整数 score 等于 100。

应读成：

准备一个名叫 score 的整数格子，把 100 放进去。

看到：

score = score + 20;

应读成：

读取当前分数，加 20，再把新分数写回 score。

看到：

int oldScore = score;

应读成：

复制当前分数，保存到另一个名叫 oldScore 的格子里。

看到：

score = 0;

应读成：

用 0 覆盖当前分数，原来的分数不再保存在 score 中。

当学生能这样给代码配音时，变量就不再是抽象符号，而是一组可以在脑中看见的存储格和变化动作。

⸻

十七、这一章真正要记住什么

离开工坊前，托马斯在黑板上写下：

数学变量常用来表示未知量或数量关系。

程序变量是内存中一个有名字的格子。

变量名表示这个格子负责记什么，变量值表示它此刻记住了什么。

一个普通变量同一时刻只能保存一个值，新值写入后旧值会被覆盖。

程序变量不会自动跟随其他变量变化，只有代码执行时才会更新。

R-01 看完后，在最后补了一句：

“变量不是答案的字母代号，而是程序在时间中留下的记忆。”

托马斯看着那排贴着名字的透明格子，终于理解了一件事。

程序并不是一大堆数学字母。

它更像一个不断变化的仓库。

每个变量负责保存一件重要的事情。程序每向前走一步，就可能打开某个格子，读取里面的旧值，算出一个新值，再把它放回去。

一个程序能够走多远，取决于它是否记住了下一步仍然需要的东西。

而所有这些记忆，都从一个看似普通的格子开始。
