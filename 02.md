# 第二章　变量不是未知数，而是一个格子

第二天早上，托马斯走进工坊时，看见四个透明登记格排在墙边。格子上的标签十分简洁：`a`、`b`、`c`、`d`。

它们当前装着：

```text
a = 5    b = 12    c = 2    d = 350
```

昨晚的值班表本来写着每个名字的含义，可一只苦力怕在边界测试中提前下班，顺便把值班表炸成了纸屑。此刻主管只留下一条任务：“售出一个苹果，库存减一。”

托马斯盯着 5 看了看，觉得苹果有 5 个很合理，于是让 R-01 更新 `a`：

```cpp
a = a - 1;
```

墙上的皇家日历立刻从第五天退回第四天。已经交过租金的村民被系统通知：“昨天尚未缴费，请再交一次。”

交易大厅里响起了一片整齐的“哼”。

R-01 检查残存记录后报告：“`a` 保存日期。苹果库存由 `b` 保存，目前仍是 12。”

“你为什么不提醒我？”

“变量名 `a` 没有表达苹果。你根据数值猜测含义，猜错概率由你承担。”

Notch 从地上捡起半张值班表：“一个格子能保存数，却不会自动保存这个数为什么存在。那部分要靠名字和程序员的脑子。”

---

## 2.1 数学里的 $x$ 与程序里的 `x`

在方程里：

\[
x+3=8
\]

$x$ 常常是等待求出的未知数。解出 $x=5$，任务就结束了。

数学中的字母也可以表达数量关系：

\[
y=2x+1
\]

这句话描述所有符合条件的 $x$ 和 $y$。它没有指定一个内存位置，也没有说程序先算哪一个。

程序变量更具体。执行：

```cpp
int apples = 12;
```

机器会准备一个能保存整数的内存位置，给它名字 `apples`，再写入 12。可以把它想成：

```text
┌────────────────┐
│ 名字：apples   │
│ 类型：int      │
│ 当前内容：12   │
└────────────────┘
```

变量不是“一个还没猜出来的数字”，而是程序运行时真正使用的存储格。名字指出去哪里读写，类型约束格子能装什么，当前值则是这一刻保存在里面的数据。

同一个格子以后可以改成 11、15 或 0。它还是 `apples`，因为它一直负责记录苹果库存。

数学变量重在表达关系；程序变量还承担记忆工作。程序走到下一步时，凡是仍需知道的信息，都得在某个地方活着。

---

## 2.2 一个格子在同一时刻只能保存一个值

主管重新确认苹果库存是 12。卖出一个以后，托马斯先不写代码，而是画出目标：

\[
\text{新库存}=\text{旧库存}-1
\]

内存中只有一个 `apples` 格。执行前它装 12，执行后要装 11。

```cpp
apples = apples - 1;
```

逐帧变化如下：

| 帧 | `apples` 格 | 右边计算结果 | 说明 |
|---:|---:|---:|---|
| 0 | 12 | 未计算 | 旧库存仍在 |
| 1 | 12 | 11 | 读取 12，算出 11 |
| 2 | 11 | 11 | 写回后，12 被覆盖 |

一个普通变量在同一时刻只保存一个当前值。它不会同时装着“卖出前的 12”和“卖出后的 11”。如果后续还要打印旧库存，就必须提前准备另一个格子：

```cpp
int oldApples = apples;
apples = apples - 1;
```

现在 `oldApples` 保存 12，`apples` 保存 11。两个时刻的信息之所以都在，不是因为变量变宽了，而是因为我们用了两个格子。

R-01 把这条规则写在仓库墙上：“想同时保留两个值，请使用两个位置。把愿望重复两遍不能增加内存容量。”

---

## 2.3 新值写进去，旧值去了哪里

托马斯仍不放心：“被覆盖的 12 到底去哪了？”

“从 `apples` 的当前状态中消失。”

“有没有隐藏历史？”

“这不是版本控制系统。”

赋值产生的是覆盖。假设执行：

```cpp
int diamonds = 5;
diamonds = diamonds + 3;
diamonds = diamonds - 2;
```

`diamonds` 的生命轨迹是：

| 执行语句 | 执行前 | 右边结果 | 执行后 |
|---|---:|---:|---:|
| `int diamonds = 5;` | 未定义 | 5 | 5 |
| `diamonds = diamonds + 3;` | 5 | 8 | 8 |
| `diamonds = diamonds - 2;` | 8 | 6 | 6 |

最终格子里只有 6。旧值 5 和 8 曾经存在过，但当前内存状态不会替你保存它们。

这里可以分清两件常被混在一起的事：

- 变量的含义可以保持不变：它一直表示钻石数量；
- 变量的内容可以连续变化：5、8、6 是不同时刻的数量。

门牌写“钻石仓库”，不代表仓库里永远有五颗钻石。门牌负责说明用途，库存负责反映现在。某些村民倒希望二者永远不变，尤其是库存很高而账本很旧的时候。

---

## 2.4 变量名是在说明“这个格子记什么”

Notch 把事故中的四个变量重新命名：

```cpp
int day = 5;
int apples = 12;
int diamonds = 2;
int score = 350;
```

现在“售出一个苹果”不再需要猜测。任务先被翻译成状态变化：苹果当前数量减少一；日期、钻石和分数都不变。

```cpp
apples = apples - 1;
```

好名字至少应该让读者回答两个问题：

1. 这个格子表示什么？
2. 当前值的单位或角色是什么？

`damagedBoxCount` 比 `x` 更清楚，因为它说明保存的是损坏箱子的个数。`maxPosition` 和 `maxValue` 也不能混用，一个保存位置，一个保存数值。

名字不必长成一份事故报告。短循环里的 `i` 能清楚表示当前位置时，没有必要写成 `currentArrayTraversalIndex`。判断标准不是字母数量，而是读者能否在当前上下文中说出格子的工作。

可以用一张登记卡检查变量：

| 变量名 | 当前值示例 | 它在记什么 | 后面为什么还需要 |
|---|---:|---|---|
| `apples` | 11 | 当前苹果库存 | 后续还会进货或售出 |
| `score` | 350 | 当前得分 | 奖励和扣分要在此基础上更新 |
| `oldApples` | 12 | 更新前的库存 | 需要打印变更记录 |

变量之所以出现，不是因为 C++ 语法要求程序里放几个 `int`。它出现，是因为程序走到下一步时仍需要记住某件事。

---

## 2.5 `current`、`sum`、`count` 为什么比 `a`、`b` 更好

下午，R-01 要检查三只货箱，箱内数量依次为 4、7、2。托马斯暂时不写循环，只观察机器人检查第二只箱子时需要记住什么。

```text
已经看过：4
当前看到：7
目前总和：4
已经统计的箱数：1
```

这些状态可以各有一个名字：

```cpp
int current = 7;
int sum = 4;
int count = 1;
```

它们都保存整数，工作却不同：

- `current` 是这一刻正在处理的数；
- `sum` 是已经处理部分的总和；
- `count` 是已经统计的数量。

当 7 被纳入统计后，状态变为：

```cpp
sum = sum + current;
count = count + 1;
```

| 时刻 | `current` | `sum` | `count` | 含义 |
|---|---:|---:|---:|---|
| 处理第二箱前 | 7 | 4 | 1 | 7 还没算进去 |
| 更新 `sum` 后 | 7 | 11 | 1 | 总和已经包含 7 |
| 更新 `count` 后 | 7 | 11 | 2 | 已处理两只箱子 |

若把它们全叫 `a`、`b`、`c`，机器仍能执行，但人必须另外记住每个字母的职责。代码一长，脑子就会同时承担程序和猜谜游戏两份工作。

名字中的 `current` 或“目前”还提醒我们：变量保存的是过程状态，不一定一开始就是最终答案。`sum=4` 不是算错了，它只表示“目前看完第一箱后的总和”。等全部箱子处理完，过程状态才成为最终结果。

---

## 2.6 变量变化轨迹图

程序变量活在时间里，所以读代码不能只问“它是多少”，还要问“在哪一步是多少”。

看下面的代码：

```cpp
int coins = 4;
coins = coins + 2;
coins = coins * 3;
coins = coins - 5;
```

生命轨迹是：

```text
未定义 → 4 → 6 → 18 → 13
```

状态表能把变化说得更清楚：

| 步骤 | `coins` 执行前 | 计算结果 | `coins` 执行后 |
|---:|---:|---:|---:|
| 初始化 | 未定义 | 4 | 4 |
| 加 2 | 4 | 6 | 6 |
| 乘 3 | 6 | 18 | 18 |
| 减 5 | 18 | 13 | 13 |

只看最终的 13，会错过程序怎样到达它。轨迹则能指出错误第一次发生在哪里。若第三步本应乘 2，状态从 6 变成 18 的那一刻就已经偏离目标，不必等输出以后再盯着整个程序发呆。

### 一次赋值不是永久连线

变量轨迹还能拆穿另一个常见误会：

```cpp
int x = 3;
int y = x + 1;
x = 10;
```

执行第二行时，`y` 得到 4。第三行只改 `x`，所以最终状态是：

| 步骤 | `x` | `y` |
|---|---:|---:|
| `int x = 3;` 后 | 3 | 未定义 |
| `int y = x + 1;` 后 | 3 | 4 |
| `x = 10;` 后 | 10 | 4 |

数学关系 $y=x+1$ 可以表示持续关系，但程序赋值只在执行当时计算一次。`y` 没有与 `x` 拉上一根自动同步的红石线。要让它更新，必须再次执行 `y = x + 1;`。

---

## 错误博物馆

### 展品一：靠数值猜变量含义

```cpp
a = a - 1;
```

这句本身语法正确，却无法告诉读者 `a` 是苹果、日期还是生命值。事故中托马斯把日期减一，正是因为变量名没有保留业务含义。

修复方式不是“下次猜准一点”，而是让名字承担说明工作：

```cpp
apples = apples - 1;
```

### 展品二：以为复制会建立永久连接

```cpp
int x = 3;
int y = x;
x = 9;
```

`y = x;` 只复制当时的 3。后来 `x` 变成 9，`y` 仍是 3。普通整数变量之间没有心灵感应。

### 展品三：保存得太晚

任务要求同时保留更新前后的分数。错误写法是：

```cpp
score = score + 20;
int oldScore = score;
```

若旧分数是 100，执行后两个格子都是 120。名叫 `oldScore` 并不会让数据穿越回上一行。

正确顺序要在覆盖前保存：

```cpp
int oldScore = score;
score = score + 20;
```

| 步骤 | `score` | `oldScore` |
|---|---:|---:|
| 初始 | 100 | 未定义 |
| 保存旧值后 | 100 | 100 |
| 更新后 | 120 | 100 |

名字说明格子的职责，顺序决定它实际装进去的是哪一刻的值。两者缺一不可。

---

## 翻译训练

### 训练一：给变量找工作

任务是统计损坏箱子的个数。请写出变量名、含义、初始值和更新时间。

参考答案：

- 名字：`damagedBoxCount`；
- 含义：目前已经发现的损坏箱子数；
- 初始值：0；
- 更新时间：每确认一只箱子损坏时加一。

### 训练二：补生命轨迹

```cpp
int health = 20;
health = health - 7;
health = health + 3;
```

轨迹是：

```text
20 → 13 → 16
```

请分别说明两次赋值覆盖了什么：第一次用 13 覆盖 20，第二次用 16 覆盖 13。

### 训练三：判断旧值是否需要保存

任务 A：“把 `score` 增加 10，以后只使用新分数。”不必额外保存旧值。

任务 B：“把 `score` 增加 10，并打印更新前后的分数。”必须在覆盖前把旧值复制到另一个变量。

### 训练四：代码到含义

```cpp
int current = 6;
int sum = 14;
int count = 3;
```

不要只说“有三个整数”。请为它们配音：当前处理的数是 6；已处理部分的总和是 14；已经处理了 3 个数据。

托马斯把墙上的 `a、b、c、d` 全部换掉。皇家日历恢复第五天，苹果库存也终于从 12 正确变成 11。

R-01 记录：“变量名不会改善机器执行速度，但会显著降低人类把昨天当成今天的概率。”

托马斯已经明白变量是格子。可新的麻烦很快出现：同一个格子会在一行又一行代码中改变。若不沿着执行时间逐帧观察，他仍然会把不同时间的值混在一起。
