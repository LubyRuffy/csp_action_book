# 第十二章　数学区间怎样变成循环边界

皇家铁路的夜间照明坏了。

准确地说，不是灯坏了，是负责检查灯的 R-01 检查得很有选择性。矿车隧道里有五盏红石灯，控制台给它们挂了地址牌：

```text
地址：  0   1   2   3   4
灯号： L0  L1  L2  L3  L4
```

Notch 的命令是：“检查地址 1 到地址 3 的每一盏灯。”

托马斯很快写好循环。R-01 从 1 号灯开始，检查了 1 号、2 号，然后转身回来。

“3 号呢？”托马斯问。

“停止条件已满足。”

“我说的是从 1 到 3！”

“你的程序说，到 3 以前。”

3 号灯很配合地熄灭了。紧接着，一辆运南瓜的矿车在黑暗中撞上停车器，南瓜沿轨道滚出二十多米。村民们没有受伤，只是围着托马斯要求赔偿。他们对程序边界不感兴趣，对南瓜边界极其敏感。

托马斯把停止条件改了一下。这次 R-01 检查完整条隧道时，又走到了 5 号位置。

那里没有灯，只有一只刚从检修洞里探头的苦力怕。

Notch 拉下总闸，叹了口气：“看来今天得把‘到哪里结束’说清楚。数组外面没有隐藏奖励，通常只有非法内存和苦力怕。”

---

## 一、数学里的“从左到右”有两种说法

数学区间 \([l,r]\) 包含左右两个端点。比如：

\[
[1,3]=\{1,2,3\}
\]

这里共有：

\[
3-1+1=3
\]

个整数。一般地，整数闭区间 \([l,r]\) 的长度是：

\[
r-l+1
\]

那个“加一”不是数学家临走时顺手塞进去的。只算 \(r-l\)，算的是从左端走到右端跨了几步；把起点本身也算进去，才得到元素个数。

程序里还常见左闭右开区间 \([l,r)\)：

\[
[1,4)=\{1,2,3\}
\]

它包含左端 1，不包含右端 4，长度正好是：

\[
r-l
\]

两种写法都能表示同一批位置：

\[
[1,3]=[1,4)
\]

区别只在于，前一种把“最后要处理的位置”写进区间，后一种把“第一个不处理的位置”写在右端。

R-01 把这两句话贴在控制台上：

```text
闭区间 [l,r]：处理到 r 为止，r 也处理
左闭右开 [l,r)：走到 r 就停，r 不处理
```

人说“到 3”，可能觉得意思已经够明白。机器会继续追问：3 算不算？这不是抬杠，是循环能否少一次或多一次的分界线。

---

## 二、五盏灯在内存里占哪些格子

隧道有五盏灯。人类习惯说“第一盏、第二盏……第五盏”，但数组下标通常从 0 开始：

| 人类称呼 | 第一盏 | 第二盏 | 第三盏 | 第四盏 | 第五盏 |
|---|---:|---:|---:|---:|---:|
| 数组下标 | 0 | 1 | 2 | 3 | 4 |
| 是否有效 | 是 | 是 | 是 | 是 | 是 |

五个元素的有效下标是：

\[
0,1,2,3,4
\]

也就是：

\[
[0,4]=[0,5)
\]

因此，长度是 `n` 时，最后一个有效下标是 `n-1`。`n` 本身很有用，但它表示的是右边界外的第一个位置，不是最后一格。

托马斯在仓库门上写“共有 5 格”，R-01 不会因此造出一格编号 5。数量和最后下标差一，因为编号从 0 起步。

内存视角下，访问下标 5 相当于把手伸到五个合法格子之外。那里可能碰巧没有立刻爆炸，也不代表那里属于你。苦力怕没有每次都出现，也不代表洞口安全。

---

## 三、把闭区间逐帧展开

先解决最初的任务：检查地址 1 到地址 3，两个端点都要检查。

写任何循环之前，托马斯先按 Notch 的要求填写“翻译单”：

- 已知状态：灯的有效下标是 0 到 4，本次处理区间是 \([1,3]\)。
- 要记住的状态：`i` 表示当前正在检查的下标；检查结果由当次灯的状态决定，不必保存过去的 `i`。
- 更新顺序：先处理 `i` 指向的灯，再让 `i` 增加 1。
- 旧值是否还需要：处理当前灯时需要当前 `i`，处理完成后才可以增加。
- 停止条件：当 `i` 变成 4 时停止，因为 3 已经处理过。

于是，闭区间 \([l,r]\) 自然翻译成“从 `l` 开始，只要 `i <= r` 就继续”。以统计这三盏灯中亮灯数量为例：

```cpp
int onCount = 0;
for (int i = 1; i <= 3; i++) {
    if (lamp[i]) {
        onCount++;
    }
}
```

假设三盏灯的状态依次是亮、灭、亮，逐帧变化如下：

| 时刻 | `i` | 是否满足 `i <= 3` | 当前灯 | `onCount` | 动作 |
|---|---:|---|---|---:|---|
| 开始 | 1 | 是 | 亮 | 0 | 计数加一 |
| 第一次更新后 | 2 | 是 | 灭 | 1 | 不增加 |
| 第二次更新后 | 3 | 是 | 亮 | 1 | 计数加一 |
| 第三次更新后 | 4 | 否 | 不读取 | 2 | 停止 |

注意最后一行。`i` 可以变成 4，循环用它做一次条件判断，但不会读取 4 号灯。停止条件像门卫：先看地址牌，再决定放不放机器人进去。

---

## 四、同一段位置也可以写成左闭右开

仍然处理下标 1、2、3，可以把范围写成 \([1,4)\)。右端 4 表示第一个不处理的位置。

代码出现前，再确认一次状态：

- 已知状态：要处理的下标是 1、2、3。
- 要记住的状态：`i` 仍表示当前位置。
- 更新顺序：处理后执行加一。
- 旧下标处理完便不再需要。
- 停止条件：`i` 到达 4 就停，绝不读取 `lamp[4]`。

对应循环是：

```cpp
int onCount = 0;
for (int i = 1; i < 4; i++) {
    if (lamp[i]) {
        onCount++;
    }
}
```

状态表只改变了条件的写法：

| `i` | `i < 4` | 是否处理 |
|---:|---|---|
| 1 | 真 | 处理 1 号 |
| 2 | 真 | 处理 2 号 |
| 3 | 真 | 处理 3 号 |
| 4 | 假 | 停止 |

左闭右开区间特别适合整个数组。若长度是 `n`，有效范围就是 \([0,n)\)，循环条件直接写成“`i` 小于 `n`”。长度和停止位置使用同一个数，不必每次算 `n-1`。

这不是哪种写法更高级的问题。关键是先说清右端是否包含，再让条件忠实地表达它。

---

## 五、长度、末尾位置和停止位置别坐错椅子

假设一个数组从下标 `l` 开始，连续放了 `length` 个元素。

它们的位置是：

\[
l,l+1,\ldots,l+length-1
\]

因此：

\[
\text{最后位置}=l+length-1
\]

而第一个不属于这段的位置是：

\[
\text{停止位置}=l+length
\]

把这几个量排在一起更清楚：

| 已知量 | 闭区间写法 | 左闭右开写法 |
|---|---|---|
| 起点 | `l` | `l` |
| 长度 | `length` | `length` |
| 最后处理位置 | `l + length - 1` | `l + length - 1` |
| 循环右端 | `l + length - 1` | `l + length` |
| 继续条件 | 不超过最后位置 | 还没到停止位置 |

比如从下标 2 开始取 4 个元素，位置是 2、3、4、5。闭区间是 \([2,5]\)，左闭右开是 \([2,6)\)。如果有人把“长度 4”直接当成“最后下标 4”，机器人就会问：“从哪里开始的 4？”这个问题很烦，但它是对的。

---

## 六、错误博物馆：把数量当成最后下标

第一件展品来自托马斯修订后的整条隧道检查程序。

当时已知数组长度是 `n=5`，合法下标为 0 到 4。他想处理全部元素，却把 `n` 当成了最后下标，停止条件允许 `i` 走到 5。错误循环的动作可以概括为：从 0 开始，只要 `i` 不超过 5 就继续。

逐帧记录已经足够定罪：

| `i` | 位置是否合法 | 结果 |
|---:|---|---|
| 0 | 是 | 检查第一格 |
| 1 | 是 | 检查第二格 |
| 2 | 是 | 检查第三格 |
| 3 | 是 | 检查第四格 |
| 4 | 是 | 检查第五格 |
| 5 | 否 | 越界访问 |

错误不是“循环语法记错了”，而是把两个不同概念混成一个：`n` 是元素个数，也是第一个不能访问的位置；`n-1` 才是最后一个元素的位置。

第二件展品反过来少算一次。任务是闭区间 \([1,3]\)，却使用“到 3 就停止”的条件，于是只处理 1、2。条件本身没有坏，它准确表达了 \([1,3)\)。坏的是数学区间和程序条件没有说同一种话。

第三件展品从 1 开始处理一个长度为 `n` 的数组。这样避开了下标 0，却仍在处理 `n` 个位置时闯到下标 `n`。人类第一项与数组下标 0 之间的错位，是边界博物馆的长期赞助商。

---

## 七、翻译训练：先说范围，再写符号

### 训练一：数学到过程

数学要求处理整数闭区间 \([4,7]\)。不要先写循环，先列出实际下标。

答案是 4、5、6、7，共 \(7-4+1=4\) 个。若使用左闭右开写法，它等价于 \([4,8)\)。

### 训练二：代码到状态

一个循环从 `i=2` 开始，在 `i<6` 时继续，每次加一。请给它配音。

参考配音：从下标 2 开始，依次处理 2、3、4、5；当 `i` 到达 6 时停止。它表达的是 \([2,6)\)，也等价于整数闭区间 \([2,5]\)。

### 训练三：补状态表

数组长度为 4，请补出全部有效下标和第一次非法下标。

| 项目 | 下标 |
|---|---|
| 第一个有效位置 | 0 |
| 最后一个有效位置 | 3 |
| 第一个非法位置 | 4 |

### 训练四：抓住那个“加一”

闭区间 \([l,r]\) 为什么有 `r-l+1` 个整数？

因为 `r-l` 只计算从 `l` 走到 `r` 的间隔数，还要把起点 `l` 本身算进去。

### 训练五：判断右端

“处理从 `start` 开始的 `length` 个格子”应在什么位置停止？

最后处理的位置是 `start+length-1`，第一个不处理的位置是 `start+length`。如果循环使用左闭右开范围，就在到达后者时停止。

---

## 八、边界不是循环最后再补的标点

隧道重新通车前，托马斯把任务完整地说了一遍：

“灯数组长度为 5，有效下标是 0 到 4。检查下标 1、2、3。`i` 从 1 开始，每次处理后加一；当 `i` 到 4 时停止。”

R-01 依次检查三盏灯，在 4 号位置之前停下。

“本次未访问非法内存。”它报告。

“你能不能把任务完成说得高兴一点？”

“可以。本次未遇见苦力怕。”

托马斯觉得这已经算机器人的热烈庆祝了。

这一章需要留下的不是某一种循环外形，而是三个清楚的地址：从哪里开始，最后处理谁，走到哪里停止。数学区间一旦把端点是否包含说清楚，循环边界就不再靠猜。

\[
[l,r]\longleftrightarrow l,l+1,\ldots,r
\]

\[
[l,r)\longleftrightarrow l,l+1,\ldots,r-1
\]

计算机不会理解“差不多到那里”。它会在边界线上严格执行，而边界线外面，通常没有多出来的数组格子替人类的含糊负责。
