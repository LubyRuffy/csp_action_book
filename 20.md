# 第二十章　数列公式怎样一步一步生成

皇家铁路的清晨班次通常很安静。矿车从零号站台出发，每到一站多挂三节车厢：第一站两节，第二站五节，第三站八节，第四站十一节。

托马斯把规律写给 R-01：

\[
a_i=a_{i-1}+3
\]

“照公式生成十个站台的车厢牌。”

R-01 在零号牌前停住，机械手伸向牌子左边的墙。

“你在找什么？”

“`a[-1]`。”

“墙里没有负一号格子！”

“公式要求读取前一项。当前项为零号，前一项为负一号。”

下一秒，机器人启动探测钻头，准备证明墙里确实没有。Notch 及时拔掉了红石电源，皇家铁路因此只损失了一块墙砖，而不是整面时刻表。

“你给了递推规则，”Notch 说，“却没给它出发点。人看见 `2,5,8,11`，会把第一项当作理所当然。机器认为理所当然是一种未声明变量。”

---

## 一、数学眼中的一列数

描述同一列数，数学常用两种办法。

第一种是通项公式。若从下标 0 开始：

\[
a_i=2+3i,\qquad i=0,1,\ldots,n-1
\]

想知道 `a[7]`，可以直接代入 7，不必先算前六项。

第二种是递推公式：

\[
a_0=2,\qquad a_i=a_{i-1}+3\quad(i\ge 1)
\]

它说的不是一个孤立答案，而是一套生成动作：先把第一个格子设成 2；以后每一格都读取左边刚生成的值，加 3，再写入自己。

二者得到同一列数，却给计算机两条不同道路。通项公式像按站号直接打印车票；递推公式像矿车一站一站往前开。今天要练的是后一种，因为它把“前一项”怎样活到“后一项”里展示得最清楚。

---

## 二、内存里没有省略号

纸上写 `2,5,8,11,...`，省略号会让人自动继续。数组里没有会工作的省略号。R-01 看到的是一排独立格子：

```text
a[0]  a[1]  a[2]  a[3]  a[4]
 ?      ?      ?      ?      ?
```

先执行 `a[0] = 2`，第一个格子才有可读的旧状态。生成 `a[i]` 时，需要三样东西：

- `i`：现在要填写哪个格子；
- `a[i-1]`：已经存在的前一项；
- `a[i]`：准备接收新值的格子。

这里不会覆盖前一项，因为新值写进另一个数组格。数组把整个数列的历史都留下了。R-01 可以回头查看任意一站，代价是每一项都占一个格子。

逐帧生成五项：

| 执行动作 | `i` | `a[0]` | `a[1]` | `a[2]` | `a[3]` | `a[4]` |
|---|---:|---:|---:|---:|---:|---:|
| 初始，尚未写入 | — | ? | ? | ? | ? | ? |
| 写入首项 | — | 2 | ? | ? | ? | ? |
| `a[1]=a[0]+3` | 1 | 2 | 5 | ? | ? | ? |
| `a[2]=a[1]+3` | 2 | 2 | 5 | 8 | ? | ? |
| `a[3]=a[2]+3` | 3 | 2 | 5 | 8 | 11 | ? |
| `a[4]=a[3]+3` | 4 | 2 | 5 | 8 | 11 | 14 |

注意每一行的因果关系。填写 `a[3]` 时，机器人读取的是已经生成的 `a[2]=8`。它不会读取未来的 `a[4]`，也不会凭省略号猜出 11。

---

## 三、从动作长出代码

代码出现前，先把问题问完。

现在已知首项是 2；需要记住整个数列，所以使用数组；下一步要把前一项加 3 写进当前格；前一项仍要保留，写入不同格即可；循环从 1 开始，因为零号格已经单独准备；生成到 `n-1` 后停止。

于是代码只是把这些回答排好：

```cpp
int a[100];
a[0] = 2;
for (int i = 1; i < n; i++) {
    a[i] = a[i - 1] + 3;
}
```

给循环配音：

> 从一号格开始。读取左边相邻格的现值，加三，把结果写入当前格，然后向右走一格。只要当前下标还小于 `n`，就继续。

若使用通项公式，则每一项都只依赖下标：

```cpp
for (int i = 0; i < n; i++) {
    a[i] = 2 + 3 * i;
}
```

这段从 0 开始，因为 `i=0` 时公式本身就能得到首项 2。循环边界相同，起点不同，原因不在写法习惯，而在零号元素能否由当前公式安全生成。

---

## 四、需要两个旧状态的数列

下午，村民育种站送来另一张规则表：

\[
f_0=1,\qquad f_1=1,
\]

\[
f_i=f_{i-1}+f_{i-2}\quad(i\ge2)
\]

每一轮能供应的饲料包数，等于前两轮的总和。托马斯说：“这不还是加法吗？”

R-01 指着两个输入位置：“是加法，但本次加法欠前两个时刻各一笔数据。”

生成 `f[4]` 时，要读取 `f[3]` 和 `f[2]`。因此必须先准备两个首项，循环也要从 2 开始。

```cpp
int f[100];
f[0] = 1;
f[1] = 1;
for (int i = 2; i < n; i++) {
    f[i] = f[i - 1] + f[i - 2];
}
```

它的状态表是：

| 正在生成 | 读取的两个旧格 | 算式 | 新写入 | 已有序列 |
|---|---|---|---:|---|
| 首项 | 无 | 直接给定 | 1 | `1` |
| 第二项 | 无 | 直接给定 | 1 | `1,1` |
| `f[2]` | `f[1]=1`,`f[0]=1` | `1+1` | 2 | `1,1,2` |
| `f[3]` | `f[2]=2`,`f[1]=1` | `2+1` | 3 | `1,1,2,3` |
| `f[4]` | `f[3]=3`,`f[2]=2` | `3+2` | 5 | `1,1,2,3,5` |
| `f[5]` | `f[4]=5`,`f[3]=3` | `5+3` | 8 | `1,1,2,3,5,8` |

每次写入后，新项才有资格在下一轮成为“旧项”。这里研究的是旧状态如何被读取和保存，不是给数列套上更复杂的算法名字。育种站只想按时收到饲料，并不关心托马斯能否用术语把村民说睡着。

---

## 五、错误博物馆：递推式也有起跑线

### 展品一：从零号格递推

```cpp
for (int i = 0; i < n; i++) {
    a[i] = a[i - 1] + 3;
}
```

第一次循环会读取 `a[-1]`。那不是“前面可能有个 2”，而是有效数组范围之外。数学下标可以按定义延伸，C++ 数组边界外只有风险，不藏彩蛋。

### 展品二：忘记第二个首项

```cpp
f[0] = 1;
for (int i = 1; i < n; i++) {
    f[i] = f[i - 1] + f[i - 2];
}
```

`i=1` 时仍会读取 `f[-1]`。公式明确要求 `i>=2`，代码却擅自提前营业。

### 展品三：把下标加进数值

```cpp
a[i] = a[i] + 3;
```

右边读取的是当前尚未生成的格子，而不是前一格。正确依赖是 `i-1`。一个字符的差别，代表机器人把手伸向完全不同的箱子。

### 展品四：多走一步

若数组有效下标是 `0` 到 `n-1`，条件应是 `i<n`。写成 `i<=n` 会试图生成 `a[n]`。铁路有 `n` 个站牌，最后一个编号不是 `n`，这是苦力怕最喜欢检查的常识。

---

## 六、翻译训练

### 1. 数学到代码

已知：

\[
b_0=4,\qquad b_i=2b_{i-1}-1\quad(i\ge1)
\]

写出生成前 `n` 项的核心代码，并说明循环为何从 1 开始。

参考：

```cpp
b[0] = 4;
for (int i = 1; i < n; i++) {
    b[i] = 2 * b[i - 1] - 1;
}
```

因为 `b[0]` 已直接给定，规则只负责一号及后面的格子。

### 2. 代码到状态

```cpp
c[0] = 3;
for (int i = 1; i < 4; i++) {
    c[i] = c[i - 1] * 2;
}
```

补出轨迹：`3 → 6 → 12 → 24`。并给第三行配音：读取刚生成的前一项，乘二，写入当前格。

### 3. 找依赖

对于 `g[i]=g[i-1]-g[i-2]`，生成 `g[6]` 时必须读取哪两个格子？答案是 `g[5]` 和 `g[4]`。在这次写入前，二者都不能是未定义状态。

### 4. 只画表，不写代码

首项为 5，以后每项比前一项少 2。请画出前五项的状态表，并标出每一步读取格与写入格。答案序列为 `5,3,1,-1,-3`。

---

## 七、站台重新亮起

傍晚，R-01 重新打印铁路牌。零号牌先写上 2，然后机械臂从一号格开始向右移动：读取、加三、写入；再读取、加三、写入。

`2,5,8,11,14` 依次亮起。

托马斯看着那串早就会心算的数字，第一次没有只盯着结果。他看见每一个数都从前一格里带走一份旧信息，再在新格里留下下一步可用的状态。

“原来递推公式不是在介绍一串数。”他说，“它是在告诉机器人，新的一格怎样从旧格里出生。”

R-01 把探测钻头收回工具箱。

“并且首项必须由人类负责出生。否则我将继续检查墙体。”

Notch 在时刻表下写下本章的句子：

> 通项公式可以直接计算某一项；递推公式必须先准备初始状态，再按下标顺序生成新状态。

数学把一列数压缩成一句关系。代码要把这句话展开成起点、读取位置、写入位置和停止边界。省略号能留在纸上，不能被派去管理内存。