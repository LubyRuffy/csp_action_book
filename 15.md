# 第十五章　下标是数字在内存里的地址牌

皇家仓库有一排四个矿物箱，库存记录是：

```text
5  2  5  9
```

管理员的命令是：“第二个装着 5 的箱子刚补了三块矿石，把它改成 8。”

托马斯转告 R-01：“找到 5，改成 8。”

机器人从左边开始，看到第一个 5，立刻把它改成 8。记录变成：

```text
8  2  5  9
```

“我说的是第二个 5！”托马斯喊道。

“命令中没有地址。”

“同样都是 5，你就不能看出来哪个是后面那个吗？”

“我能。它们的值相同，位置不同。人类只提供了值，随后希望机器推测位置。”

仓库管理员看着被改错的账本，问能不能把“后面那个 5”补上，再把“前面那个 8”改回来。托马斯忽然发现，纸上的四个数字看起来只是一个列表，进了内存以后，每个数字还多了一块不能弄丢的地址牌。

Notch 把四只透明格子排在工作台上：“这一章不讲怎样寻找，也不讲怎样排列。只讲一件更早的事：数组里的值住在哪里。”

---

## 一、纸上的 `[1,5]` 与内存里的两格

人看到：

\[
[1,5]
\]

往往只注意两个值：1 和 5。数组在内存里却不是悬在纸上的括号。它是一排连续格子，每格有自己的下标：

| 下标 | 0 | 1 |
|---|---:|---:|
| 保存的值 | 1 | 5 |

这里有两类数字，不能混在一起：

- 1 和 5 是元素的值；
- 0 和 1 是元素的位置。

第二格中恰好也保存着 5，不代表“下标 1 就是数值 5”。下标回答“去哪里读写”，元素值回答“那里现在放着什么”。

R-01 找箱子先看地址牌，再打开箱子。人类说“拿 5”，它还得知道拿哪个位置的 5；人类说“打开下标 1”，它就能准确走到第二格，然后读出里面的 5。

这就是数组表达式 `a[i]` 的动作含义：

> 到数组 `a` 的第 `i` 号格子，读取或修改那一格的内容。

`i` 是地址的一部分，不是箱子里的货物。

---

## 二、人类序号和数组下标错开一格

日常说“第一个箱子”时，通常从 1 开始数。数组常从 0 开始编号：

| 人类说法 | 第一个 | 第二个 | 第三个 | 第四个 |
|---|---:|---:|---:|---:|
| 数组下标 | 0 | 1 | 2 | 3 |
| 本例中的值 | 5 | 2 | 5 | 9 |

所以“第二个装着 5 的箱子”在本例中位于下标 2，因为它是整排的第三个箱子，也是从左往右第二次出现数值 5 的位置。

“第几个”和“下标是多少”不是永远相同。若自然序号从 1 开始，那么：

\[
\text{数组下标}=\text{自然序号}-1
\]

托马斯第一次听到“下标 2”时，总觉得那应该是第二格。R-01 评价：“人类发明从零编号，然后花大量课程解释为什么从零编号，系统运行稳定。”

无论喜欢与否，读写数组前必须先确认当前使用的是自然序号还是数组下标。错一格不会得到“差不多”的元素，只会得到隔壁那格。

---

## 三、`a[i]` 既可以读，也可以成为写入目标

假设当前数组是：

| 下标 | 0 | 1 | 2 | 3 |
|---|---:|---:|---:|---:|
| `a` 中的值 | 5 | 2 | 5 | 9 |

当 `i=2` 时，读取 `a[i]` 得到 5。若把新值写入 `a[i]`，被改变的只有下标 2 这一格。

生成代码前，先把任务拆开：

- 已知状态：数组有四个有效元素，目标下标是 2，那里当前保存 5。
- 要记住的状态：`i` 表示要修改的位置；本任务不需要保留旧值 5。
- 更新顺序：先用下标定位，再读取旧值并加 3，最后写回同一格。
- 原来的值是否还需要：右边计算时需要旧值 5；同一条赋值先算右边，因此不需额外保存。
- 停止条件：只修改一次，不需要循环。

动作落成代码：

```cpp
int i = 2;
a[i] = a[i] + 3;
```

逐帧状态为：

| 时刻 | `i` | `a[0]` | `a[1]` | `a[2]` | `a[3]` |
|---|---:|---:|---:|---:|---:|
| 开始 | 2 | 5 | 2 | 5 | 9 |
| 读取右边 | 2 | 5 | 2 | 5 | 9 |
| 算出 `5+3` | 2 | 5 | 2 | 5 | 9 |
| 写回 `a[2]` | 2 | 5 | 2 | 8 | 9 |

下标 0、1、3 没有出现在等号左边，所以它们不会变化。数组是一排相邻格子，不是一串用绳子绑住的数字。改写其中一格，不会让其他格子为了保持队形自动挪动。

---

## 四、修改不是插入，覆盖不会腾位置

托马斯在纸上把 `[1,5]` 改成 `[1,3,5]`，只要在中间写个 3，括号会很有礼貌地变长。

内存里如果只有：

| 下标 | 0 | 1 | 2 |
|---|---:|---:|---:|
| 内容 | 1 | 5 | 空闲 |

直接把 3 写到下标 1，会发生覆盖：

| 时刻 | `a[0]` | `a[1]` | `a[2]` |
|---|---:|---:|---:|
| 写入前 | 1 | 5 | 空闲 |
| 写入后 | 1 | 3 | 空闲 |

5 不会自动搬到下标 2。它已经从下标 1 被覆盖，而下标 2 仍然空着。

这个动作叫修改：给已有位置写一个新值。插入则要求原来的元素继续存在，还要为新元素腾出位置。两者的数学结果看起来只差一个数字，内存过程却不相同。

本章先看清格子和地址。下一章会让那枚 5 真正活着搬到右边。现在只需记住：赋值只管左边指定的格子，不附送搬家服务。

---

## 五、数组长度决定哪些地址有效

一排数组若当前有 `n` 个有效元素，下标范围是：

\[
0,1,2,\ldots,n-1
\]

即 \([0,n)\)。

对于长度为 4 的数组：

| 下标 | 0 | 1 | 2 | 3 | 4 |
|---|---:|---:|---:|---:|---:|
| 是否属于数组 | 是 | 是 | 是 | 是 | 否 |

`a[4]` 不是“第五格暂时没放东西”，除非数组本身确实预留了更大的容量。长度和容量也要区分：

- 容量表示这排内存总共准备了多少格；
- 当前长度表示其中有多少格属于现有数据。

例如容量为 6，当前长度为 4：

| 下标 | 0 | 1 | 2 | 3 | 4 | 5 |
|---|---:|---:|---:|---:|---:|---:|
| 是否有物理格子 | 是 | 是 | 是 | 是 | 是 | 是 |
| 是否属于当前数据 | 是 | 是 | 是 | 是 | 否 | 否 |

下标 4、5 可用于以后插入，但现在不应当被当成已有元素参与统计。仓库里有空货位，不等于清单里已经有两箱空气。

访问物理容量之外的位置更危险。程序可能读到无关数据，也可能破坏别的内存。越界并不保证马上发出爆炸声，所以比苦力怕更麻烦：至少苦力怕通常会嘶一声。

---

## 六、同一个数出现两次，靠位置区分

回到事故数组：

\[
[5,2,5,9]
\]

两个 5 在数学值上相等，但它们不是同一个数组元素。

| 描述 | 第一个 5 | 第二个 5 |
|---|---:|---:|
| 值 | 5 | 5 |
| 下标 | 0 | 2 |
| 左边邻居 | 无 | 下标 1 的 2 |
| 右边邻居 | 下标 1 的 2 | 下标 3 的 9 |

如果把下标 0 改成 8，下标 2 的值不会跟着改变。普通整数数组中，每格各自保存一个值；值相等不代表格子相连。

这也是为什么程序有时不仅保存“找到了什么值”，还要保存“它在哪个位置”。例如变量 `value=5` 只能说明目标数值，变量 `position=2` 才能让机器人回到第二个 5 所在的格子。

值与位置可以相同得很巧。例如 `a[2]=2` 时，下标和元素值都写着 2。此时最容易说出含糊句子：“把 2 改掉。”到底是修改下标 2 的格子，还是寻找数值 2？R-01 会要求提交书面补充，而不是替人类猜谜。

---

## 七、根据位置逐格处理

数组的优势在于，一排相关数据可以共用一个名字，再用变化的下标访问不同格子。

假设要把四个库存都增加 1。代码出现前先确定：

- 已知状态：有效下标是 0、1、2、3，当前长度为 4。
- 要记住的状态：`i` 表示当前处理的地址。
- 更新顺序：读取 `a[i]`，加一，写回同一格，然后 `i` 加一。
- 原来的值是否还需要：当前格右边计算时需要，写回后不再需要；其他格尚未读取，不能误改。
- 停止条件：`i` 到达 4 时停止，不访问下标 4。

于是：

```cpp
for (int i = 0; i < 4; i++) {
    a[i] = a[i] + 1;
}
```

以初始数组 `[5,2,5,9]` 为例：

| `i` | 本次修改的格子 | 修改前数组 | 修改后数组 |
|---:|---|---|---|
| 0 | `a[0]` | `[5,2,5,9]` | `[6,2,5,9]` |
| 1 | `a[1]` | `[6,2,5,9]` | `[6,3,5,9]` |
| 2 | `a[2]` | `[6,3,5,9]` | `[6,3,6,9]` |
| 3 | `a[3]` | `[6,3,6,9]` | `[6,3,6,10]` |

每次只有一个左值 `a[i]` 被写入。循环让 `i` 变化，于是同一条动作依次落到不同地址。数学说“每个元素加一”，程序则要让地址牌从 0 走到 3。

---

## 八、错误博物馆：把值当地址，把地址当数量

第一件展品来自一次钻石补货。数组里某格的值是 9，托马斯让 R-01 “修改 9”，机器人被迫追问：是访问下标 9，还是寻找值为 9 的格子？若数组只有四格，前一种解释直接越界；后一种解释还需要一个明确的位置确认过程。一个数字不能同时兼任货物数量和仓库门牌，除非任务清楚说明它的角色。

第二件展品把“第三个元素”翻译成下标 3。数组从 0 编号，第三个元素位于下标 2。错误状态如下：

| 想修改 | 实际使用下标 | 真正被修改 |
|---|---:|---|
| 第三个元素 | 3 | 第四个元素 |

第三件展品认为给 `a[1]` 写入新值后，后面的元素会自动右移。实际内存里只有 `a[1]` 改变，原值被覆盖，长度也不会自动增加。这个错误将在下一章成为正式事故现场。

第四件展品访问 `a[n]`，理由是“数组有 n 个元素”。数量是 n，最后下标却是 `n-1`。门外那块地址牌不因为来访者很有自信就变成合法地址。

第五件展品看到两个值都是 5，便认为改一个会同步修改另一个。状态表立刻拆穿它：

| 动作 | `a[0]` | `a[1]` | `a[2]` | `a[3]` |
|---|---:|---:|---:|---:|
| 初始 | 5 | 2 | 5 | 9 |
| 修改 `a[0]` | 8 | 2 | 5 | 9 |

下标 2 没被写入，所以第二个 5 继续待在原地，甚至不知道前面的同类已经变成了 8。

---

## 九、翻译训练：先报地址，再报内容

### 训练一：读出地址和值

数组为 `[7,4,7]`。请分别回答：下标 1 的值是多少？第二个 7 在哪里？

下标 1 的值是 4；第二个 7 位于下标 2。前一个问题给地址求值，后一个问题根据描述确定地址，方向不同。

### 训练二：预测一次写入

初始数组为 `[3,6,9]`，执行“把 1 号格子的值改成 8”。状态会怎样变化？

| 时刻 | `a[0]` | `a[1]` | `a[2]` |
|---|---:|---:|---:|
| 修改前 | 3 | 6 | 9 |
| 修改后 | 3 | 8 | 9 |

只有下标 1 变化，6 被覆盖，其他格不移动。

### 训练三：自然序号转下标

“第六个元素”的数组下标是多少？若数组从 0 编号，答案是 5。

### 训练四：判断有效范围

数组当前长度为 7，有效下标是 0 到 6。下标 7 是第一个范围外位置。

### 训练五：给动作配音

看到 `a[i]` 出现在右边时，应读成：“到下标 `i` 的格子读取当前值。”

看到 `a[i]` 出现在左边时，应读成：“把右边算出的结果写进下标 `i` 的格子，覆盖那里原来的值。”

### 训练六：区分修改和插入

把 `[2,5,8]` 的下标 1 改成 4，结果是 `[2,4,8]`，长度不变。

在下标 1 插入 4，目标结果是 `[2,4,5,8]`，原来的 5 和 8 必须保留，长度增加。这两个任务不能使用同一套内存动作。

---

## 十、每个数组元素都有姓和门牌号

仓库账本恢复后，托马斯重新向 R-01 下令：“数组 `stock` 当前是 `[5,2,5,9]`。修改下标 2 的元素，把它从 5 增加到 8。”

机器人准确打开第三格。状态变成：

```text
5  2  8  9
```

管理员点点头：“这回是后面那个 5。”

R-01 回答：“更准确地说，是 `stock` 的 2 号格。‘后面那个’在仓库扩建后可能失去精度。”

托马斯没有反驳。机器人确实很难相处，但地址牌比“那个”“这个”“靠后一点的”可靠得多。

数组名说明是哪一排格子，下标说明这一排中的哪一格，元素值说明那格此刻保存什么。把三者分开，才能看懂一次读写究竟去了哪里。

下一章要插入新元素时，真正需要移动的也不是纸上的数字，而是这些有地址的格子中保存的值。格子不会自动让路，地址也不会跟着内容一起漂走。程序必须一格一格安排搬运顺序。
