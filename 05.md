# 第五章　先算右边，还是先改左边

领取牌风波刚结束，工坊又接到一项看似更安全的任务：给北线矿车充能。

充能台有两个读数：`energy` 表示车内已有能量，`crystals` 表示还没消耗的红石晶体。每颗晶体能补 2 点能量，一次充能消耗 1 颗晶体。

充能前：

```text
energy = 12
crystals = 5
```

管理员给出的规则是：

\[
E'=E+2C,\qquad C'=C-1
\]

也就是说，这次先根据原有的 5 颗晶体计算充能结果，能量应变成 22；充能结束后，库存读数变成 4。

托马斯觉得第二个式子短，先写了它。R-01 取走一颗晶体，把 `crystals` 改成 4，然后计算能量：

\[
12+2\times4=20
\]

矿车少了 2 点能量。它在半坡停住，慢慢倒滑，依次撞翻一辆空车、两只木桶和一位正在涨价的村民。

村民当场把“坡道惊吓费”加入了报价。

“公式明明是对的。”托马斯说。

Notch 指着已经变成 4 的晶体读数：“公式里的 \(C\) 指旧值 5。你算它时，旧值已经被你亲手改没了。”

---

## 一、一条赋值内部：右边先完成

先只看一句：

```cpp
energy = energy + crystals * 2;
```

这句话不会从左向右一边读一边胡乱改。赋值有明确的内部顺序：

1. 读取右边需要的当前值；
2. 按运算规则算完整个右边；
3. 把结果写入左边的格子。

若 `energy=12、crystals=5`，计算机先拿到 12 和 5，算出 `12+5*2=22`，最后才用 22 覆盖 `energy` 中的 12。

右边还没算完时，左边不会先被擦掉。否则 `x = x + 1` 这种语句根本没法工作。

逐帧状态如下：

| 帧 | `energy` 格子 | `crystals` 格子 | 右边中间结果 |
|---|---:|---:|---:|
| 开始 | 12 | 5 | 未计算 |
| 读取并乘 2 | 12 | 5 | 10 |
| 完成加法 | 12 | 5 | 22 |
| 写回左边 | 22 | 5 | 22 |

在前三帧里，两个格子的内容都没有变化。表达式的中间结果可以暂时由计算过程拿着，直到最后统一写入 `energy`。

---

## 二、复杂表达式要拆开看

管理员后来又加了一条规则：充能得分等于“基础能量加晶体能量”，再乘线路倍率：

\[
score=(energy+2\times crystals)\times multiplier
\]

已知 `energy=12、crystals=5、multiplier=3`，人眼容易直接得到 66。R-01 的执行画面应当更细：先算 `crystals*2` 得 10，再算 `energy+10` 得 22，最后算 `22*3` 得 66。

如果托马斯想检查每一层，也可以给中间结果安排名字。写代码前先说明：

- 已知状态是 12 点能量、5 颗晶体、倍率 3；
- 要记住晶体贡献 `crystalEnergy` 和倍率前的小计 `subtotal`；
- 这些变量只是保存中间结果，方便检查；
- 计算过程中不修改三个输入值；
- 顺序是晶体贡献、小计、最终得分；
- 三步完成即停止。

于是得到：

```cpp
int crystalEnergy = crystals * 2;
int subtotal = energy + crystalEnergy;
int score = subtotal * multiplier;
```

状态表把长公式展开了：

| 执行后 | `crystalEnergy` | `subtotal` | `score` |
|---|---:|---:|---:|
| 初始 | 未定义 | 未定义 | 未定义 |
| 晶体能量算完 | 10 | 未定义 | 未定义 |
| 小计算完 | 10 | 22 | 未定义 |
| 乘倍率后 | 10 | 22 | 66 |

一行写完并不错误：

```cpp
int score = (energy + crystals * 2) * multiplier;
```

但拆开以后，每个中间值都能被命名、观察和核对。长表达式不是越挤越聪明。把三节矿车硬塞成一节，并不会缩短铁路，只会让车门打不开。

---

## 三、多条赋值之间：前一句会改变后一句

一条赋值内部，右边先算完；多条赋值之间，却不会共享“统一结算时间”。第一句一结束，状态立刻改变，第二句只能读取新状态。

托马斯的错误顺序是：

```cpp
crystals = crystals - 1;
energy = energy + crystals * 2;
```

逐帧推演：

| 步骤 | `energy` | `crystals` | 本步读取的晶体数 |
|---|---:|---:|---:|
| 初始 | 12 | 5 | — |
| 库存减一后 | 12 | 4 | 5 |
| 计算能量后 | 20 | 4 | 4 |

第二句没有算错。它忠实算出了 `12+4*2`。错的是我们希望它读取旧 `crystals=5`，却把这笔数据提前花掉了。

正确顺序应先清偿对旧晶体数的依赖，再修改晶体库存。代码出现前，把状态关系说透：

- 已知 `energy=12、crystals=5`；
- 新能量依赖旧 `energy` 和旧 `crystals`；
- 新晶体数只依赖旧 `crystals`；
- 旧 `crystals` 要被能量公式读取，所以不能先覆盖；
- 先更新 `energy`，再更新 `crystals`；
- 两项更新后停止。

```cpp
energy = energy + crystals * 2;
crystals = crystals - 1;
```

状态成为：

| 步骤 | `energy` | `crystals` |
|---|---:|---:|
| 初始 | 12 | 5 |
| 能量更新后 | 22 | 5 |
| 晶体更新后 | 22 | 4 |

这里不需要 `temp`，因为安全的更新顺序已经保护了所有依赖。临时变量不是每次都要请来的保镖；若能在覆盖前先完成读取，就不必多开一个格子。

---

## 四、谁依赖谁：代码里的欠债关系

Notch 在黑板上画了两张卡：

```text
新 energy  欠 旧 energy、旧 crystals
新 crystals 欠 旧 crystals
```

所谓“欠”，就是计算新值之前还要读取那个旧值。只要债没还清，旧值就不能被覆盖。

判断更新顺序时，不必先盯着代码，可以先画依赖箭头：

```text
旧 crystals ──→ 新 energy
旧 crystals ──→ 新 crystals
旧 energy   ──→ 新 energy
```

新 `energy` 需要旧 `crystals`，所以修改 `crystals` 以前，必须先算出新 `energy`。而修改 `energy` 不会伤害新 `crystals`，因为后者不需要旧 `energy`。安全顺序因此只有一个明显选择：能量在前，库存随后。

如果现实要求必须先扣晶体呢？例如红石机关一拉下，库存传感器立刻减一，无法推迟。这时就要提前保存旧晶体数。

写代码前再检查一次：旧晶体数仍被新能量需要；现实顺序会先覆盖它；因此必须在覆盖前复制到 `oldCrystals`；能量计算使用副本；扣除与充能完成后停止。

```cpp
int oldCrystals = crystals;
crystals = crystals - 1;
energy = energy + oldCrystals * 2;
```

| 步骤 | `energy` | `crystals` | `oldCrystals` |
|---|---:|---:|---:|
| 初始 | 12 | 5 | 未定义 |
| 保存旧值 | 12 | 5 | 5 |
| 先扣库存 | 12 | 4 | 5 |
| 再算能量 | 22 | 4 | 5 |

临时变量的用途不是让代码显得正式，而是在无法避开覆盖时，替未清偿的依赖保留证据。

---

## 五、更新顺序图

连续更新可以用一张小图决定顺序。先写目标，而不是先写语句：

```text
目标 A：newEnergy = oldEnergy + 2 * oldCrystals
目标 B：newCrystals = oldCrystals - 1
```

然后逐项问：

| 目标 | 需要旧 `energy` | 需要旧 `crystals` |
|---|---|---|
| 新能量 | 是 | 是 |
| 新晶体数 | 否 | 是 |

若先写新晶体数，会破坏目标 A 需要的旧晶体数；若先写新能量，不会破坏目标 B。于是 A 先、B 后。

有时表里会出现环：新 A 需要旧 B，新 B 又需要旧 A。那就无法仅靠重排解决，必须保存旧值。两个变量交换是最小的环，下一章三个变量同时变化时，环会更热闹。

R-01 评价：“人类称其为依赖图。我称其为‘覆盖以前请先查看欠款’。”

---

## 六、错误博物馆：每一行都对，合起来仍然错

第一件展品仍是这两句：

```cpp
crystals--;
energy += crystals * 2;
```

语法正确，单看每句也都符合某种动作。可组合顺序让第二句读错了时刻。错误标签写着：

> 把“旧晶体数”误翻译成了“扣除后的晶体数”。

第二件展品少了括号：

```cpp
int score = energy + crystals * 2 * multiplier;
```

若数学目标是 `(energy + crystals*2) * multiplier`，上式只让晶体贡献乘倍率，基础能量没有乘。它算的是另一条完全合法的公式。机器不会因为你心里画过括号就自动补上。

第三件展品把中间结果写回输入：

```cpp
energy = crystals * 2;
energy = energy + 12;
```

在当前例子里也许碰巧得到 22，但原有能量若不是 12，第二句仍硬加 12。变量名 `energy` 一会儿表示“晶体贡献”，一会儿又表示“总能量”，状态含义中途换了工作。更清楚的做法是使用 `crystalEnergy`，或直接在右边读取原状态。

错误博物馆不只收藏算术错误。许多程序算得非常准确，只是准确地计算了错误时刻、错误含义或错误公式。

---

## 七、翻译训练

### 训练一：判断读取时刻

初始 `x=4、y=3`：

```cpp
x = x + y;
y = x * 2;
```

第一句后 `x=7`；第二句读取新 `x`，所以 `y=14`。它不是用旧 `x=4` 算出 8。

### 训练二：决定安全顺序

数学目标：

\[
A'=A+B,\qquad B'=B-1
\]

新 A 需要旧 B，新 B 不需要旧 A，因此应先更新 A，再更新 B。若必须先改 B，就提前保存旧 B。

### 训练三：给表达式分层

把：

\[
result=(a+b)\times(c-2)
\]

翻译成可检查的状态。可以设计 `left=a+b`、`right=c-2`，最后 `result=left*right`。两个中间量谁先算都行，因为它们只读输入，彼此不依赖。

### 训练四：从表还原代码

| 步骤 | `fuel` | `heat` | `oldFuel` |
|---|---:|---:|---:|
| 初始 | 6 | 10 | 未定义 |
| 保存后 | 6 | 10 | 6 |
| 扣除后 | 5 | 10 | 6 |
| 加热后 | 5 | 28 | 6 |

对应动作是先保存 `fuel`，再让 `fuel` 减一，最后用旧燃料数计算 `heat = heat + oldFuel * 3`。

北线矿车重新充到 22 点能量，顺利爬上坡道。托马斯把事故记录改成一句更准确的话：

> 一条赋值先算右边再写左边；多条赋值之间，前一句写下的新状态会立刻成为后一句读到的世界。

R-01 在旁边补充：“矿车已到站。受惊村民也已到站，并再次上调了报价。”