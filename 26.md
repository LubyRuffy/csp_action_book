# 第二十六章　让 R-01 独立完成一次仓库任务

皇家仓库年度检查那天，Notch 把主控制室钥匙交给托马斯。

“今天没有单独的小实验。”他说，“R-01 要接收一批库存记录，插入新到货，删除一条损坏记录，再报告总库存、空箱数、最大库存及位置，最后按分段规则算搬运费。”

托马斯接过钥匙：“这些我都学过。”

“分别学过。”Notch 纠正，“今天麻烦会出现在它们互相靠得太近时。”

果然，第一版说明只写了“先处理记录，再统计”。R-01 抬头问：“处理是插入后删除，还是删除后插入？删除位置按处理前还是处理后编号？费用按原库存还是最终库存？”

门外排队的村民开始发出不耐烦的“哼”。托马斯这次没有怪机器人。他把模糊说明收回，决定先把整个任务变成一条清楚的状态时间线。

---

## 一、任务说明：先约定世界，再写代码

输入规则如下：

1. 第一行是原记录数 `n`，满足 `1<=n<=99`；
2. 第二行是 `n` 个非负库存；
3. 第三行给出 `insertPos insertValue`，位置采用从 0 开始的下标，允许插在 `0..n`；
4. 第四行给出 `deletePos`，它指的是插入完成后的数组位置。

执行顺序固定为：

```text
读取原记录
→ 插入新记录
→ 删除损坏记录
→ 对最终数组统计
→ 按最终总库存计算费用
→ 输出最终状态
```

分段费用设最终总库存为 \(S\)：

\[
fee(S)=
\begin{cases}
2S,&S\le20,\\
40+1.5(S-20),&20<S\le50,\\
85+(S-50),&S>50.
\end{cases}
\]

最大位置内部使用 0 下标，输出时加 1，显示为仓库人员习惯的“第几个箱子”。若有多个相同最大值，保留第一次出现的位置。

这些约定不是程序旁边的闲话。输入下标、操作顺序和费用依据一旦含糊，同一段代码可以得到多种“都像有道理”的答案，而 R-01 会选择其中最让人类惊讶的一种。

---

## 二、数学视角：六个目标，前后有依赖

对最终数组 \(a_0,a_1,\ldots,a_{n-1}\)：

总库存：

\[
S=\sum_{i=0}^{n-1}a_i
\]

空箱数：

\[
C=\#\{i\mid 0\le i<n,\ a_i=0\}
\]

最大库存与第一次出现位置：

\[
M=\max(a_0,a_1,\ldots,a_{n-1}),
\qquad P=\min\{i\mid a_i=M\}
\]

插入位置 `p` 时，原下标 `p..n-1` 的元素向右一格；删除位置 `q` 时，`q+1..n-1` 的元素向左一格。两次移动会改变后续统计看到的数据和位置，因此统计必须在它们之后。

程序需要保存的状态是：

| 状态 | 含义 | 初始来源 |
|---|---|---|
| `n` | 当前有效记录数 | 输入，插入后加一，删除后减一 |
| `sum` | 已处理记录总和 | 0 |
| `emptyCount` | 已发现空箱数 | 0 |
| `maxValue` | 已处理部分最大值 | 最终数组 `a[0]` |
| `maxPosition` | 当前最大值第一次出现位置 | 0 |
| `fee` | 根据最终 `sum` 算出的费用 | 统计完成后计算 |

---

## 三、先让数组完成插入和删除

示例输入：

```text
5
4 0 7 2 0
2 6
4
```

原数组有五项。在位置 2 插入 6，然后删除插入后位置 4 的记录。

插入逐帧表：

| 动作 | `a[0]` | `a[1]` | `a[2]` | `a[3]` | `a[4]` | `a[5]` | `n` |
|---|---:|---:|---:|---:|---:|---:|---:|
| 初始 | 4 | 0 | 7 | 2 | 0 | ? | 5 |
| 右移下标 4 | 4 | 0 | 7 | 2 | 0 | 0 | 5 |
| 右移下标 3 | 4 | 0 | 7 | 2 | 2 | 0 | 5 |
| 右移下标 2 | 4 | 0 | 7 | 7 | 2 | 0 | 5 |
| 写入 6，长度加一 | 4 | 0 | 6 | 7 | 2 | 0 | 6 |

删除位置 4：

| 动作 | 有效数组 | `n` |
|---|---|---:|
| 删除前 | `4 0 6 7 2 0` | 6 |
| `a[4]=a[5]` | `4 0 6 7 0 0` | 6 |
| 长度减一 | `4 0 6 7 0` | 5 |

最后一格中可能还残留 0，但 `n=5` 后它不属于有效数组。删除不是把物理内存擦成白色，而是用左移覆盖并缩短有效范围。

---

## 四、完整程序怎样从小动作组成

下面六段按顺序属于同一个 `warehouse.cpp` 文件。为了让每个动作仍能逐行配音，函数和主流程分段展示；依次拼接就是完整可编译程序，没有省略任何一行。

第一段，费用函数：

```cpp
#include <iostream>
#include <iomanip>
using namespace std;

double calcFee(int total) {
    if (total <= 20) return total * 2.0;
    if (total <= 50)
        return 40 + (total - 20) * 1.5;
    return 85 + (total - 50);
}
```

第二段，插入函数从右向左腾位置：

```cpp
void insertRecord(int a[], int &n, int pos, int value) {
    for (int i = n; i > pos; i--) {
        a[i] = a[i - 1];
    }
    a[pos] = value;
    n++;
}
```

第三段，删除函数从左向右覆盖：

```cpp
void deleteRecord(int a[], int &n, int pos) {
    for (int i = pos; i < n - 1; i++) {
        a[i] = a[i + 1];
    }
    n--;
}
```

第四段，主函数读取并按约定修改数组：

```cpp
int main() {
    int n, a[101];
    cin >> n;
    if (n < 1 || n > 99) return 0;
    for (int i = 0; i < n; i++) cin >> a[i];

    int insertPos, insertValue, deletePos;
    cin >> insertPos >> insertValue >> deletePos;
    if (insertPos < 0 || insertPos > n) return 0;
    insertRecord(a, n, insertPos, insertValue);
    if (deletePos < 0 || deletePos >= n) return 0;
    deleteRecord(a, n, deletePos);
```

第五段，扫描最终数组：

```cpp
    int sum = 0, emptyCount = 0;
    int maxValue = a[0], maxPosition = 0;
    for (int i = 0; i < n; i++) {
        sum += a[i];
        if (a[i] == 0) emptyCount++;
        if (a[i] > maxValue) {
            maxValue = a[i];
            maxPosition = i;
        }
    }
    double fee = calcFee(sum);
```

第六段，输出并结束：

```cpp
    cout << "Final:";
    for (int i = 0; i < n; i++) cout << ' ' << a[i];
    cout << "\nSum: " << sum;
    cout << "\nEmpty: " << emptyCount;
    cout << "\nMax: " << maxValue;
    cout << "\nPosition: " << maxPosition + 1;
    cout << fixed << setprecision(1) << "\nFee: " << fee << '\n';
    return 0;
}
```

每个函数只做一种生活动作。代码规模变大了，映射模式没有变：数组格、赋值、条件、循环、累加器、计数器、当前候选和安全移动。

---

## 五、统计过程的状态检查

最终有效数组为 `4,0,6,7,0`。

| `i` | `a[i]` | 更新后 `sum` | 更新后 `emptyCount` | `(maxValue,maxPosition)` |
|---:|---:|---:|---:|---|
| 初始 | — | 0 | 0 | `(4,0)` |
| 0 | 4 | 4 | 0 | `(4,0)` |
| 1 | 0 | 4 | 1 | `(4,0)` |
| 2 | 6 | 10 | 1 | `(6,2)` |
| 3 | 7 | 17 | 1 | `(7,3)` |
| 4 | 0 | 17 | 2 | `(7,3)` |

总库存 17 落在第一段费用中：

\[
fee=2\times17=34.0
\]

预期输出：

```text
Final: 4 0 6 7 0
Sum: 17
Empty: 2
Max: 7
Position: 4
Fee: 34.0
```

位置输出为 4，是因为内部下标 3 加一后对应“第四个箱子”。若说明书不写这一点，程序和仓库员会各自正确地争论一整天。

---

## 六、逐段配音

`insertRecord` 的声音是：

> 从当前有效范围右边的空格开始，把左邻记录复制过来，一直退到插入位置右边；写入新记录，再扩大有效长度。

`deleteRecord` 的声音是：

> 从待删除位置开始，把右邻记录逐个复制到左边，最后缩短有效长度。

统计循环的声音是：

> 每到一条最终记录，把库存加入目前总和；若它为零，空箱数加一；若它严格大于当前最大值，同时更新最大值与位置。

费用函数的声音是：

> 先检查总库存是否不超过 20；否则检查是否不超过 50；前两段都不满足时进入第三段。每一段都把前面区间的累计费用保留下来，只对超出部分使用新单价。

能完整配音，说明这份程序不是由六段模板粘在一起，而是一条状态连续的仓库时间线。

---

## 七、错误博物馆：综合任务最容易错在哪里

### 1. 先统计，后修改

若在插入删除前计算 `sum`，后面的数组变了，`sum` 不会自动更新。普通变量不是库存镜子。

### 2. 删除位置使用旧编号

本任务明确规定 `deletePos` 针对插入后的数组。若按原数组理解，会删除另一条记录。下标含义也随时间变化。

### 3. 插入后忘记 `n++`

新值虽然写入内存，却不在有效范围中；后续统计看不见它。

### 4. 删除后忘记 `n--`

末尾旧副本仍被统计一次，等于把已经删除的记录又请回来参加结算。

### 5. 最大值和位置分开更新

值变成 7，位置仍停在 2，会制造不匹配的一对状态。

### 6. 分段费用重复计价

第二段不能写成 `total*1.5+40`。40 已代表前 20 单位费用，后面只应计算 `total-20` 的超出部分。

### 7. 输出位置忘记换编号

内存下标 3 与仓库口语“第 4 个”都正确，缺少转换才会造成事故。

---

## 八、翻译训练

### 1. 改变操作顺序

若先删除原位置 4，再在位置 2 插入 6，示例最终数组仍可能巧合相同吗？先实际画表。本例先删最后的 0 得 `4,0,7,2`，再插入得 `4,0,6,7,2`，与原任务的 `4,0,6,7,0` 不同。

### 2. 改费用段

若总库存为 35，费用是 `40+(35-20)*1.5=62.5`。请指出 40 对应哪部分：前 20 单位。

### 3. 改最大位置约定

若要保留最后一次最大值位置，把条件 `>` 改为 `>=`，并用 `[7,2,7]` 画状态表验证。

### 4. 代码到数学

`emptyCount++` 受 `a[i]==0` 控制，对应集合 `\{i\mid a_i=0\}` 的元素个数。

### 5. 只补状态

原数组 `[1,5,3]`，在位置 1 插入 4，再删位置 2。插入后 `[1,4,5,3]`，删除后 `[1,4,3]`。总和 8，空箱 0，最大值 4，内部位置 1。

### 6. 查第一帧错误

若插入结果出现 `[1,4,5,5]`，先检查移动边界是否漏搬最后一项，或 `n++` 后是否误把末尾副本当成新增有效数据。

---

## 九、R-01 的独立执行

托马斯把状态说明、完整程序和示例表一起交给 R-01，然后退到安全线外。

机器人读取五项记录，从右向左腾出二号格，放入 6；再从四号格开始向左覆盖，缩短长度。接着它从零号格走到四号格。总和从 0 变成 17，空箱数从 0 变成 2，最大候选从 4 变成 6，再变成 7。最后，17 进入费用第一段，得到 34.0。

报表与手工状态表逐项一致。门外的村民看了三遍，没有发出“哼”。在皇家仓库，这已经算是极高评价。

Notch 问托马斯：“今天 R-01 独立完成了吗？”

托马斯想了想：“它独立执行了。但道路是我们先铺好的。”

R-01 把钥匙还给他：“机器可以独立走完明确步骤。若步骤含糊，机器也可以独立制造一份非常完整的事故报告。”

这一章没有引入复杂算法。它只是把全书学过的翻译放在同一条时间线上：求和保存目前总数，计数保存目前个数，最大值保存目前候选，分段函数变成互斥条件，插入先保护右侧旧值，删除用右邻覆盖左格。程序之所以完整，不是因为代码变长，而是因为每一个数学关系都找到了自己的状态、顺序和边界。