# 第四章　交换两个数字，为什么需要第三个格子

自动烤炉修好后的第二天，皇家仓库来了两位村民。

甲村民寄存了 6 颗钻石，乙村民寄存了 17 颗绿宝石。负责贴标签的托马斯把两张领取牌贴反了：写着“甲”的牌子挂在乙的箱子上，写着“乙”的牌子挂在甲的箱子上。

村民们很快发现了这件事。原因并不复杂：甲村民忽然变得非常满意，乙村民则发出了一长串足以让玻璃震动的“哼”。

“只要交换两张牌上的编号。”Notch 说。

托马斯把两个编号存进变量：

```text
x = 6
y = 17
```

他的数学目标十分清楚：

\[
x'=17,\qquad y'=6
\]

于是他让 R-01 执行两句命令。第一句把 `y` 复制给 `x`，第二句把 `x` 复制给 `y`。

几秒后，两张牌都变成了 17。

甲村民更加满意了。乙村民的声音已经接近红石警报器。

R-01 报告：“交换完成。附注：数字 6 已不存在。”

托马斯盯着两张一模一样的领取牌：“你为什么不把原来的 6 给 `y`？”

“因为第二句执行时，`x` 已经不是原来的 6。”

---

## 一、数学里的“同时”到了仓库就散了

在数学里写：

\[
x'=y,\qquad y'=x
\]

撇号已经帮我们区分了新旧：新 `x` 取旧 `y`，新 `y` 取旧 `x`。两个关系并排写着，人脑会把它们当作同时发生。

程序却没有“同时写两格”的普通赋值。它一次只能改一个格子。假设先执行 `x = y`，`x` 原来的 6 立即被覆盖；下一句 `y = x` 读取到的当然是新 `x=17`。

内存里没有“你应该知道我指的是刚才那个 `x`”这种服务。刚才那个值若没被保存，就只剩托马斯的懊悔，而懊悔不是 C++ 数据类型。

错误过程可以逐帧观看：

| 步骤 | `x` | `y` | 发生了什么 |
|---|---:|---:|---|
| 初始 | 6 | 17 | 两个旧值都在 |
| `x = y` 后 | 17 | 17 | 6 被覆盖 |
| `y = x` 后 | 17 | 17 | 复制的是新 `x` |

这段代码不是交换，而是先复制，再把复制结果又复制一次。

---

## 二、第三个格子不是多余，是避难所

Notch 拿来一个空木箱，贴上 `temp`。

“先把 6 放这里。”

托马斯明白了。只要在覆盖 `x` 以前把旧 `x` 暂存，后面就仍能找到它。

动手写代码前，先把任务问完整：

- 已知状态：`x=6`，`y=17`；
- 要记住的状态：覆盖前的旧 `x`；
- 更新目标：新 `x` 取旧 `y`，新 `y` 取旧 `x`；
- 旧值是否还需要：旧 `x` 在最后一步还要用；
- 更新顺序：先保存旧 `x`，再覆盖 `x`，最后恢复到 `y`；
- 停止条件：三次写入完成，不需要循环。

于是代码自然出现：

```cpp
int temp = x;
x = y;
y = temp;
```

`temp` 不是第三个需要交换的人。它只是一间临时避难所。

状态表比口头承诺可靠：

| 执行语句 | `x` | `y` | `temp` | 哪个旧值仍安全 |
|---|---:|---:|---:|---|
| 初始 | 6 | 17 | 未定义 | 6 在 `x`，17 在 `y` |
| `temp = x` | 6 | 17 | 6 | 旧 `x` 已备份 |
| `x = y` | 17 | 17 | 6 | 旧 `y` 已进入 `x` |
| `y = temp` | 17 | 6 | 6 | 两个目标都完成 |

第二行执行后，两个主格子暂时都是 17。这并不表示失败。因为 6 还活在 `temp` 里，中间状态可以看起来重复，只要下一步需要的信息没有丢。

R-01 把两张领取牌改回正确编号。甲村民的满意维持了不到一分钟，乙村民清点完自己的绿宝石后才停止报警。

---

## 三、保存、覆盖、恢复

交换过程可以压缩成三个动作：

1. 保存：`temp` 复制旧 `x`；
2. 覆盖：`x` 接收旧 `y`；
3. 恢复：`y` 接收 `temp` 中的旧 `x`。

“恢复”并不是从被覆盖的 `x` 里找回 6。那个格子里的 6 确实已经没了。我们能恢复，是因为提前做了副本。

这一区别以后非常要紧。数组插入时，挡路的元素必须先搬到安全格；递推更新时，后面还要用的旧项必须先留住。问题表面不同，内存里的危险相同：

> 一个值即将被覆盖，但未来步骤还欠它一次读取。

只要有这笔“欠债”，就不能让它先消失。

托马斯问：“为什么保存 `x`，不能保存 `y`？”

“也可以，”Notch 说，“但后面的顺序要跟着改变。”

如果选择保存旧 `y`，先回答同样的问题：旧 `y` 已在 `temp` 中，接下来可以让 `y` 接收旧 `x`，最后让 `x` 接收备份。

```cpp
int temp = y;
y = x;
x = temp;
```

状态会走另一条安全路线：

| 步骤 | `x` | `y` | `temp` |
|---|---:|---:|---:|
| 初始 | 6 | 17 | 未定义 |
| 保存旧 `y` | 6 | 17 | 17 |
| `y = x` | 6 | 6 | 17 |
| `x = temp` | 17 | 6 | 17 |

保存谁并无神秘规定。关键是，先保存那个即将被覆盖、以后又要使用的值。

---

## 四、为什么不能先覆盖，再补救

托马斯提出一个听起来很节省格子的方案：

“我先执行 `x = y`。等发现 6 不见了，再创建 `temp` 保存旧 `x`。”

R-01 回答：“届时 `temp` 将保存 17。建议同时创建变量 `regret`，但它不能恢复数据。”

赋值造成的覆盖是即时的。下面这种补救顺序没有用：

```cpp
x = y;
int temp = x;
y = temp;
```

逐帧结果仍然是：

| 步骤 | `x` | `y` | `temp` |
|---|---:|---:|---:|
| 初始 | 6 | 17 | 未定义 |
| 先覆盖 `x` | 17 | 17 | 未定义 |
| 再保存 `x` | 17 | 17 | 17 |
| 写回 `y` | 17 | 17 | 17 |

保存的时机比变量名字更重要。一个叫 `oldX` 的变量，如果在 `x` 已更新后才赋值，装的仍然是新值。标签写“昨日面包”不会让刚烤出的面包穿越到昨天。

---

## 五、错误博物馆：聪明办法为什么常把事情弄笨

交换展柜里还有两种常见作品。

第一种用加减法，不要临时变量：

```cpp
x = x + y;
y = x - y;
x = x - y;
```

在 `x=6、y=17` 时，它确实得到 `17、6`。但第一句必须先保存 23。若两个整数本来就接近 `int` 能表示的上限，`x+y` 可能超出范围。在 C++ 的有符号整数里，这不是一个值得依赖的结果。

而且这种写法难配音、难检查。一旦某行写错，两个旧值会一起缠在算式里。为了省一个普通临时格，换来溢出风险和三行谜语，很像村民为了少付一颗绿宝石，把整间交易所抵押出去。

第二种用异或运算交换整数。它不需要额外格子，却只适合特定类型，读者还得先理解位运算；如果两个名字实际指向同一存储位置，更可能把值清成零。本书要学的是保存旧状态，不是参加“谁能把三行代码写得最像古代符文”的比赛。

在真实 C++ 程序里，若已经使用标准库，也可以写 `std::swap(x, y)`。它清楚而可靠。但学习这一章仍要会画出 `temp` 的状态表，因为库函数替你完成了动作，并没有取消覆盖与保存的规律。

错误博物馆给“无临时变量交换”贴的说明是：

> 能短不等于容易理解；看起来聪明，也不等于更安全。

---

## 六、从两个格子推广出去

交换的不一定是数字。两个格子可以保存分数、坐标、箱子编号，只要类型允许赋值，保存—覆盖—恢复的思路都成立。

例如两名矿工站位分别是 `leftWorker=12`、`rightWorker=25`，需要互换位置编号。数学仍写成新左取旧右、新右取旧左，内存仍需要保护一个旧值。

交换也不一定真的需要 `temp`。如果目标只是让 `x` 取得 `y`，而旧 `x` 以后不再使用，那叫覆盖，不叫交换。不要看到两个变量就条件反射地准备第三个。临时变量出现的理由只有一个：某个将被覆盖的旧值仍然有用。

---

## 七、翻译训练

### 训练一：给错误代码配音

初始 `a=4、b=9`：

```cpp
a = b;
b = a;
```

不要说“交换失败”，先说动作：第一句把 9 写进 `a`，覆盖 4；第二句读取已经变成 9 的 `a`，再把 9 写进 `b`。轨迹是 `(4,9) → (9,9) → (9,9)`。

### 训练二：排列顺序

把下面三句排成能交换 `gold` 与 `iron` 的顺序：

```cpp
iron = temp;
temp = gold;
gold = iron;
```

正确顺序是先 `temp = gold`，再 `gold = iron`，最后 `iron = temp`。判断依据不是背序号，而是确保旧 `gold` 在第一次覆盖前已经保存。

### 训练三：补状态表

初始 `x=2、y=8`，执行标准交换：

| 步骤 | `x` | `y` | `temp` |
|---|---:|---:|---:|
| 初始 | 2 | 8 | 未定义 |
| 保存后 | 2 | 8 | 2 |
| 覆盖后 | 8 | 8 | 2 |
| 恢复后 | 8 | 2 | 2 |

### 训练四：判断是否需要保存

任务是“把 `x` 改成 `y` 当前的值，以后不再需要旧 `x`”。这里只要 `x = y`，不需要 `temp`。

任务若改成“让 `x` 和 `y` 互换”，旧 `x` 必须活到写入 `y` 的那一步，所以需要提前保存，或调用明确实现同一目的的交换工具。

领取牌事故解决后，Notch 把空木箱放回架子。托马斯以前觉得多一个 `temp` 是代码写得不够漂亮。现在他知道，这个不起眼的第三格承担了一件很具体的工作：

> 在新状态到来之前，替未来保管仍然欠着用途的旧值。

R-01 在事故记录末尾补充：“交换成功。两位村民各自取回财物。遗憾的是，他们都要求精神损失费。”